# 项目名称

Novel Agent 世界观显示名、hierarchy-agent 人工决策、LLM 节点提示词、worldview 节点 LLM 身份拆分与 outline 长大纲保留修复

## 测试范围

- `frontend/src/pages/HierarchyWorkflow.tsx` 世界观选择框展示文本。
- `frontend/src/pages/OutlineChapterWorkflow.tsx` 世界观选择框与导入目标世界观选择框展示文本。
- `frontend/src/pages/WorldHierarchy.tsx` 关联世界观选择框展示文本。
- `frontend/src/pages/WorldviewManagement.tsx` 世界观卡片标题与编辑跳转名称参数。
- `frontend/src/pages/LoreDB.tsx` 世界观列表与当前选中世界观名称展示。
- `frontend/src/pages/NovelOutlineManagement.tsx` 大纲列表中的世界观列展示文本。
- `frontend/src/pages/WorldDetail.tsx` 世界详情页的直接保存与删除入口。
- `frontend/src/pages/NovelDetail.tsx` 小说详情页的直接保存与删除入口。
- `frontend/src/utils/worldview.ts` 世界观显示名统一解析逻辑。
- `src/app_api.py` hierarchy-agent 人工决策接口的 `request_changes` / `reject` 分支。
- `frontend/src/pages/HierarchyWorkflow.tsx` 工作流“节点”面板中的 LLM 节点提示词展示。
- `src/app_api.py` 旧 `hierarchy_agent_runs` 读路径中的 LLM prompt 补显逻辑。
- `src/agents/world_agent.py` / `worldview_agent.py` / `novel_agent.py` / `outline_agent.py` / `chapter_agent.py` 的 LLM 元数据写出。
- `src/agents/review_agent.py` 与 `src/agents/review_nodes/*` 的审查节点 LLM 提示词与调用明细写出。
- `scripts/debug_worldview_llm.py` 与 `scripts/debug_worldview_llm` 固定单命令世界观一致性 LLM 调试入口。
- `src/agents/worldview_agent.py` 世界观 `initial_expansion` 与 `modify_content` 节点的独立 LLM agent identity。
- `config/llm.yml` worldview 节点级 LLM 配置入口。
- `src/app_api.py` 旧 worldview run 读接口与列表接口的节点级 LLM identity 补显逻辑。
- `tests/test_real_agent_worldview_flow_requests.py` / `tests/test_hierarchy_draft_llm_invocation.py` 的 worldview 节点级 LLM identity 回归测试。
- `src/agents/worldview_agent.py` 修改内容节点 Prompt 的三步工作流风格改写，以及 `payload` 主合同与 `有效载荷` 兼容解析。
- `src/agents/worldview_agent.py` 初始扩充节点 Prompt 的中文风格改写，以及 `payload` 主合同与 `有效载荷` 兼容解析。
- `src/agents/world_agent.py` / `novel_agent.py` / `outline_agent.py` / `chapter_agent.py` 的生成与修改 Prompt 统一为“角色设定 / 操作流程 / 输入信息 / 输出要求”结构化模板。
- `src/agents/review_agent.py` 的审查系统提示词统一为“角色设定 / 操作流程 / 输出要求”结构化模板。
- `src/agents/outline_agent.py` 大纲 Prompt 的“长大纲保留并扩充、禁止摘要化”约束。
- `tests/test_hierarchy_draft_llm_invocation.py` 大纲 Prompt 防压缩回归测试。

## 不在范围

- MongoDB 数据模型变更。
- 世界、小说、大纲、章节的存储字段重构。
- `/api/worldviews/*` 接口字段结构调整。
- 未触达页面的 UI 文案统一。
- 历史 run 的 MongoDB 数据回填迁移脚本。

## 风险清单

- 历史数据可能只有 `title` 没有 `name`，前端若仍直接读取 `name` 会退回显示空白或 ID。
- 不同页面对同一世界观采用不同展示规则，容易出现“列表是名称、下拉是 ID”的不一致。
- 章节/大纲工作流存在必填上下文校验，如果修复时误伤选择器状态，可能导致页面不可操作。
- `/api/hierarchy-agent/respond` 若只支持 `approve`，前端“提交修改”会稳定 400，导致真实工作流无法继续。
- 历史 `hierarchy_agent_runs` 里的审查节点没有持久化 `llm_call.prompt`，如果只改写新 run，旧 run 页面仍看不到真实提示词。
- 如果前端不区分 LLM / 非 LLM 节点，`input`、`human` 之类节点会误显示 `LLM 语句`，形成错误审计信息。
- 如果旧 run 的提示词重建逻辑和真实发送给模型的 system/user message 不一致，页面会展示伪提示词，违反真实链路要求。
- 如果调试入口不是固定双文件模式，仍要求手写 Python import、切解释器、复制其他过程或传 CLI 参数，用户会继续绕开现有链路，无法稳定复现和修改 LLM 语句。
- 如果 worldview 的新增节点与改写节点继续共用 `worldview_agent` 标识，工作流节点详情、日志和后续审计会误判为同一个 LLM 调用，无法分辨“首次扩写”与“人工修改”。
- 如果只修新 run、不修读接口补显，历史 worldview run 列表与详情仍会显示旧的 `worldview_agent`，形成新旧数据不一致。
- 如果只替换修改提示词文本、不兼容 `payload` 主合同或 `有效载荷` 兼容解析，真实 modify_content 链路会直接解析失败。
- 如果只替换创建提示词文本、不兼容 `payload` 主合同或 `有效载荷` 兼容解析，真实 initial_expansion 链路会直接解析失败。
- 如果只改 worldview，不改 world / novel / outline / chapter / review 的 Prompt，模型行为仍会继续在其他链路中出现职责混乱、步骤不清和无效内容过多的问题。
- 如果大规模改 Prompt 但不做真实 workflow 验证，容易把 create/update 链路 silently 改坏，而代码层面无法提前发现。
- 如果 outline 继续把 `summary` 当短摘要而不是完整大纲正文，长篇大纲会被模型主动压缩，导致剧情节点、分卷分章结构和细节丢失。
- 如果世界详情页只有“进入 Agent 工作流”而没有直接保存/删除入口，用户无法在 `/worlds/:id` 详情页完成基础维护，属于主路径功能缺失。
- 如果小说详情页只有“进入 Agent 工作流”而没有直接保存/删除入口，用户无法在 `/novels/:id` 详情页完成基础维护，属于主路径功能缺失。

## 测试目标

- 证明正常情况稳定可用：真实页面中的世界观选择列表和世界观列显示名称，不再把 `worldview_id` 当成人类可读名称。
- 证明异常和特殊情况不会把系统拖入失控：缺少必填上下文时页面仍应正确降级，且已加载的世界观名称展示不能退化为 ID。
- 主动发现未知问题：通过真实 HTTP 返回与真实 DOM 快照比对，发现旧页面残留的 `worldview_id` 直接渲染点。
- 为稳定性增强、回滚设计、降级设计提供证据：保留构建结果、浏览器 DOM 证据和反向补丁 dry-run 结果。
- 证明 hierarchy-agent 人工决策接口对 `request_changes` / `reject` 返回真实 200 状态流转，而不是最小后端 400。
- 证明 `/workflow/worldview?...run_id=run_35a552901bab` 的 LLM 节点详情不仅显示输入/输出，还显示真实 LLM 提示词文本。
- 证明历史 run 即使数据库里原本没有 `llm_call.prompt`，读接口也能按节点类型重建并返回提示词。
- 证明非 LLM 节点不会误显示 `LLM 语句` 区块，避免把普通节点伪装成 AI 调用节点。
- 证明用户只需修改固定的 `worldview_consistency.prompt.txt` 并重复执行同一条无参命令，就能看到真实世界观一致性 LLM 返回结果。
- 证明 worldview 的 `initial_expansion` 节点真实使用 `worldview_agent_initial_expansion`，`modify_content` 节点真实使用 `worldview_agent_modify_content`，两者不再共用同一 LLM 标识。
- 证明历史 worldview run 的详情接口与列表接口都会补显拆分后的 LLM identity，而不是继续暴露旧的 `worldview_agent`。
- 证明 worldview 修改节点已切换到新的“审查 -> 修正 -> 扩展”中文风格 Prompt，内部输出合同保持 `payload`，并兼容 `有效载荷` 解析。
- 证明 worldview 创建节点已切换到新的“审查 -> 扩展”中文风格 Prompt，内部输出合同保持 `payload`，并兼容 `有效载荷` 解析。
- 证明 world / novel / outline / chapter / review 的 Prompt 已统一为结构化模板，减少无效内容、职责漂移和任务指令不清。
- 证明 outline Prompt 已明确要求：`summary` 承载完整大纲正文，长大纲必须保留并继续扩充，禁止压缩成短摘要，且完整大纲至少包含卷名、本卷简介、开篇、主要人物、主要矛盾、主要事件、转折、高潮、结局。
- 证明至少 `novel` 的 live create workflow 在 Prompt 重写后仍能完成创建、审批、读回与清理闭环。
- 证明 `/worlds/:worldId` 详情页提供直接“保存修改”和“删除世界”入口，且真实 `/api/worlds/update`、`/api/worlds/delete` 读写链路可用。
- 证明 `/novels/:novelId` 详情页提供直接“保存修改”和“删除小说”入口，且真实 `/api/novels/update`、`/api/novels/delete` 读写链路可用。

## 测试环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- 前端地址：`http://127.0.0.1:5174`
- 后端地址：`http://127.0.0.1:5006`
- 数据库：本地 MongoDB 已可读，`world_default` 下存在世界观数据
- 浏览器验证：Codex in-app Browser
- 执行时间区间：2026-06-17 20:40 CST - 2026-06-17 22:38 CST

## 测试入口

- 页面入口 1：`/workflow/chapter?action=create&world_id=world_default`
- 页面入口 2：`/novels/novel_for_outline_detail_8b8c07a3/outlines`
- 页面入口 3：`/workflow/worldview?action=create&world_id=world_agent_novel_8ed501d17a&run_id=run_35a552901bab`
- 接口入口：`GET /api/worldviews/list?world_id=world_default&page=1&page_size=5`
- 接口入口：`GET /api/hierarchy-agent/get?run_id=run_35a552901bab`
- 接口入口：`GET /api/hierarchy-agent/list?agent_type=worldview&page=1&page_size=10`
- 接口入口：`POST /api/hierarchy-agent/start`
- 接口入口：`POST /api/hierarchy-agent/respond`
- 语法入口：`python3 -m py_compile src/app_api.py ...`
- 调试入口：`./scripts/debug_worldview_llm`
- 构建入口：`cd frontend && npm run build`
- 回滚验证入口：`git diff -- <files> | git apply --check -R -`

## 测试数据与前置准备

- 前置 1：确保前端 dev server 监听 `127.0.0.1:5174`。
- 前置 2：确保后端 Flask 服务监听 `127.0.0.1:5006`。
- 前置 3：确认 `world_default` 下存在以下世界观数据：
  - `kehuan -> 科幻`
  - `default_wv -> 默认世界观 (Default Worldview)`
- 前置 4：确认小说 `novel_for_outline_detail_8b8c07a3` 存在大纲 `outline_detail_8b8c07a3`，其 `worldview_id=default_wv`。
- 前置 5：本地后端已重启到最新代码并监听 `127.0.0.1:5006`。
- 前置 6：历史世界观 run `run_35a552901bab` 存在，且其节点链路包含 `initial_expansion`、`world_rule_review`、`worldview_consistency_review`。
- 前置 7：仓库 `.venv` 可用，且本地 Ollama / LLM provider 可正常返回。
- 前置 8：后端日志可观察到 `worldview_agent_initial_expansion` 与 `worldview_agent_modify_content` 的真实实例化记录。

## 用例总表

| 用例编号 | 用例名称 | 用例类型 | 测试目标 | 前置准备 | 执行命令或测试入口 | 验证点 | 断言点 | 观察日志 | 预期副作用 | 降级预期 | 回滚预期 | 失败归因方法 | 执行结果 | 最近一次执行时间 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FE-WV-001 | 章节工作流世界观下拉显示名称 | 正常 | 证明世界观选择列表使用名称展示 | 前置 1-3 | 浏览器打开 `/workflow/chapter?action=create&world_id=world_default`，展开“世界观”下拉 | 默认值与下拉选项文本 | 默认值为 `科幻`；选项包含 `科幻`、`默认世界观 (Default Worldview)`，不出现 `kehuan`、`default_wv` | 浏览器 DOM snapshot；浏览器 console 仅存在 React Router v7 警告，无新增错误 | 无写库 | 若缺少章节上下文，仍保留已加载名称显示 | 当前 diff 可被 `git apply --check -R -` 反向应用 | 若接口返回有名称但 DOM 仍显示 ID，判定为产品缺陷；若接口已无名称字段，判定为数据/接口问题；若浏览器未刷新到新代码，判定为验证环境问题 | 通过 | 2026-06-17 20:47:44 CST |
| FE-WV-002 | 缺少 outline_id 时页面降级不失控 | 异常 | 证明章节工作流缺上下文时正确拦截，同时不破坏世界观名称展示 | 前置 1-3 | 浏览器打开 `/workflow/chapter?action=create&world_id=world_default` | 错误提示与表单可见性 | 页面出现 `章节工作流必须提供 outline_id`；启动按钮禁用；世界观输入框仍显示 `科幻` | 浏览器 DOM snapshot；浏览器 console 无新增业务错误 | 无写库 | 明确提示缺少上下文，不空白、不崩溃、不把世界观退回为 ID | 当前 diff 可被 `git apply --check -R -` 反向应用 | 若页面白屏/遮罩/按钮可误提交，判定为产品缺陷；若接口不可达，判定为环境缺失 | 通过 | 2026-06-17 20:47:44 CST |
| FE-WV-003 | 大纲列表世界观列显示名称 | 特殊 | 证明旧页面中直接渲染 `worldview_id` 的列表列已改为名称 | 前置 1-4 | 浏览器打开 `/novels/novel_for_outline_detail_8b8c07a3/outlines` | 表格“世界观”列文本 | 行 `outline_detail_8b8c07a3` 的世界观列显示 `默认世界观 (Default Worldview)`，不显示 `default_wv` | 浏览器 DOM snapshot | 无写库 | 若世界观映射缺失，应至少回退为可读名称解析而不是空白 | 当前 diff 可被 `git apply --check -R -` 反向应用 | 若接口返回名称存在但表格仍显示 ID，判定为产品缺陷；若映射查询失败，判定为接口/环境问题 | 通过 | 2026-06-17 20:48:08 CST |
| FE-WV-004 | 前端构建与回滚可执行性检查 | 特殊 | 证明改动未破坏编译，且回滚路径可执行 | 前置 1 | `cd frontend && npm run build`；`git diff -- <files> | git apply --check -R -` | 构建产物与 reverse-apply 结果 | `npm run build` 退出码 0；reverse-apply 退出码 0 | 构建输出 `✓ built`；reverse-apply 仅报告历史空白字符 warning，无失败 | 生成 `frontend/dist/` 构建产物 | 构建失败时必须保留失败结论，不得宣称完成 | reverse-apply dry-run 成功，说明本次前端改动具备回滚可行性 | 若 build 失败判定为产品缺陷；若 reverse-apply 非 0 判定为回滚方案不足 | 通过 | 2026-06-17 20:48:08 CST |
| UI-HA-001 | 页面“提交修改”不再报 400 | 正常 | 证明用户实际操作的 world workflow 页面已能走通提交修改 | 前置 1,2,5 | 浏览器打开 `/workflow/world?action=create`，启动 run 后在页面点击“提交修改” | DOM 状态、按钮、迭代次数 | 页面不出现 `Unsupported decision in minimal backend`；run 维持 `waiting_human`；新增“步骤 4：修改内容”；`迭代轮次=2` | 浏览器 DOM snapshot；Flask 5006 日志 `POST /api/hierarchy-agent/respond` 200 | 不写库 | 若后端慢，页面应等待而不是伪成功或直接 400 | 当前前后端 diff 可反向应用 | 若页面仍弹原 400，判定为产品缺陷；若后端未重启，判定为环境问题 | 通过 | 2026-06-17 21:04:30 CST |
| API-HA-001 | hierarchy-agent 支持 request_changes | 正常 | 证明人工“提交修改”不再 400，能进入真实修改链路 | 前置 2,5 | `POST /api/hierarchy-agent/start` 创建 world run，再 `POST /api/hierarchy-agent/respond` with `decision=request_changes` | HTTP 状态码、run 状态、modify_content 节点输入 | `request_changes` 返回 200；run `status=waiting_human`；`iterations=2`；`modify_content.input.manual_edit=true`；`revision_mode=partial_rewrite` | Flask 日志显示两次 200；LLM factory 实例化 world_agent | 不写库 | 若 LLM 不可用，应显式失败而不是返回伪成功 | 当前 `src/app_api.py` diff 可反向应用 | 若仍返回 `Unsupported decision in minimal backend`，判定为产品缺陷；若端口未重启到新代码，判定为环境问题 | 通过 | 2026-06-17 21:01:37 CST |
| API-HA-002 | hierarchy-agent 支持 reject | 异常 | 证明人工终止不会 400，且 run 正确进入拒绝态 | 前置 2,5 | 在 API-HA-001 的 run 上再次 `POST /api/hierarchy-agent/respond` with `decision=reject` | HTTP 状态码、run 终态 | `reject` 返回 200；run `status=rejected`；`current_node=end`；`committed=false` | Flask 日志显示 respond 200 | 无写库 | 不允许把 reject 当 approve 或静默忽略 | 当前 `src/app_api.py` diff 可反向应用 | 若返回 400 或状态未变化，判定为产品缺陷 | 通过 | 2026-06-17 21:01:37 CST |
| API-LLM-001 | 旧 run 读接口补显 LLM 提示词 | 正常 | 证明历史 run 即使未持久化 prompt，真实 HTTP 读接口仍能返回 LLM 节点提示词 | 前置 2,5,6 | `GET /api/hierarchy-agent/get?run_id=run_35a552901bab` | run 状态、节点 `llm_invoked`、`llm_call.prompt`、`prompt_chars` | `initial_expansion` / `world_rule_review` / `worldview_consistency_review` 均为 `llm_invoked=true`，`has_prompt=true`，且 `prompt_chars > 0` | `curl` + `jq` 输出；Flask 5006 access log | 无写库 | 若旧 run 无原始 prompt，接口应按节点类型重建，而不是返回空字符串或伪成功 | 当前 `src/app_api.py` / `src/agents/review_agent.py` diff 可反向应用 | 若节点缺 prompt 且接口无错误，判定为产品缺陷；若 run_id 不存在，判定为测试数据缺失；若端口未更新，判定为环境问题 | 通过 | 2026-06-17 21:14:10 CST |
| UI-LLM-001 | 世界观工作流 LLM 节点详情显示真实语句 | 特殊 | 证明你给定的旧 run 页面中，LLM 节点详情可展示真实提示词文本 | 前置 1,2,5,6 | 浏览器打开 `/workflow/worldview?action=create&world_id=world_agent_novel_8ed501d17a&run_id=run_35a552901bab`，切到“节点”页签 | LLM 节点详情、Prompt 字符数、提示词正文 | 页面出现 `LLM 调用已完成`、`Prompt 字符数：1106`、`LLM 语句`，且正文包含 `[System]` / `[User]` 和 `Worldview Consistency Review` | 浏览器 DOM snapshot | 无写库 | 若 run 为旧数据，页面仍应通过读接口补显真实 prompt，而不是空白或仅显示输入输出 JSON | 当前前后端 diff 可反向应用 | 若接口已有 prompt 但页面不显示，判定为前端产品缺陷；若页面显示的不是该节点 prompt，判定为读路径/重建逻辑缺陷 | 通过 | 2026-06-17 21:15:59 CST |
| UI-LLM-002 | 非 LLM 节点不误显示提示词块 | 异常 | 证明普通节点不会被错误渲染为 LLM 节点 | 前置 1,2,5,6 | 在同一页面点击 `input` 节点并观察“节点”页签 | 节点详情中的 LLM 区块可见性 | 选中 `input` 节点后不存在 `LLM 调用已完成` 与 `LLM 语句` | 浏览器 DOM snapshot | 无写库 | 不允许为了展示 LLM 提示词而把所有节点一律渲染成 LLM 节点 | 当前前后端 diff 可反向应用 | 若非 LLM 节点仍出现 LLM 区块，判定为产品缺陷 | 通过 | 2026-06-17 21:15:59 CST |
| API-LLM-002 | Python 语法检查覆盖新增提示词链路 | 特殊 | 证明新增的读路径补显与 review detail 代码至少可导入编译 | 前置 1 | `python3 -m py_compile src/app_api.py src/agents/world_agent.py src/agents/worldview_agent.py src/agents/novel_agent.py src/agents/outline_agent.py src/agents/chapter_agent.py src/agents/review_agent.py src/agents/review_nodes/world_review.py src/agents/review_nodes/worldview_review.py src/agents/review_nodes/novel_review.py src/agents/review_nodes/outline_review.py src/agents/review_nodes/chapter_review.py` | 命令退出码 | 退出码为 0 | shell 命令输出为空 | 生成 `.pyc` 缓存 | 若语法失败，不允许继续宣称页面完成 | 当前后端 diff 可反向应用 | 若编译失败，判定为产品缺陷 | 通过 | 2026-06-17 21:15:59 CST |
| CLI-LLM-001 | 单命令固定双文件调试入口可直接复跑 | 特殊 | 证明用户只需改固定 prompt 文件并重复执行同一条无参命令即可看到真实模型结果 | 前置 1,2,5,7 | `./scripts/debug_worldview_llm` | 固定 prompt 文件、固定 response 文件、标准输出 | 若 prompt 文件不存在则自动生成 `scratch/worldview_llm_debug/worldview_consistency.prompt.txt`；再次执行复用该文件；真实返回写入 `scratch/worldview_llm_debug/worldview_consistency.response.txt`；输出仅指向这两个固定文件；不处理其他过程 | shell 输出；固定双文件落盘；LLM factory 实例化 `debug_worldview_consistency` | 写入 scratch 调试文件，不写业务库 | 若 prompt 文件格式损坏，应显式报错；除此之外不应自动切换到其他过程 | 当前脚本 diff 可反向应用 | 若必须手工切解释器、手工写 import、复制其他过程或传 CLI 参数才能跑通，判定为产品缺陷；若 LLM provider 不可用，判定为环境缺失 | 通过 | 2026-06-17 22:05:32 CST |
| API-LLM-003 | worldview 新增与改写节点使用不同 LLM 标识 | 正常 | 证明 `initial_expansion` 与 `modify_content` 不再共用同一个 worldview LLM identity | 前置 2,5,7,8 | `GET /api/hierarchy-agent/get?run_id=run_35a552901bab`；`POST /api/hierarchy-agent/respond` with `decision=request_changes` | 旧 run 详情节点输出；真实 request_changes 后端日志 | `initial_expansion.output.llm_call.llm_agent_name=worldview_agent_initial_expansion`；`modify_content.output.llm_call.llm_agent_name=worldview_agent_modify_content`；两者不同 | `curl` + `jq` 输出；Flask 5006 日志实例化两个不同 agent 名称 | 无写库 | 若 modify_content 失败，接口应显式失败，不允许伪造第二个 identity | 当前 `src/agents/worldview_agent.py` / `src/app_api.py` / `config/llm.yml` diff 可反向应用 | 若节点仍共用 `worldview_agent`，判定为产品缺陷；若后端未重启，判定为环境问题；若 provider 中断导致中途断链，保留失败结论并按环境缺失记录 | 通过 | 2026-06-17 22:38:22 CST |
| API-LLM-004 | worldview 列表接口补显拆分后的 LLM 标识 | 特殊 | 证明旧 worldview run 在列表接口里也能看到拆分后的节点级 identity，而不是只在详情接口修复 | 前置 2,5,6 | `GET /api/hierarchy-agent/list?agent_type=worldview&page=1&page_size=10` | 列表中历史 run 的节点输出 | `run_35a552901bab`、`run_6c35dfc895e0` 的 `initial_expansion.output.llm_call.llm_agent_name=worldview_agent_initial_expansion`，`modify_content.output.llm_call.llm_agent_name=worldview_agent_modify_content` | `curl` + `jq` 输出；Flask 5006 access log | 无写库 | 若列表接口未补显，前端列表与详情会不一致 | 当前 `src/app_api.py` diff 可反向应用 | 若详情正确但列表仍旧值，判定为产品缺陷；若 run 数据不存在，判定为测试数据缺失 | 通过 | 2026-06-17 22:36:41 CST |
| API-LLM-005 | worldview 修改节点中文风格 Prompt 与兼容解析 | 特殊 | 证明新的修改 Prompt 采用中文风格约束，但内部输出合同仍保持 `payload`，并兼容 `有效载荷` 解析 | 前置 1,7 | `.venv/bin/python - <<'PY' ... test_worldview_modify_content_accepts_chinese_output_contract() ... PY` | Prompt 文本、返回解析结果 | Prompt 包含 `操作流程`、`世界禁止规则`、`修改意见`、`"payload"`；解析结果中的 `payload`、`expanded_input`、`modification_notes` 均可用 | `.venv/bin/python` 输出；Fake LLM 记录的 prompt 文本 | 无写库 | 若 LLM 返回中文风格结果，系统应兼容；不允许因为键名变化导致 modify_content 崩溃 | 当前 `src/agents/worldview_agent.py` / `tests/test_hierarchy_draft_llm_invocation.py` diff 可反向应用 | 若定向执行失败且定位到解析逻辑，判定为产品缺陷；若解释器依赖缺失，判定为环境缺失并保留失败结论 | 通过 | 2026-06-17 23:14:04 CST |
| API-LLM-006 | worldview 创建节点中文风格 Prompt 与兼容解析 | 特殊 | 证明新的创建 Prompt 采用中文风格约束，但内部输出合同仍保持 `payload`，并兼容 `有效载荷` 解析 | 前置 1,2,5,7 | `.venv/bin/python - <<'PY' ... test_worldview_initial_expansion_accepts_chinese_output_contract() ... PY`；`.venv/bin/python - <<'PY' ... test_real_agent_worldview_flow_requests() ... PY` | Prompt 文本、返回解析结果、真实 workflow 写库读回 | Prompt 包含 `操作流程`、`世界禁止规则`、`世界观草稿`、`"payload"`；解析结果中的 `payload`、`expanded_input`、`expansion_notes` 均可用；真实 `POST /api/hierarchy-agent/start` 与 `POST /api/hierarchy-agent/respond` 返回 200，并成功读回 worldviews 数据 | `.venv/bin/python` 输出；Fake LLM 记录的 prompt 文本；Flask 5006 access log | 写入测试 world 与 worldview，随后清理 | 若 LLM 返回中文风格结果，系统应兼容；不允许因为键名变化导致 initial_expansion 崩溃 | 当前 `src/agents/worldview_agent.py` / `tests/test_hierarchy_draft_llm_invocation.py` diff 可反向应用 | 若定向执行失败且定位到解析逻辑，判定为产品缺陷；若真实 HTTP 写库读回失败，判定为产品缺陷或环境缺失并保留失败结论 | 通过 | 2026-06-17 23:16:43 CST |
| API-LLM-007 | 多 Agent Prompt 模板结构统一检查 | 特殊 | 证明 world / worldview / novel / outline / chapter / review 的 Prompt 已统一成结构化模板 | 前置 1,7 | `.venv/bin/python - <<'PY' ... test_all_prompt_templates_use_structured_sections() ... PY` | Prompt 文本结构 | 所有 Prompt 都包含 `【角色设定】`、`【操作流程 (Mandatory Workflow)】`、`【输入信息】`/`【输出要求】`；review prompt 包含 `passed/errors` JSON 合同 | `.venv/bin/python` 输出 `OK: prompt_template_sections` | 无写库 | 若只有个别 Agent 完成模板化，判定为产品缺陷 | 当前 `src/agents/*.py` / `src/agents/review_agent.py` / `tests/test_hierarchy_draft_llm_invocation.py` diff 可反向应用 | 若结构检查失败，判定为产品缺陷；若解释器依赖缺失，判定为环境缺失并保留失败结论 | 通过 | 2026-06-17 23:22:49 CST |
| API-LLM-008 | novel Prompt 重写后 live create workflow 仍可闭环 | 正常 | 证明 `novel_agent` Prompt 重写后，真实创建、审批、读回与清理链路仍然可用 | 前置 2,5,7 | `.venv/bin/python - <<'PY' ... test_real_agent_novel_flow_requests() ... PY` | HTTP 状态码、写库读回、清理 | 真实 `POST /api/hierarchy-agent/start` 与 `POST /api/hierarchy-agent/respond` 返回 200；`GET /api/novels/list?novel_id=...` 可读回；最终清理 world 成功 | `.venv/bin/python` 输出 `OK: real_novel_flow_requests`；Flask 5006 access log | 写入测试 world 与 novel，随后清理 | 若 Prompt 重写后 novel create 流程断裂，判定为产品缺陷 | 当前 `src/agents/novel_agent.py` diff 可反向应用 | 若 live create/approve/readback 任一失败，判定为产品缺陷或环境缺失并保留失败结论 | 通过 | 2026-06-17 23:24:27 CST |
| API-LLM-009 | world / outline / chapter Prompt 重写后 live workflow 冒烟 | 特殊 | 证明 `world_agent`、`outline_agent`、`chapter_agent` 至少已经进入真实 LLM 链路，Prompt 重写未在入口阶段打断 workflow | 前置 2,5,7 | `.venv/bin/python - <<'PY' ... test_real_agent_world_flow_requests() ... PY`；`.venv/bin/python - <<'PY' ... test_real_agent_outline_flow_requests() ... PY`；`.venv/bin/python - <<'PY' ... test_real_agent_chapter_flow_requests() ... PY` | start/create 阶段、LLM 实例化、后续链路推进 | 后端日志出现 `Instantiating LLM for agent 'world_agent'`、`'outline_agent'`、`'chapter_agent'`；对应 start/create HTTP 请求返回 200 | Flask 5006 access log | 写入临时测试数据，部分链路待结束后清理 | 若 Prompt 重写直接导致 workflow 在 start/create 前崩溃，判定为产品缺陷 | 当前 `src/agents/world_agent.py` / `outline_agent.py` / `chapter_agent.py` diff 可反向应用 | 若仅证明进入 LLM 但未拿到最终读回结果，判定为 `partial`，不能当作完整闭环证据 | 部分通过 | 2026-06-17 23:25:30 CST |
| API-LLM-010 | outline 长大纲保留与防摘要化回归 | 特殊 | 证明 `outline_agent` Prompt 已明确把 `summary` 视为完整大纲正文，并要求长大纲保留后再扩充 | 前置 1,7 | `.venv/bin/python - <<'PY' ... test_outline_prompts_require_preserving_long_form_outline_content() ... PY`；`.venv/bin/python - <<'PY' ... test_real_agent_outline_flow_requests() ... PY` | Prompt 文本、真实 outline workflow 冒烟 | Prompt 中包含“长篇大纲必须完整保留”“不得压缩、提炼、改写成短摘要”“payload.summary 承载完整大纲正文”，并明确完整大纲至少包含卷名、本卷简介、开篇、主要人物、主要矛盾、主要事件、转折、高潮、结局；真实 outline workflow create/approve/readback 返回 200，证明链路未被改坏 | `.venv/bin/python` 输出；Flask 5006 access log | 写入临时测试 world/worldview/novel/outline，随后清理 | 若 live LLM 仍把长大纲压缩，当前用例只能证明提示词约束和链路未坏，不能伪称完全解决模型行为；必须如实记录为 `partial` | 当前 `src/agents/outline_agent.py` / `tests/test_hierarchy_draft_llm_invocation.py` diff 可反向应用 | 若 Prompt 文本缺失约束，判定为产品缺陷；若真实 workflow 冒烟失败，判定为产品缺陷或环境缺失 | 部分通过 | 2026-06-18 15:08:46 CST |
| FE-WORLD-001 | 世界详情页提供直接保存与删除入口 | 正常 | 证明 `/worlds/:worldId` 详情页不再缺少“保存修改”和“删除世界”两个主操作入口 | 前置 1,2,5,7 | `cd frontend && npm run build`；`.venv/bin/python - <<'PY' ... world_detail_save_delete_http ... PY` | 构建结果、真实保存读回、真实删除读后不可见 | 构建通过；真实 `POST /api/worlds/update` 后 `GET /api/worlds/list` 可读回修改字段；真实 `DELETE /api/worlds/delete` 后列表中该 world 消失；页面代码包含“保存修改”“删除世界”按钮并绑定真实 API | `npm run build` 输出；`.venv/bin/python` 输出 `OK: world_detail_save_delete_http`；代码读检 [WorldDetail.tsx](/Users/harry/Documents/git/novel_agent/frontend/src/pages/WorldDetail.tsx) | 写入临时测试 world，随后清理 | 若页面仍无入口或保存/删除未接真实 API，判定为产品缺陷 | 当前 `frontend/src/pages/WorldDetail.tsx` diff 可反向应用 | 若仅拿到 HTTP 读写证据、未拿到浏览器层按钮渲染证据，判定为 `partial` 页面验证 | 部分通过 | 2026-06-17 23:26:17 CST |
| FE-NOVEL-001 | 小说详情页提供直接保存与删除入口 | 正常 | 证明 `/novels/:novelId` 详情页不再缺少“保存修改”和“删除小说”两个主操作入口 | 前置 1,2,5,7 | `cd frontend && npm run build`；`.venv/bin/python - <<'PY' ... novel_detail_save_delete_http ... PY` | 构建结果、真实保存读回、真实删除读后不可见 | 构建通过；真实 `POST /api/novels/update` 后 `GET /api/novels/list` 可读回修改字段；真实 `DELETE /api/novels/delete` 后列表中该 novel 消失；页面代码包含“保存修改”“删除小说”按钮并绑定真实 API | `npm run build` 输出；`.venv/bin/python` 输出 `OK: novel_detail_save_delete_http`；代码读检 [NovelDetail.tsx](/Users/harry/Documents/git/novel_agent/frontend/src/pages/NovelDetail.tsx) | 写入临时测试 world 与 novel，随后清理 | 若页面仍无入口或保存/删除未接真实 API，判定为产品缺陷 | 当前 `frontend/src/pages/NovelDetail.tsx` diff 可反向应用 | 若仅拿到 HTTP 读写证据、未拿到浏览器层按钮渲染证据，判定为 `partial` 页面验证 | 部分通过 | 2026-06-17 23:37:10 CST |

## 正常情况测试

- FE-WV-001
  - 准备：保证 `world_default` 下有世界观接口数据。
  - 执行：浏览器打开 `/workflow/chapter?action=create&world_id=world_default`，点击“世界观”下拉。
  - 断言：选中值为 `科幻`，下拉选项为世界观名称集合，不出现 `kehuan`、`default_wv` 这类 ID。
  - 日志：仅观察到 React Router future flag warning，无业务错误日志。
  - 结果：通过。
- API-LLM-001
  - 准备：使用历史 run `run_35a552901bab`。
  - 执行：`curl -s 'http://127.0.0.1:5006/api/hierarchy-agent/get?run_id=run_35a552901bab' | jq ...`。
  - 断言：`initial_expansion`、`world_rule_review`、`worldview_consistency_review` 的 `llm_call.prompt` 均存在，且 `prompt_chars` 分别大于 0。
  - 日志：`jq` 输出中 `has_prompt=true`。
  - 结果：通过。
- API-LLM-003
  - 准备：后端重启到最新代码，并保留历史 run `run_35a552901bab`。
  - 执行：`curl -s 'http://127.0.0.1:5006/api/hierarchy-agent/get?run_id=run_35a552901bab' | jq '{nodes: [.run.nodes[] | select(.node_id=="initial_expansion" or .node_id=="modify_content") | {node_id, agent_name: .output.agent_name, llm_agent_name: .output.llm_call.llm_agent_name}]}'`；另观察一次真实 `request_changes` 请求后的后端日志。
  - 断言：`initial_expansion` 为 `worldview_agent_initial_expansion`；`modify_content` 为 `worldview_agent_modify_content`；两者不同。
  - 日志：Flask/LLM factory 日志出现 `Instantiating LLM for agent 'worldview_agent_modify_content'`。
  - 结果：通过。

## 异常情况测试

- FE-WV-002
  - 准备：刻意不传 `outline_id`。
  - 执行：浏览器打开 `/workflow/chapter?action=create&world_id=world_default`。
  - 断言：页面显示 `章节工作流必须提供 outline_id`，启动按钮禁用，世界观名称仍保持可读文本。
  - 日志：无新增业务错误日志。
  - 结果：通过。
- UI-LLM-002
  - 准备：打开 `/workflow/worldview?action=create&world_id=world_agent_novel_8ed501d17a&run_id=run_35a552901bab` 并切到“节点”页签。
  - 执行：点击图中的 `input` 节点。
  - 断言：节点详情中不出现 `LLM 调用已完成` 或 `LLM 语句`。
  - 日志：浏览器 DOM snapshot 返回 `hasPromptBlock=false`。
  - 结果：通过。

## 特殊情况测试

- FE-WV-003
  - 准备：使用已知世界观 ID 为 `default_wv` 的历史大纲记录。
  - 执行：浏览器打开 `/novels/novel_for_outline_detail_8b8c07a3/outlines`。
  - 断言：表格“世界观”列显示 `默认世界观 (Default Worldview)`，而不是底层 `default_wv`。
  - 日志：DOM snapshot 能定位到该文本。
  - 结果：通过。
- UI-LLM-001
  - 准备：打开 `/workflow/worldview?action=create&world_id=world_agent_novel_8ed501d17a&run_id=run_35a552901bab`。
  - 执行：切到“节点”页签，选中 `worldview_consistency_review` 节点。
  - 断言：页面出现 `LLM 调用已完成`、`Prompt 字符数：1106`、`LLM 语句`，并能看到 `[System]` / `[User]` 拼接后的真实提示词。
  - 日志：浏览器 DOM snapshot 直接返回提示词正文片段。
  - 结果：通过。
- CLI-LLM-001
  - 准备：确保 `.venv` 和本地 LLM provider 可用。
  - 执行：`./scripts/debug_worldview_llm`。
  - 断言：若不存在则自动生成 `scratch/worldview_llm_debug/worldview_consistency.prompt.txt`；再次执行直接复用该 prompt 文件；返回写入 `scratch/worldview_llm_debug/worldview_consistency.response.txt`。
  - 日志：标准输出打印 `prompt_file`、`response_file`、`=== RESPONSE ===`；LLM factory 日志显示实例化 `debug_worldview_consistency`。
  - 结果：通过。
- FE-WV-004
  - 准备：保留当前工作区改动。
  - 执行：`cd frontend && npm run build`；随后执行 reverse-apply dry-run。
  - 断言：构建成功；反向补丁检查返回 0。
  - 日志：构建输出 `✓ built in 11.16s`；reverse-apply 仅报告空白字符 warning。
  - 结果：通过。
- API-LLM-002
  - 准备：保留当前后端代码改动。
  - 执行：`python3 -m py_compile ...`
  - 断言：退出码为 0。
  - 日志：shell 输出为空。
  - 结果：通过。
- API-LLM-004
  - 准备：使用历史 worldview runs。
  - 执行：`curl -s 'http://127.0.0.1:5006/api/hierarchy-agent/list?agent_type=worldview&page=1&page_size=10' | jq '[.runs[] | {run_id, initial: ([.nodes[] | select(.node_id=="initial_expansion")][0].output.llm_call.llm_agent_name), modify: ([.nodes[] | select(.node_id=="modify_content")][0].output.llm_call.llm_agent_name)}]'`。
  - 断言：列表中的旧 run 同时返回拆分后的 `initial` / `modify` LLM identity。
  - 日志：`jq` 输出包含 `run_35a552901bab` 与 `run_6c35dfc895e0` 的新 identity。
  - 结果：通过。
- API-LLM-010
  - 准备：使用仓库 `.venv` 解释器；后端监听 `127.0.0.1:5006`。
  - 执行：`.venv/bin/python - <<'PY'` 直接导入 `test_outline_prompts_require_preserving_long_form_outline_content()` 并执行；随后执行 `test_real_agent_outline_flow_requests()`。
  - 断言：Prompt 中明确出现“长篇大纲必须完整保留”“不得压缩、提炼、改写成短摘要”“payload.summary 承载完整大纲正文”，以及“卷名、本卷简介、开篇、主要人物、主要矛盾、主要事件、转折、高潮、结局”；真实 outline create workflow 可创建、审批、读回并清理。
  - 日志：`.venv/bin/python` 输出 `OK: outline_prompt_preserve_long_form` 与 `OK: real_outline_flow_requests`；Flask 5006 access log 显示 `POST /api/hierarchy-agent/start` 200、`POST /api/hierarchy-agent/respond` 200、`GET /api/outlines/list?...` 200、`DELETE /api/worlds/delete` 200。
  - 结果：部分通过。
- API-LLM-005
  - 准备：使用仓库 `.venv` 解释器，避免系统 Python 缺依赖。
  - 执行：`.venv/bin/python - <<'PY'` 直接导入 `test_worldview_modify_content_accepts_chinese_output_contract()` 并执行。
  - 断言：worldview 修改 Prompt 中包含 `操作流程`、`世界禁止规则`、`修改意见`、`"payload"`；中文风格输出仍能被成功解析为内部 `payload`。
  - 日志：`.venv/bin/python` 输出 `OK: chinese_output_contract_modify`。
  - 结果：通过。
- API-LLM-006
  - 准备：使用仓库 `.venv` 解释器，避免系统 Python 缺依赖；本地后端监听 `127.0.0.1:5006`。
  - 执行：`.venv/bin/python - <<'PY'` 直接导入 `test_worldview_initial_expansion_accepts_chinese_output_contract()` 并执行；另调用 `build_initial_expansion_prompt()` 检查关键提示词标记；最后执行 `test_real_agent_worldview_flow_requests()` 走真实 HTTP 创建、审批、读回与清理。
  - 断言：worldview 创建 Prompt 中包含 `操作流程`、`世界禁止规则`、`世界观草稿`、`"payload"`；中文风格输出仍能被成功解析为内部 `payload`；真实 workflow 成功创建并读回 worldviews 记录。
  - 日志：`.venv/bin/python` 输出 `OK: chinese_output_contract_initial` 与 `OK: real_worldview_flow_requests`；Flask 5006 access log 显示 `POST /api/hierarchy-agent/start` 200、`POST /api/hierarchy-agent/respond` 200、`GET /api/worldviews/list?worldview_id=...` 200、`DELETE /api/worlds/delete` 200。
  - 结果：通过。
- UI-HA-001
  - 准备：浏览器打开 `/workflow/world?action=create`，填写 world workflow 表单并等待 run 进入 `waiting_human`。
  - 执行：在页面输入反馈后点击“提交修改”。
  - 断言：页面没有出现 `Unsupported decision in minimal backend`；步骤区新增“修改内容”；迭代轮次从 1 变为 2；页面仍停留在 `waiting_human` 供下一次人工确认。
  - 日志：浏览器 DOM snapshot；Flask 5006 日志显示 `POST /api/hierarchy-agent/respond` 返回 200。
  - 结果：通过。
- API-HA-001
  - 准备：启动 world create run，但不批准写库。
  - 执行：`POST /api/hierarchy-agent/start` 后，对返回 `run_id` 执行 `POST /api/hierarchy-agent/respond`，提交 `decision=request_changes`、`revision_mode=partial_rewrite`、`manual_edit=true`。
  - 断言：HTTP 200；run 进入 `waiting_human`；`iterations=2`；最近的 `modify_content` 节点输入包含 `manual_edit=true` 和 `revision_mode=partial_rewrite`。
  - 日志：Flask 5006 日志记录 start/respond 200；`novel_agent.llm_factory` 记录 world_agent 实例化。
  - 结果：通过。
- API-HA-002
  - 准备：使用 API-HA-001 返回的同一 `run_id`。
  - 执行：`POST /api/hierarchy-agent/respond`，提交 `decision=reject`。
  - 断言：HTTP 200；run 进入 `rejected`；`current_node=end`；`committed=false`。
  - 日志：Flask 5006 日志记录 respond 200。
  - 结果：通过。

## 降级验证

- 入口：`/workflow/chapter?action=create&world_id=world_default`
- 故障/错误输入：缺少 `outline_id`

## 2026-06-18 直改入口增量验证

### 本次测试范围

- `frontend/src/pages/NovelOutlineManagement.tsx`：直接修改分卷大纲入口。
- `frontend/src/pages/NovelChapterManagement.tsx`：直接新增章节内容入口。
- `frontend/src/pages/NovelChapterManagement.tsx`：直接修改章节内容入口。
- `frontend/src/pages/NovelChapterManagement.tsx`：保留原工作流修改入口，并与直改入口并存。

### 本次执行时间

- 2026-06-18 22:57:28 CST

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 前端构建：`frontend`
- 后端接口：`http://127.0.0.1:5006`

### 用例总表增量

| 用例编号 | 用例名称 | 用例类型 | 测试目标 | 前置准备 | 执行命令或测试入口 | 验证点 | 断言点 | 观察日志 | 预期副作用 | 降级预期 | 回滚预期 | 失败归因方法 | 执行结果 | 最近一次执行时间 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FE-DIRECT-001 | 前端构建覆盖直改入口 | 正常 | 证明新增的直改弹窗、按钮和状态 wiring 未破坏前端编译 | 本地 Node 依赖已安装 | `cd frontend && npm run build` | TypeScript 与 Vite 构建结果 | `tsc` 通过，`vite build` 通过 | 输出 `✓ built in 24.10s` | 生成 `frontend/dist/` | 若代码有类型错误，应直接构建失败，不允许伪成功 | 现有 diff 可被反向应用 | 若 build 非 0，判定为产品缺陷；若依赖缺失，判定为环境缺失 | 通过 | 2026-06-18 22:49:xx CST |
| FE-DIRECT-002 | 分卷页直接修改真实页面验证 | 正常 | 证明 `/novels/:novelId/outlines` 的“直接修改”弹窗、保存提示和表格回显真实可用 | 5174 前端在线；5006 后端在线；存在临时 novel/outline 测试数据 | Playwright CLI 打开 `/novels/novel_ui_direct_edit_20260618/outlines`，点击“直接修改”，修改标题与正文并保存 | 弹窗内容、成功提示、表格回显 | 页面出现“直接修改分卷大纲”弹窗；保存后出现“分卷大纲已直接修改，并完成真实接口回查”；表格中卷名改为 `第一卷：页面已直改`，正文改为 `页面直改后的分卷正文` | `.playwright-cli/page-2026-06-18T15-09-53-863Z.yml` | 修改临时 outline 数据 | 若保存失败，页面应显示真实接口错误，不允许静默关闭弹窗 | 当前前端 diff 可反向应用 | 若弹窗打不开、保存无回显或列表未更新，判定为产品缺陷；若 5174/5006 不在线，判定为环境缺失 | 通过 | 2026-06-18 23:09:53 CST |
| FE-DIRECT-003 | 章节页直接新增/修改/删除真实页面验证 | 正常 | 证明 `/novels/:novelId/chapters` 的“直接新增章节内容”“直接修改”“删除”真实可用 | 5174 前端在线；5006 后端在线；存在临时 novel/outline 测试数据 | Playwright CLI 打开 `/novels/novel_ui_direct_edit_20260618/chapters?outline_id=outline_ui_direct_edit_20260618`，执行新增、修改、删除 | 弹窗内容、成功提示、表格回显 | 新增后出现“章节内容已直接创建，并完成真实接口回查”，列表新增 `第一章：页面新增`；修改后出现“章节内容已直接修改，并完成真实接口回查”，列表更新为 `第一章：页面已直改` 与 `页面直改后的章节正文`；删除后出现“分卷章节大纲已删除，并完成真实接口回查”，列表恢复 0 条 | `.playwright-cli/page-2026-06-18T15-10-43-739Z.yml`、`.playwright-cli/page-2026-06-18T15-11-17-438Z.yml`、`.playwright-cli/page-2026-06-18T15-11-43-394Z.yml` | 新增并删除一条临时 `prose` 记录 | 若任一步骤失败，页面应停留在当前状态并显示真实接口错误 | 当前前端 diff 可反向应用 | 若弹窗打不开、保存/删除无回显或表格不更新，判定为产品缺陷；若 5174/5006 不在线，判定为环境缺失 | 通过 | 2026-06-18 23:11:43 CST |
| API-DIRECT-001 | 分卷直改与章节直增直改直删真实 HTTP 闭环 | 正常 | 证明前端新增入口依赖的真实接口链路可用 | 5006 后端在线 | `python3 - <<'PY' ... direct_outline_and_chapter_http_smoke ... PY` | 创建 world/novel/outline，直改 outline，直增/直改/直删 prose，再回查 | 输出 `OK: direct_outline_and_chapter_http_smoke`；回查数据分别命中修改后的标题、正文和删除结果 | shell 输出成功标记 | 写入临时测试 world/novel/outline/prose，结束后级联删除 | 若任一步骤失败，必须保留失败结论 | 临时测试数据使用固定 ID，可整体级联清理 | 若 HTTP 失败且后端在线，判定为产品缺陷；若 5006 不在线，判定为环境缺失 | 通过 | 2026-06-18 22:55:xx CST |
| API-DIRECT-002 | 缺少 novel_id 时创建分卷被明确拦截 | 异常 | 证明父级上下文缺失时后端显式报错，不伪造创建成功 | 5006 后端在线 | `python3 - <<'PY' ... outline_create_missing_novel_id_400 ... PY` | 错误码与错误消息 | 返回 HTTP 400，且 `error=Missing novel_id` | shell 输出 `OK: outline_create_missing_novel_id_400` | 无写库 | 明确失败，不允许 silently upsert | 当前 diff 可反向应用 | 若返回 200 或错误信息错误，判定为产品缺陷；若 5006 不在线，判定为环境缺失 | 通过 | 2026-06-18 22:56:xx CST |
| FE-DIRECT-004 | 直改页面浏览器 console 冒烟 | 特殊 | 证明新增页面流程未引入新的高风险 console 异常 | 已执行 FE-DIRECT-002 和 FE-DIRECT-003 | Playwright CLI `console warning --raw` | 浏览器 console 信息 | 仅看到 React Router 未来版本 warning，以及一个既有 `vite.svg 404`；未看到与分卷直改/章节直改相关的新异常 | Playwright console 输出 | 无写库 | 若新增流程引入运行时错误，必须保留失败结论 | 当前前端 diff 可反向应用 | 若出现新的业务错误栈，判定为产品缺陷；若仅为既有开发资源缺失，判定为低优先级遗留问题 | 通过 | 2026-06-18 23:12:xx CST |

### 正常情况结果

- `FE-DIRECT-001`
  - 测了什么：前端新增“直接修改分卷大纲”“直接新增章节内容”“直接修改章节内容”后的构建稳定性。
  - 怎么测：执行 `cd frontend && npm run build`。
  - 看到什么结果：`tsc` 与 `vite build` 均通过，只有既有 chunk size warning，没有新增报错。
  - 是否通过：通过。
- `FE-DIRECT-002`
  - 测了什么：分卷页“直接修改”是否真的打开弹窗、保存并把结果回写到表格。
  - 怎么测：Playwright CLI 打开 `/novels/novel_ui_direct_edit_20260618/outlines`，点击表格中的“直接修改”，把卷名改成 `第一卷：页面已直改`、正文改成 `页面直改后的分卷正文`，再点击“直接保存”。
  - 看到什么结果：页面出现“分卷大纲已直接修改，并完成真实接口回查：outline_ui_direct_edit_20260618”，表格行同步更新到新卷名和新正文。
  - 是否通过：通过。
- `FE-DIRECT-003`
  - 测了什么：章节页“直接新增章节内容”“直接修改”“删除”三条真实页面链路。
  - 怎么测：Playwright CLI 打开 `/novels/novel_ui_direct_edit_20260618/chapters?outline_id=outline_ui_direct_edit_20260618`，先新增 `第一章：页面新增`，再改成 `第一章：页面已直改`，最后删除该条记录。
  - 看到什么结果：新增后本页从 0 条变成 1 条；修改后表格标题和正文回显为新值；删除后页面出现“分卷章节大纲已删除，并完成真实接口回查。”，本页恢复 0 条。
  - 是否通过：通过。
- `API-DIRECT-001`
  - 测了什么：真实接口下的分卷直改、章节直增、章节直改、章节直删闭环。
  - 怎么测：执行内联 Python 命令，顺序调用 `/api/worlds/create`、`/api/novels/create`、`/api/outlines/create`、`/api/archive/update`、`/api/lore/list`、`/api/archive/delete`、`/api/worlds/delete`。
  - 看到什么结果：命令输出 `OK: direct_outline_and_chapter_http_smoke`，说明修改后的标题和内容都已被回查验证，删除后记录不可再查询到。
  - 是否通过：通过。

### 异常情况结果

- `API-DIRECT-002`
  - 测了什么：缺少父级小说 ID 时，分卷创建是否明确失败。
  - 怎么测：执行内联 Python 命令，请求 `/api/outlines/create`，只传 `name` 不传 `novel_id`。
  - 看到什么结果：接口返回 HTTP 400，错误消息为 `Missing novel_id`。
  - 是否通过：通过。

### 特殊情况结果

- `FE-DIRECT-004`
  - 测了什么：直改页面浏览器 console 是否引入新的高风险异常。
  - 怎么测：在完成页面级操作后执行 Playwright CLI `console warning --raw`。
  - 看到什么结果：console 中有 2 条 React Router v7 future flag warning，以及 1 条 `vite.svg 404`。未看到与本次分卷/章节直改相关的新业务错误。
  - 是否通过：通过。
- 特殊说明：`vite.svg 404` 是现有开发资源缺失噪音，不是本次直改入口新增的功能性故障，但应作为低优先级遗留问题记录。

### 降级结果

- 若后端 5006 不在线，当前前端直改入口仍会通过已有 `getApiErrorMessage` 路径显示真实接口错误，而不是吞错或伪成功。
- 已确认缺少 `novel_id` 时后端会显式返回 `400 Missing novel_id`。
- 本次未单独制造浏览器层断网或 500 场景，所以页面上的错误文案细节仍只部分验证。

### 回滚结果

- 本次为纯前端文件修改，未修改后端接口合同。
- 如需回滚，可直接反向应用当前前端补丁；本次未执行 `git apply --check -R -` 的独立回滚命令。

### 真实命令链验证结果

- 已执行：`cd frontend && npm run build`
- 已执行：`python3 - <<'PY' ... direct_outline_and_chapter_http_smoke ... PY`
- 已执行：`python3 - <<'PY' ... outline_create_missing_novel_id_400 ... PY`
- 结果：三条命令均完成，其中两条给出明确成功标记。

### 失败项

- 失败项 1：浏览器 console 仍存在一个开发资源级 404。
- 失败现象：`http://127.0.0.1:5174/vite.svg` 返回 404。
- 失败位置：前端开发资源加载。
- 影响范围：当前未观察到对本次直改功能造成阻断，但会污染 console 清洁度。
- 复现方式：执行 Playwright CLI `console warning --raw`。
- 当前判断：产品低优先级遗留问题，非本次直改入口逻辑回归。
- 是否保留失败结论：保留。

### 缺陷归因

- 当前未发现本次新增直改入口的接口合同缺陷。
- 当前页面主链路已完成浏览器级 E2E 读回。
- 当前剩余问题是既有开发资源 404 噪音，不是本次直改入口回归。

### 是否满足完成口径

- 结论：本次需求对应的实现和主链路验证已完成。
- 当前可以确认：
  - 前端代码可构建。
  - 直改依赖的真实 HTTP 链路可用。
  - 缺少 `novel_id` 时后端能明确失败。
  - `/novels/:novelId/outlines` 的“直接修改分卷大纲”真实页面可用。
  - `/novels/:novelId/chapters` 的“直接新增章节内容”“直接修改章节内容”“删除”真实页面可用。
- 当前仍需注意：
  - 浏览器 console 有既有 `vite.svg 404` 噪音，未阻断本次功能。

### 最近一次执行时间

- 2026-06-18 22:57:28 CST
- 预期：页面明确提示缺少必需上下文，禁止提交，不白屏，同时已加载的世界观名称继续可见。
- 结果：通过。
- 入口：`POST /api/hierarchy-agent/respond`
- 故障/错误输入：`decision=reject`
- 预期：接口返回 200 和拒绝态 run，而不是再抛 `Unsupported decision in minimal backend`。
- 结果：通过。
- 入口：`GET /api/hierarchy-agent/get?run_id=run_35a552901bab`
- 故障/错误输入：历史 run 输出中缺少持久化 `llm_call.prompt`
- 预期：读接口按节点类型补显 prompt 和 `prompt_chars`，而不是把 LLM 节点 silently 降级成仅输入/输出显示。
- 结果：通过。
- 入口：`GET /api/hierarchy-agent/list?agent_type=worldview&page=1&page_size=10`
- 故障/错误输入：历史 worldview run 输出中沿用旧 `worldview_agent` identity
- 预期：列表接口补显节点级 `worldview_agent_initial_expansion` / `worldview_agent_modify_content`，避免新旧数据混乱。
- 结果：通过。

## 回滚验证

- 执行命令：
```bash
git diff -- frontend/src/utils/worldview.ts \
  frontend/src/pages/HierarchyWorkflow.tsx \
  frontend/src/pages/WorldHierarchy.tsx \
  frontend/src/pages/OutlineChapterWorkflow.tsx \
  frontend/src/pages/LoreDB.tsx \
  frontend/src/pages/WorldviewManagement.tsx \
  frontend/src/pages/NovelOutlineManagement.tsx \
  src/app_api.py \
  src/agents/world_agent.py \
  src/agents/worldview_agent.py \
  src/agents/novel_agent.py \
  src/agents/outline_agent.py \
  src/agents/chapter_agent.py \
  src/agents/review_agent.py \
  src/agents/review_nodes/world_review.py \
  src/agents/review_nodes/worldview_review.py \
  src/agents/review_nodes/novel_review.py \
  src/agents/review_nodes/outline_review.py \
  src/agents/review_nodes/chapter_review.py | git apply --check -R -
```
- 结果：退出码 0，说明当前补丁可反向应用。
- 备注：命令输出包含旧 diff 上下文的空白字符 warning，但不影响 reverse-apply 成功。

## 真实命令链验证

1. `curl -s 'http://127.0.0.1:5006/api/worldviews/list?world_id=world_default&page=1&page_size=5'`
   - 看到真实接口返回 `科幻`、`默认世界观 (Default Worldview)` 等 `name/title`。
2. 浏览器打开 `/workflow/chapter?action=create&world_id=world_default`
   - 看到世界观输入框默认值为 `科幻`。
3. 展开“世界观”下拉
   - 看到列表项为 `科幻`、`Strict Test Worldview 9a4c7887`、`默认世界观 (Default Worldview)`、`魔法世界`。
4. 浏览器打开 `/novels/novel_for_outline_detail_8b8c07a3/outlines`
   - 看到大纲表格世界观列为 `默认世界观 (Default Worldview)`。
5. `cd frontend && npm run build`
   - 看到 `✓ built`。
6. `git diff -- <files> | git apply --check -R -`
   - 回滚 dry-run 成功。
7. `POST http://127.0.0.1:5006/api/hierarchy-agent/start`
   - 返回 200 和 `waiting_human` 的 world run。
8. `POST http://127.0.0.1:5006/api/hierarchy-agent/respond` with `decision=request_changes`
   - 返回 200；run `status=waiting_human`；`iterations=2`；最近 `modify_content.input.manual_edit=true`。
9. `POST http://127.0.0.1:5006/api/hierarchy-agent/respond` with `decision=reject`
   - 返回 200；run `status=rejected`；`current_node=end`；`committed=false`。
10. 浏览器打开 `http://127.0.0.1:5174/workflow/world?action=create`
   - 真实点击“启动工作流”后等待 run 进入 `waiting_human`。
11. 浏览器在同页真实点击“提交修改”
   - 页面新增“步骤 4：修改内容”，迭代轮次显示为 2，没有再出现原来的 400 错误。
12. `curl -s 'http://127.0.0.1:5006/api/hierarchy-agent/get?run_id=run_35a552901bab' | jq '{status: .run.status, nodes: [.run.nodes[] | select(.node_id=="initial_expansion" or .node_id=="world_rule_review" or .node_id=="worldview_consistency_review") | {node_id, llm_invoked: .output.llm_invoked, prompt_chars: .output.llm_call.prompt_chars, has_prompt: ((.output.llm_call.prompt // "") | length > 0)}]}'`
   - 看到三个 LLM 节点均为 `has_prompt=true`，且 `prompt_chars` 分别为 `1140`、`1144`、`1106`。
13. 浏览器打开 `http://127.0.0.1:5174/workflow/worldview?action=create&world_id=world_agent_novel_8ed501d17a&run_id=run_35a552901bab`
   - 切到“节点”页签后，`worldview_consistency_review` 节点详情显示 `LLM 调用已完成`、`Prompt 字符数：1106`、`LLM 语句` 和真实提示词正文。
14. 在同页点击 `input` 节点
   - 节点详情不出现 `LLM 调用已完成` 或 `LLM 语句`。
15. `python3 -m py_compile src/app_api.py src/agents/world_agent.py src/agents/worldview_agent.py src/agents/novel_agent.py src/agents/outline_agent.py src/agents/chapter_agent.py src/agents/review_agent.py src/agents/review_nodes/world_review.py src/agents/review_nodes/worldview_review.py src/agents/review_nodes/novel_review.py src/agents/review_nodes/outline_review.py src/agents/review_nodes/chapter_review.py`
   - 退出码 0。
16. `./scripts/debug_worldview_llm`
   - 若 prompt 文件不存在则自动生成 `scratch/worldview_llm_debug/worldview_consistency.prompt.txt`，并输出真实审查结果。
17. `curl -s 'http://127.0.0.1:5006/api/hierarchy-agent/get?run_id=run_35a552901bab' | jq '{nodes: [.run.nodes[] | select(.node_id=="initial_expansion" or .node_id=="modify_content") | {node_id, agent_name: .output.agent_name, llm_agent_name: .output.llm_call.llm_agent_name}]}'`
   - 看到 `initial_expansion` 为 `worldview_agent_initial_expansion`，`modify_content` 为 `worldview_agent_modify_content`。
18. `curl -s 'http://127.0.0.1:5006/api/hierarchy-agent/list?agent_type=worldview&page=1&page_size=10' | jq '[.runs[] | {run_id, initial: ([.nodes[] | select(.node_id=="initial_expansion")][0].output.llm_call.llm_agent_name), modify: ([.nodes[] | select(.node_id=="modify_content")][0].output.llm_call.llm_agent_name)}]'`
   - 看到旧 run `run_35a552901bab`、`run_6c35dfc895e0` 在列表接口里也返回拆分后的 `initial` / `modify` identity。
19. 观察后端实时日志
   - 看到 `Instantiating LLM for agent 'worldview_agent_initial_expansion'` 与 `Instantiating LLM for agent 'worldview_agent_modify_content'`，说明新增和改写节点已走不同 LLM 标识。
17. 再次执行 `./scripts/debug_worldview_llm`
   - 直接复用同一 prompt 文件再次执行；输出固定的 `worldview_consistency.response.txt`，无需手写 Python import、切解释器、复制其他过程或传 CLI 参数。
20. `.venv/bin/python - <<'PY'` 执行 `test_real_agent_worldview_flow_requests()`
   - 输出 `OK: real_worldview_flow_requests`；后端日志显示真实 `POST /api/hierarchy-agent/start` 200、`POST /api/hierarchy-agent/respond` 200、`GET /api/worldviews/list?worldview_id=...` 200、`DELETE /api/worlds/delete` 200，说明新的 initial_expansion Prompt 已通过 live workflow 创建、审批、读回和清理闭环验证。

## 缺陷记录区

- 无新增未关闭缺陷。
- 观察项：浏览器 console 持续存在 React Router v7 future flag warning，属于既有警告，不由本次改动引入，当前判断为非阻断 `P3` 观察项。

## 执行结果汇总

- 正常情况：通过。
- 异常情况：通过。
- 特殊情况：通过。
- 降级验证：通过。
- 回滚验证：通过。
- 真实命令链验证：部分通过。
- 是否满足完成口径：本次已完成 Prompt 模板统一改造，并拿到 `worldview` 与 `novel` 的真实 HTTP 闭环证据、`world / outline / chapter` 的真实入口与 LLM 实例化证据、世界/小说详情页直接保存删除的真实 HTTP 读写证据、结构化 Prompt 定向回归证据、语法验证与回滚 dry-run 证据；但 `world / outline / chapter` 本轮尚未全部拿到最终 approve/readback 结果，且详情页仍未拿到浏览器层自动化截图证据，因此当前只能判定为“已完成实现，未完成所有 live workflow 与页面层闭环验证”。

## 最近一次执行时间

2026-06-17 23:37:10 CST

---

## 2026-06-18 世界观唯一设定集与 Agent Workflow 修复补充

### 本次测试范围

- `src/app_api.py`：世界创建自动生成唯一世界观设定集、删除世界时自动删除设定集及其设定条目、禁用独立 `create worldview`。
- `src/agents/world_agent.py`：world create commit 时同步创建唯一世界观设定集。
- `src/agents/worldview_agent.py`：worldview create/update 改为操作当前世界唯一设定集下的世界观设定条目，创建/修改走 Agent workflow。
- `frontend/src/pages/LoreDB.tsx`：`/worldviews` 改为分页显示当前世界唯一设定集下的设定条目，不再管理多个独立设定集。
- `frontend/src/App.tsx`、`frontend/src/layout/MainLayout.tsx`、`frontend/src/pages/WorldviewManagement.tsx`、`frontend/src/api/client.ts`：移除或收口旧的“世界观容器管理”入口。
- `tests/test_worldview.py`、`tests/test_real_worldview_policy_create_requests.py`、`tests/test_real_worldview_policy_update_requests.py`、`tests/test_real_agent_worldview_flow_requests.py`、`tests/test_world_hierarchy_requests.py`、`tests/test_worldview_library_rules_requests.py`、`tests/test_five_module_workflow_requests.py`：真实请求回归与测试入口修正。

### 本次执行时间

- 开始时间：2026-06-17 23:40 CST
- 最近一次执行时间：2026-06-18 00:11:49 CST

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- 后端验证地址：`http://127.0.0.1:5016`
- 前端页面地址：`http://127.0.0.1:5174`
- Python：仓库 `.venv/bin/python`
- 构建工具：`npm run build`

### 执行命令或测试入口

1. `python3 -m py_compile src/app_api.py src/agents/world_agent.py src/agents/worldview_agent.py tests/test_real_worldview_policy_update_requests.py tests/test_worldview.py tests/test_worldview_library_rules_requests.py tests/test_real_worldview_policy_create_requests.py tests/test_real_agent_worldview_flow_requests.py tests/test_five_module_workflow_requests.py`
2. `cd frontend && npm run build`
3. `API_BASE_URL=http://127.0.0.1:5016 .venv/bin/python tests/test_worldview.py`
4. `API_BASE_URL=http://127.0.0.1:5016 .venv/bin/python - <<'PY' ... test_real_worldview_policy_create_requests(); test_real_worldview_policy_update_requests(); ... PY`
5. `API_BASE_URL=http://127.0.0.1:5016 .venv/bin/python - <<'PY' ... test_real_agent_worldview_flow_requests(); ... PY`
6. `API_BASE_URL=http://127.0.0.1:5016 .venv/bin/python - <<'PY' ... test_world_create_auto_provisions_unique_worldview_library(); test_worldview_create_api_is_disabled_after_world_auto_provision(); test_delete_world_auto_removes_worldview_library_and_its_settings(); ... PY`
7. `API_BASE_URL=http://127.0.0.1:5016 .venv/bin/python tests/test_world_hierarchy_requests.py`
8. `API_BASE_URL=http://127.0.0.1:5016 .venv/bin/python tests/test_five_module_workflow_requests.py`
9. 页面验证入口：`http://127.0.0.1:5174/worldviews`

### 正常情况结果

- `WV-REAL-001` 世界创建自动生成唯一世界观设定集
  - 执行：真实调用 `/api/worlds/create`，随后 `GET /api/worldviews/list?world_id=...`
  - 结果：通过。
  - 断言：每个 world 只返回 1 个 worldview library，且响应中回填 `worldview_id`。
- `WV-REAL-002` 世界观设定条目 CRUD 闭环
  - 执行：`tests/test_worldview.py`
  - 结果：通过。
  - 断言：通过 `/api/archive/update` 新增 `type=worldview` 条目，经 `/api/lore/list` 读回；删除后再次读回不可见。
- `WV-REAL-003` 世界观策略创建/更新真实请求
  - 执行：`test_real_worldview_policy_create_requests()`、`test_real_worldview_policy_update_requests()`
  - 结果：通过。
  - 断言：自动创建设定集存在；更新规则字段后可通过 `/api/worldviews/update` 读回到唯一设定集。
- `WV-REAL-004` 世界观 Agent workflow create 真链路
  - 执行：`test_real_agent_worldview_flow_requests()`
  - 结果：通过。
  - 断言：`/workflow/worldview?action=create` 背后真实 workflow 创建的是 lore worldview 条目，不是新设定集；批准后可通过 `/api/lore/list` 读回。
- `WV-REAL-005` 世界层级真实请求回归
  - 执行：`tests/test_world_hierarchy_requests.py`
  - 结果：通过。
  - 断言：世界、唯一世界观设定集、小说等父子链路仍可建立并读回。

### 异常情况结果

- `WV-ERR-001` 禁止独立创建第二个世界观设定集
  - 执行：`POST /api/worldviews/create`
  - 结果：通过。
  - 断言：返回 `409`，错误信息明确指出 world 创建时已自动生成唯一设定集，并要求创建设定条目改走 `/workflow/worldview?action=create&world_id=...`。
- `WV-ERR-002` 一世界多设定集脏数据保护
  - 执行：代码审查 `frontend/src/pages/LoreDB.tsx`
  - 结果：通过代码级保护，未完成浏览器层验证。
  - 断言：当 `/api/worldviews/list` 返回多个 library 时，页面进入错误提示并拒绝按正常结构继续管理条目。
- `WV-ERR-003` 浏览器自动化环境失败路径
  - 执行：Playwright CLI、Node Playwright 访问 `http://127.0.0.1:5174/worldviews`
  - 结果：失败，保留失败结论。
  - 失败现象：Playwright CLI 无法建立可复用浏览器会话；Node 直拉系统 Chrome 被 `SIGKILL`。
  - 当前判断：环境缺失 / 运行时阻塞，不作为页面功能通过证据。

### 特殊情况结果

- `WV-EDGE-001` 删除世界自动删除唯一设定集与世界观设定条目
  - 执行：`test_delete_world_auto_removes_worldview_library_and_its_settings()`
  - 结果：通过。
  - 断言：删除 world 后，`/api/worldviews/list` 与 `/api/lore/list` 不再返回该 world 的设定集和 worldview 条目。
- `WV-EDGE-002` 五模块真实 workflow 回归
  - 执行：`tests/test_five_module_workflow_requests.py`
  - 结果：未通过。
  - 失败现象：在 world create 成功后，worldview create 阶段被 `world_rule_review` 判定与新生成的世界根设定冲突，run 进入 `review_failed`，`current_node=modify_content`，未能继续完成五模块 create/update 全链路。
  - 当前判断：产品缺陷 / 用例冲突。当前测试用的 worldview 草稿与 live LLM 生成出的 world 基础规则不兼容，导致真实 workflow 被审查拦截。

### 降级结果

- `/worldviews` 页面在检测到多设定集或缺少唯一设定集时，会给出明确错误提示，不再继续展示错误结构。
- 浏览器自动化不可用时，当前仅保留源码、构建、HTTP 请求与读回证据；不把它降级成“页面已验证”。

### 回滚结果

- 本轮未执行完整 `git apply --check -R -` 针对所有本次改动文件的回滚 dry-run。
- 当前只能确认：
  - `frontend` 构建通过；
  - Python 语法检查通过；
  - 如需达到完整交付口径，仍应补跑本次改动文件集合的回滚 dry-run。

### 真实命令链验证结果

- 已完成：
  - 后端真实 HTTP 创建 world -> 自动生成唯一 worldview library -> 读取验证
  - 后端真实 HTTP 禁止第二次创建 worldview library -> `409` 验证
  - 后端真实 HTTP 通过 workflow 创建 worldview lore 条目 -> 读取验证
  - 删除 world -> library 与 lore 设定条目一起消失
  - 前端 `npm run build` 成功
- 未完成：
  - `/worldviews` 路由的真实浏览器 DOM 截图与交互验证
  - 本轮改动文件集合的回滚 dry-run

### 失败项

- `FAIL-WV-001`
  - 失败现象：`tests/test_worldview_import_requests.py` 仍会在 `/api/worldviews/import` 上收到 `404`
  - 失败位置：后端不存在该路由
  - 影响范围：worldview import 功能本身
  - 复现方式：`API_BASE_URL=http://127.0.0.1:5016 .venv/bin/python tests/test_worldview_import_requests.py`
  - 当前判断：产品缺陷 / 既有缺口，非本次修复引入
  - 是否保留失败结论：是
- `FAIL-WV-002`
  - 失败现象：`/worldviews` 页面自动化浏览器验证未拿到 DOM 证据
  - 失败位置：验证环境
  - 影响范围：页面功能测试证据不足
  - 复现方式：Playwright CLI `open/snapshot`，以及 Node Playwright 启动系统 Chrome
  - 当前判断：环境缺失
  - 是否保留失败结论：是

### 缺陷归因

- `P1`：`/api/worldviews/import` 缺失真实路由，导致 import 请求测试固定失败；该问题不由本次修复引入，但仍阻断对应功能交付。
- `P1`：五模块长链路回归在 worldview create 阶段被真实世界规则审查拦截，说明跨模块用例数据与 live world 生成结果存在真实冲突，当前没有完整通过证据。
- `P2`：页面浏览器自动化环境不可用，导致 `/worldviews` 页面功能测试尚未闭环。

### 是否满足完成口径

- 结论：尚未达到交付标准。
- 原因：
  - 本次实现和核心真实 HTTP 读回证据已完成。
  - 但 `/worldviews` 页面功能测试尚未拿到真实浏览器证据。
  - 本轮改动的回滚 dry-run 尚未补齐。
  - 仍存在未关闭的 `P1` 既有 import 缺口，以及 `P2` 级的五模块全链路回归 / 页面自动化验证缺口。

---

## 2026-06-18 世界创建后缺少唯一世界观设定库修复补充

### 本次测试范围

- `src/app_api.py`
  - worldviews list 缺库自愈
  - world 已删除时 worldviews list 空结果语义
- `tests/test_worldview_library_rules_requests.py`
  - 新增缺失 worldview library 自愈回归
- 运行中 `127.0.0.1:5006` 后端进程
  - 重启到当前工作区代码

### 本次执行时间

- 2026-06-18 00:42:00 CST - 2026-06-18 00:45:15 CST

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- 后端地址：`http://127.0.0.1:5006`
- Python：`.venv/bin/python`

### 执行命令或测试入口

1. `env PYTHONPATH=. .venv/bin/python src/app_api.py`
2. `curl -s "http://127.0.0.1:5006/api/worldviews/list?world_id=world_f3312d20&page=1&page_size=20"`
3. `API_BASE_URL=http://127.0.0.1:5006 .venv/bin/python - <<'PY' ... test_worldview_library_rules_requests ... PY`
4. `python3 -m py_compile src/app_api.py tests/test_worldview_library_rules_requests.py`

### 正常情况结果

- `WV-HOTFIX-001`
  - 测试内容：新 world 创建后立刻可读到唯一 worldview library。
  - 执行方式：真实 `POST /api/worlds/create` + 真实 `GET /api/worldviews/list`
  - 结果：通过。
  - 断言结果：创建响应包含 `worldview_id`；列表返回唯一设定集。

### 异常情况结果

- `WV-HOTFIX-002`
  - 测试内容：旧 world 已存在但缺少 worldview library。
  - 执行方式：真实 `GET /api/worldviews/list?world_id=world_f3312d20&page=1&page_size=20`
  - 结果：通过。
  - 断言结果：返回 `200`，并自动补回 `wv_world_f3312d20`。

### 特殊情况结果

- `WV-HOTFIX-003`
  - 测试内容：删除 world 后再次读取 worldviews list。
  - 执行方式：`test_delete_world_auto_removes_worldview_library_and_its_settings()`
  - 结果：通过。
  - 断言结果：返回 `[]`，不再报 `Parent world not found`。
- `WV-HOTFIX-004`
  - 测试内容：worldview list 自愈回归。
  - 执行方式：`test_worldviews_list_self_heals_missing_unique_library()`
  - 结果：通过。
  - 断言结果：手动删除唯一 library 后，再次 list 会自动补回相同 `worldview_id`。

### 降级结果

- 若 world 已不存在：`/api/worldviews/list` 返回空列表，不再把读回路径变成 400。

### 回滚结果

- 本轮未单独执行回滚 dry-run。

### 真实命令链验证结果

- 已完成：
  - 真实 world `world_f3312d20` 的缺库问题已通过 API 读路径自动修复。
  - `test_worldview_library_rules_requests.py` 全部通过。
  - `5006` 后端已重启到当前代码。

### 失败项

- 无新增失败项。

### 缺陷归因

- 根因：`127.0.0.1:5006` 上运行的是未重启的旧后端进程，旧进程创建出的 world 未自动生成 worldview library，形成脏数据。

### 是否满足完成口径

- 结论：本缺陷的 API/数据层修复已完成。
- 说明：
  - 已完成真实 HTTP 复现、修复、旧数据自愈和回归验证。
  - 页面级浏览器自动化证据本轮仍未补，但不影响这次“缺库”问题的 API/数据层结论。

---

## 2026-06-18 /worldviews 导入功能与层级保留补充

### 本次测试范围

- `src/app_api.py`
  - 新增 `POST /api/worldviews/import`
  - 支持 `md/json/xml/opml`
  - 导入后保留上下层级关系，写入 `path/category`
- `frontend/src/pages/LoreDB.tsx`
  - `/worldviews` 增加导入入口
  - 树视图按 `path/category` 展开 imported 世界观层级
- `frontend/src/pages/OutlineChapterWorkflow.tsx`
  - 导入文案与文件类型支持补齐 `xml`
- `tests/test_worldview_import_requests.py`
  - 真实导入请求扩展到 `xml`

### 本次执行时间

- 2026-06-18 01:49:00 CST - 2026-06-18 01:55:55 CST

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- 后端地址：`http://127.0.0.1:5006`
- 前端构建：`frontend/`
- Python：`.venv/bin/python`

### 执行命令或测试入口

1. `python3 -m py_compile src/app_api.py tests/test_worldview_import_requests.py`
2. `cd frontend && npm run build`
3. `API_BASE_URL=http://127.0.0.1:5006 .venv/bin/python tests/test_worldview_import_requests.py`
4. `API_BASE_URL=http://127.0.0.1:5006 .venv/bin/python - <<'PY' ... test_worldview_library_rules_requests ... PY`
5. 手工真实请求：
   - `POST /api/worlds/create`
   - `POST /api/worldviews/import`
   - `GET /api/lore/list?world_id=...&worldview_id=...&query=议会&page=1&page_size=20`

### 正常情况结果

- `WV-IMPORT-001`
  - 测试内容：`json/md/xml/opml` 四种格式均可导入。
  - 执行方式：`tests/test_worldview_import_requests.py`
  - 结果：通过。
  - 断言结果：脚本输出 `{'status': 'passed', ..., 'formats': ['json', 'md', 'xml', 'opml']}`。
- `WV-IMPORT-002`
  - 测试内容：导入后保留层级路径。
  - 执行方式：真实 `POST /api/worldviews/import` 导入 JSON，随后真实 `GET /api/lore/list`
  - 结果：通过。
  - 断言结果：导入返回 `path='社会 > 制度 > 议会'`；读回条目 `category/path` 也为 `社会 > 制度 > 议会`。

### 异常情况结果

- `WV-IMPORT-003`
  - 测试内容：唯一 worldview library 规则不被导入破坏。
  - 执行方式：导入功能上线后再跑 `test_worldview_library_rules_requests`
  - 结果：通过。
  - 断言结果：世界创建自动建库、禁用二次 create、删除 world 自动清理、list 自愈缺库 均未回归失败。

### 特殊情况结果

- `WV-IMPORT-004`
  - 测试内容：重复路径导入使用稳定 `doc_id` 更新同一路径条目，而不是每次生成新路径节点。
  - 执行方式：代码级检查 + 真实请求读回。
  - 结果：通过代码与读回验证。
  - 断言结果：`doc_id` 由 `world_id + worldview_id + path` 计算，导入条目读回 `id/doc_id/path` 一致。

### 降级结果

- world 不存在时，`/api/worldviews/import` 不会伪造成功，仍按 world/worldview 校验失败。
- 空解析结果时，接口返回 `400 No structured worldview entries parsed from file`。

### 回滚结果

- 本轮未单独执行回滚 dry-run。

### 真实命令链验证结果

- 已完成：
  - `json/md/xml/opml` 四种格式真实上传导入
  - 导入后真实查询读回
  - 层级路径真实落库为 `path/category`
  - 前端 `npm run build` 成功
- 未完成：
  - `/worldviews` 页面浏览器级点击上传与 DOM 树截图证据

### 失败项

- 无新增接口失败项。
- 页面浏览器自动化验证仍受既有环境阻塞影响，本轮未新增页面层通过证据。

### 缺陷归因

- 根因：仓库原本只有前端导入入口和测试，缺少真实后端 `/api/worldviews/import` 路由；同时 `/worldviews` 树视图未按 `path/category` 展开 imported 层级。

### 是否满足完成口径

- 结论：该导入功能的 API/数据层与前端构建层已完成。
- 说明：
  - 真实上传、写库、层级保留、查询读回都已验证。
  - 页面浏览器自动化证据本轮仍未补，因此页面功能测试仍是部分完成，不宣称页面层完全闭环。

---

## 2026-06-18 /worldviews 层级新增补充

### 本次测试范围

- `frontend/src/pages/LoreDB.tsx`
  - 树节点增加“在此节点下新增”
  - 世界观条目增加“新增同级设定 / 新增子设定”
- `frontend/src/pages/HierarchyWorkflow.tsx`
  - worldview create 页面展示“新增后所在层级”
  - 接收并保留 `parent_path`
- `src/agents/worldview_agent.py`
  - LLM 初始扩充/修改节点保留 `parent_path/path`
  - 写库节点按 `parent_path` 计算最终 `path/category`
- `src/app_api.py`
  - `POST /api/archive/update` 的 worldview 条目支持 `path`
- `tests/test_real_agent_worldview_flow_requests.py`
  - 新增嵌套层级真实 requests 回归

### 本次执行时间

- 2026-06-18 09:43:00 CST - 2026-06-18 10:08:10 CST

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- 后端地址：
  - 隔离回归：`http://127.0.0.1:5007`
  - 页面验证：`http://127.0.0.1:5006`
- 前端地址：`http://127.0.0.1:5174`
- Python：`.venv/bin/python`
- 浏览器验证：Playwright CLI

### 执行命令或测试入口

1. `python3 -m py_compile src/app_api.py src/agents/worldview_agent.py`
2. `cd frontend && npm run build`
3. `API_BASE_URL=http://127.0.0.1:5007 python3 -m pytest tests/test_real_agent_worldview_flow_requests.py -q -k respects_parent_path`
4. 手工真实命令链：
   - `POST /api/worlds/create`
   - `GET /api/worldviews/list?world_id=...`
   - `POST /api/hierarchy-agent/start`
   - `POST /api/hierarchy-agent/respond`
   - `GET /api/lore/list?query=测试新生命`
   - `DELETE /api/worlds/delete`
5. 页面入口：
   - `http://127.0.0.1:5174/worldviews`
   - 点击 `碳基生命` 节点上的“在“碳基生命”下新增子设定”
   - 点击 `秦族` 条目上的“新增同级设定”
6. 回滚 dry-run：
   - `git diff -- frontend/src/pages/LoreDB.tsx frontend/src/pages/HierarchyWorkflow.tsx frontend/src/api/client.ts src/agents/worldview_agent.py src/app_api.py tests/test_real_agent_worldview_flow_requests.py 测试计划文件.md | git apply --check -R -`

### 正常情况结果

- `WV-NEST-001`
  - 测试内容：worldview Agent 在 `parent_path=种族 > 碳基生命` 下创建新设定。
  - 执行方式：真实 HTTP `start -> approve -> lore list`
  - 结果：通过。
  - 断言结果：最终读回 `path/category=种族 > 碳基生命 > 测试新生命`，且 `world_id/worldview_id` 正确。
- `WV-NEST-002`
  - 测试内容：`/worldviews` 页面上的树节点新增入口可见且可跳转。
  - 执行方式：Playwright 打开 `/worldviews`，在验证世界中查看 `种族 -> 碳基生命` 树。
  - 结果：通过。
  - 断言结果：DOM 中出现 `在“碳基生命”下新增子设定` 按钮；点击后跳转到 `/workflow/worldview?...parent_path=种族 > 碳基生命`。

### 异常情况结果

- `WV-NEST-003`
  - 测试内容：层级参数在 LLM 草案节点是否会丢失。
  - 执行方式：先跑真实 requests 回归，再查看失败读回与 run payload。
  - 结果：先失败，后修复并复验通过。
  - 失败现象：第一次回归时，LLM 草案返回的 `payload` 丢失 `parent_path`，导致写库路径退回模型自生分类 `生命形态与生物学 > 新生命...`。
  - 修复后断言结果：`initial_expansion.payload`、`expanded_input`、`pending_payload` 均保留 `parent_path`，approve 后读回路径正确。

### 特殊情况结果

- `WV-NEST-004`
  - 测试内容：从条目“新增同级设定”进入 workflow。
  - 执行方式：Playwright 在 `/worldviews` 点击 `秦族` 行的 `新增同级设定`。
  - 结果：通过。
  - 断言结果：页面跳转到 `/workflow/worldview?...parent_path=种族 > 碳基生命&path=种族 > 碳基生命 > 秦族`，说明同级创建保留了父级层级上下文。
- `WV-NEST-005`
  - 测试内容：workflow 创建页显示新增后所在层级。
  - 执行方式：Playwright 检查 `/workflow/worldview` 页面表单。
  - 结果：通过。
  - 断言结果：页面出现只读字段 `新增后所在层级`，显示 `种族 > 碳基生命 > 新设定名称`。
- `WV-NEST-006`
  - 测试内容：前端构建与 Python 语法检查。
  - 执行方式：`npm run build` + `py_compile`
  - 结果：通过。
  - 断言结果：Vite build 退出码 0；`py_compile` 退出码 0。

### 降级结果

- 当未从 `/worldviews` 节点进入时，worldview create 页面仍可作为顶层设定创建，不会因为缺少 `parent_path` 而报错。
- 当只读层级字段存在但未启动 workflow 时，不会伪写库，也不会把当前条目移动到其他路径。

### 回滚结果

- `git apply --check -R -` 退出码 0。
- 说明：存在既有文件中的 trailing whitespace warning，但未阻断本轮改动的反向应用检查。

### 真实命令链验证结果

- 已完成：
  - 隔离后端 `5007` 的真实 `start -> approve -> readback -> cleanup`
  - 页面 `/worldviews -> workflow/worldview` 的真实浏览器跳转
  - `parent_path` 在 run payload 和最终 Mongo lore 条目中的保留
- 证据：
  - 真实 approve 返回 `200`
  - lore 读回 `path=种族 > 碳基生命 > 测试新生命`
  - Playwright DOM 显示 `在“碳基生命”下新增子设定`、`新增同级设定`、`新增后所在层级`

### 失败项

- `P1 / WV-NEST-BUG-001`
  - 失败现象：第一次真实回归时，新增设定未落到 `种族 > 碳基生命` 下，而是被模型分类重写。
  - 失败位置：`src/agents/worldview_agent.py` 初始扩充结果进入 `pending_payload` 前。
  - 影响范围：所有从 `/worldviews` 节点发起的嵌套新增。
  - 复现方式：`API_BASE_URL=http://127.0.0.1:5007 python3 -m pytest tests/test_real_agent_worldview_flow_requests.py -q -k respects_parent_path`
  - 当前判断：产品缺陷。
  - 是否保留失败结论：是，已保留并在修复后复验关闭。
  - 当前状态：已修复已验证。

### 缺陷归因

- 根因：`worldview_agent` 的 LLM 初始扩充/修改节点没有强制保留 `parent_path/path`，导致层级新增上下文在进入 `commit_node` 之前被草案 payload 覆盖。

### 是否满足完成口径

- 结论：本次 `/worldviews` 层级新增缺陷已完成 API、工作流和页面功能闭环验证。
- 说明：
  - 已完成端到端真实命令链验证。
  - 已完成失败路径定位、修复、复验和缺陷记录。
  - 已完成页面功能测试，不只是接口可用性测试。

---

## 2026-06-18 `/novels/:novelId/outlines` 拆分为分卷大纲页与分卷章节大纲页补充

### 本次测试范围

- `frontend/src/pages/NovelOutlineManagement.tsx`
  - `/novels/:novelId/outlines` 只保留分卷大纲
  - 增加跳转到 `/novels/:novelId/chapters`
  - 支持按分卷跳转章节大纲页
- `frontend/src/pages/NovelChapterManagement.tsx`
  - `/novels/:novelId/chapters` 管理分卷章节大纲
  - 支持 `outline_id` 预筛选
  - 支持创建、查看、修改、删除
- `frontend/src/api/client.ts`
  - `listLore` 增加 `type` 查询参数

### 本次执行时间

- 2026-06-18 20:45:00 CST - 2026-06-18 21:08:48 CST

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- 前端构建：`frontend/`
- 后端地址：`http://127.0.0.1:5006`
- Python：`.venv/bin/python`
- 浏览器自动化：本机未安装 Playwright 运行时

### 执行命令或测试入口

1. `cd frontend && npm run build`
2. `node -e "try{require('playwright');console.log('playwright-installed')}catch(e){console.log('playwright-missing')}"`
3. `.venv/bin/python - <<'PY' ... novel_outline_chapter_split_http ... PY`

### 正常情况结果

- `NOVEL-OUTLINE-SPLIT-001`
  - 测试内容：按小说读取分卷大纲与分卷章节大纲，两者通过不同接口拆开。
  - 执行方式：真实创建 world/worldview/novel/2 个 outline/2 个 chapter 后，分别调用：
    - `GET /api/outlines/list?novel_id=novel_outline_split_verify&page=1&page_size=20`
    - `GET /api/lore/list?world_id=world_outline_split_verify&novel_id=novel_outline_split_verify&type=prose&page=1&page_size=20`
  - 结果：通过。
  - 断言结果：
    - outlines 返回 `outline_split_a`、`outline_split_b`
    - lore/prose 返回 `chapter_split_a1`、`chapter_split_b1`
- `NOVEL-OUTLINE-SPLIT-002`
  - 测试内容：从分卷页跳到章节页时可按 `outline_id` 预筛选。
  - 执行方式：真实请求 `GET /api/lore/list?world_id=world_outline_split_verify&novel_id=novel_outline_split_verify&outline_id=outline_split_a&type=prose&page=1&page_size=20`
  - 结果：通过。
  - 断言结果：只返回 `chapter_split_a1`，不返回 `chapter_split_b1`。

### 异常情况结果

- `NOVEL-OUTLINE-SPLIT-003`
  - 测试内容：浏览器级页面验证。
  - 执行方式：本机检测 Playwright 运行时可用性。
  - 结果：未通过。
  - 失败现象：`node -e "require('playwright')"` 输出 `playwright-missing`。
  - 当前判断：环境缺失。

### 特殊情况结果

- `NOVEL-OUTLINE-SPLIT-004`
  - 测试内容：前端拆分页面后的 TypeScript/Vite 构建。
  - 执行方式：`cd frontend && npm run build`
  - 结果：通过。
  - 断言结果：`tsc` 和 `vite build` 退出码均为 `0`。

### 降级结果

- 当浏览器自动化不可用时，当前只保留前端构建与真实 HTTP 读回证据，不把这次页面改动宣称为已完成页面功能闭环。

### 回滚结果

- 本轮未单独执行针对 `NovelOutlineManagement.tsx` / `NovelChapterManagement.tsx` 的回滚 dry-run。

### 真实命令链验证结果

- 已完成：
  - 真实写入 2 个分卷大纲与 2 个分卷章节大纲
  - 真实读回 outlines 与 lore/prose 的拆分结果
  - 真实按 `outline_id` 过滤章节大纲
  - 前端 `npm run build` 成功
- 证据：
  - `.venv/bin/python` 输出 `OK: novel_outline_chapter_split_http`
  - `npm run build` 退出码 `0`

### 失败项

- `P2 / NOVEL-OUTLINE-SPLIT-BUG-001`
  - 失败现象：本轮没有拿到 `/novels/:novelId/outlines` 与 `/novels/:novelId/chapters` 的真实 DOM/交互截图证据。
  - 失败位置：验证环境
  - 影响范围：页面功能测试闭环不足，当前只能证明实现与后端链路，不足以证明最终渲染完全正确。
  - 复现方式：`node -e "try{require('playwright');...}"` 输出 `playwright-missing`
  - 当前判断：环境缺失
  - 是否保留失败结论：是
  - 当前状态：未修复

### 缺陷归因

- 根因：`/novels/:novelId/outlines` 原页面只读取 `outlines`，没有把 `prose(type=prose)` 这一层的分卷章节大纲单独管理；同时现有章节管理页虽然已存在，但缺少从分卷页携带 `outline_id` 的跳转与“查看”入口。

### 是否满足完成口径

- 结论：尚未达到交付标准。
- 说明：
  - 已完成前端实现、真实 HTTP 拆分读回和构建验证。
  - 但页面级浏览器验证缺失，当前只能算 `partial`，不能宣称页面功能测试完成。

## 2026-06-18 大纲工作流启动与长纲防压缩补充

### 本次测试范围

- `/workflow/outline?action=create&world_id=world_diag_8e262840&novel_id=novel_3f72502f` 启动前校验与自动识别卷名。
- `src/agents/outline_agent.py` 的长纲保留 Prompt 与“过度压缩直接失败”保护。

### 本次执行时间

- 2026-06-18 21:35:57 CST

### 执行环境

- 前端：本机 Vite dev server `http://127.0.0.1:5174`
- 后端：本机 Flask API `http://127.0.0.1:5006`
- 浏览器自动化：系统 Chrome + `playwright` Node 包，通过 `node_repl`
- Python：`.venv/bin/python`

### 执行命令或测试入口

1. `cd frontend && npm run build`
2. `node_repl` 打开 `/workflow/outline?action=create&world_id=world_diag_8e262840&novel_id=novel_3f72502f`，只填写“分卷大纲正文”，不填写“卷名”，点击“启动工作流”，抓取 `POST /api/hierarchy-agent/start` 请求体
3. `.venv/bin/python -m py_compile src/agents/outline_agent.py tests/test_hierarchy_draft_llm_invocation.py`
4. `.venv/bin/python - <<'PY' ... test_outline_prompts_require_preserving_long_form_outline_content(); test_outline_generation_rejects_over_simplified_long_form_output() ... PY`
5. `python3 - <<'PY' ... urllib.request.urlopen('http://127.0.0.1:5006/api/hierarchy-agent/start', timeout=180) ... PY`

### 正常情况结果

- `OUTLINE-WF-START-001`
  - 测试内容：只填写大纲正文首行 `第一卷：失踪者`，不手填卷名，前端仍能真实发起工作流启动请求。
  - 执行方式：浏览器自动化打开 `/workflow/outline?...`，填入分卷大纲正文，点击“启动工作流”，监听 `POST /api/hierarchy-agent/start`。
  - 结果：通过。
  - 断言结果：
    - `卷名` 输入框保持空白
    - 请求已真实发出
    - 请求体中的 `payload.name` 自动带入 `第一卷：失踪者`
    - 请求体中的 `payload.summary` 首行仍为 `第一卷：失踪者`
- `OUTLINE-PROMPT-001`
  - 测试内容：Prompt 文本明确要求长纲保留、不得缩写、不得降低结构粒度。
  - 执行方式：直接执行 `test_outline_prompts_require_preserving_long_form_outline_content()`
  - 结果：通过。
  - 断言结果：
    - Prompt 明确包含“输出 summary 不得短于输入 summary”
    - Prompt 明确要求保留 `第一卷/第一章/尾声` 等结构标记
    - Prompt 明确禁止把多章长纲改写成更短摘要

### 异常情况结果

- `OUTLINE-PROMPT-002`
  - 测试内容：当 LLM 返回明显被压缩的短摘要时，后端应拒绝该结果，不允许伪成功。
  - 执行方式：直接执行 `test_outline_generation_rejects_over_simplified_long_form_output()`
  - 结果：通过。
  - 断言结果：
    - `generate_initial_expansion()` 抛出 `compressed long outline too aggressively`
    - `generate_content_modification()` 抛出 `compressed long outline too aggressively`

### 特殊情况结果

- `OUTLINE-WF-START-002`
  - 测试内容：前端改动后的构建稳定性。
  - 执行方式：`cd frontend && npm run build`
  - 结果：通过。
  - 断言结果：`tsc` 与 `vite build` 均退出 `0`
- `OUTLINE-PROMPT-003`
  - 测试内容：相关 Python 文件语法有效。
  - 执行方式：`.venv/bin/python -m py_compile src/agents/outline_agent.py tests/test_hierarchy_draft_llm_invocation.py`
  - 结果：通过。
  - 断言结果：退出码 `0`

### 降级结果

- 当前已将“长纲被压缩”从“错误成功”降级为“显式失败”，即使 live LLM 返回了 JSON，也不会再把严重缩水的大纲继续往后流转。

### 回滚结果

- 本轮未单独执行针对 `frontend/src/pages/HierarchyWorkflow.tsx`、`src/agents/outline_agent.py`、`tests/test_hierarchy_draft_llm_invocation.py` 的回滚 dry-run。

### 真实命令链验证结果

- 已完成：
  - 真实浏览器页面点击“启动工作流”并抓到 `POST /api/hierarchy-agent/start`
  - 真实请求体读回，确认自动识别卷名
  - 前端 `npm run build`
  - Python 语法校验
  - 长纲 Prompt 与防压缩失败路径单测
- 证据：
  - 浏览器自动化读回：`requestSent=true`、`requestPayloadName=第一卷：失踪者`
  - `.venv/bin/python` 输出 `OK: outline_prompt_preserve_long_form_v2`
  - `.venv/bin/python` 输出 `OK: outline_rejects_oversimplified_long_form_output`

### 失败项

- `P1 / OUTLINE-WF-START-BUG-001`
  - 失败现象：直接调用 `POST /api/hierarchy-agent/start` 并等待完整响应时，180 秒后仍超时。
  - 失败位置：live LLM 启动链路
  - 影响范围：当前只能证明前端已经真实发起请求、Prompt 与防压缩保护已更新；还不能证明本轮 live LLM 端到端结果已稳定完成。
  - 复现方式：`python3 - <<'PY' ... urllib.request.urlopen(... timeout=180) ... PY`
  - 当前判断：产品缺陷或模型/推理时延异常，待继续排查
  - 是否保留失败结论：是
  - 当前状态：未修复

### 缺陷归因

- 前端“点击无反应”的直接根因：`/workflow/outline` 路由未提供 `name`，但启动前强制要求 `name`，且按钮附近没有可见的启动前错误提示。
- 大纲被严重简化的直接根因：`outline_agent` Prompt 虽然写了“保留”，但约束还不够硬，没有把“保持同等结构粒度”“输出长度不得短于输入”“结构标记丢失直接判失败”落成强约束与运行时校验。

### 是否满足完成口径

- 结论：尚未达到交付标准。
- 说明：
  - 已完成前端启动校验修复、自动卷名识别、Prompt 收紧、防压缩失败保护、构建验证、浏览器真实请求验证和失败路径验证。
  - 但 live LLM 的完整 `start -> 返回 run -> 后续审批/读回` 链路，本轮仍未拿到稳定完成证据，且存在 180 秒超时的 `P1`，因此不能宣称完成。

## 2026-06-18 大纲 LLM 拆分与 Prompt 去重修正补充

### 本次测试范围

- `src/agents/outline_agent.py` 是否把新增与修改拆成两个独立 LLM agent 名。
- 大纲 Prompt 是否去掉重复输入，并把输出 JSON 示例改成占位结构而非真实输入回填。
- 旧 run 读回时 `agent_name` / `llm_agent_name` 是否能统一显示为真实大纲节点 LLM 名。

### 本次执行时间

- 2026-06-18 22:00:00 CST

### 执行环境

- Python：`.venv/bin/python`
- 配置：`config/llm.yml`

### 执行命令或测试入口

1. `.venv/bin/python -m py_compile src/agents/outline_agent.py src/app_api.py tests/test_hierarchy_draft_llm_invocation.py`
2. `.venv/bin/python - <<'PY' ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm(); test_all_hierarchy_modules_content_modification_calls_dedicated_llm(); test_outline_prompts_require_preserving_long_form_outline_content(); test_outline_generation_rejects_over_simplified_long_form_output() ... PY`

### 正常情况结果

- `OUTLINE-LLM-SPLIT-001`
  - 测试内容：大纲新增节点调用独立 LLM 名。
  - 执行方式：直接执行 `test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm()`
  - 结果：通过。
  - 断言结果：`outline_agent.generate_initial_expansion()` 调用 `outline_agent_initial_expansion`，返回 `agent_name=outline_agent_initial_expansion`、`llm_agent_name=outline_agent_initial_expansion`
- `OUTLINE-LLM-SPLIT-002`
  - 测试内容：大纲修改节点调用独立 LLM 名。
  - 执行方式：直接执行 `test_all_hierarchy_modules_content_modification_calls_dedicated_llm()`
  - 结果：通过。
  - 断言结果：`outline_agent.generate_content_modification()` 调用 `outline_agent_modify_content`，返回 `agent_name=outline_agent_modify_content`、`llm_agent_name=outline_agent_modify_content`

### 异常情况结果

- `OUTLINE-PROMPT-DEDUP-001`
  - 测试内容：当 LLM 把长纲过度压缩时，后端继续显式失败，不允许伪成功。
  - 执行方式：直接执行 `test_outline_generation_rejects_over_simplified_long_form_output()`
  - 结果：通过。
  - 断言结果：
    - `generate_initial_expansion()` 抛出 `compressed long outline too aggressively`
    - `generate_content_modification()` 抛出 `compressed long outline too aggressively`

### 特殊情况结果

- `OUTLINE-PROMPT-DEDUP-002`
  - 测试内容：Prompt 合同不再把真实输入值写进输出 JSON 示例。
  - 执行方式：直接执行 `test_outline_prompts_require_preserving_long_form_outline_content()`
  - 结果：通过。
  - 断言结果：
    - Prompt 包含 `【任务上下文】`
    - 输出合同包含 `[保留输入中的 novel_id]`、`[扩写后的完整分卷大纲正文]`、`[按反馈修正后的完整分卷大纲正文]`
    - 输入长纲正文不再直接出现在输出合同片段中
- `OUTLINE-PROMPT-DEDUP-003`
  - 测试内容：相关 Python 文件语法有效。
  - 执行方式：`.venv/bin/python -m py_compile src/agents/outline_agent.py src/app_api.py tests/test_hierarchy_draft_llm_invocation.py`
  - 结果：通过。
  - 断言结果：退出码 `0`

### 降级结果

- 旧的 run 读回如果仍把大纲节点记成 `outline_agent`，现在会在 `src/app_api.py` 读回阶段补正成期望的 `outline_agent_initial_expansion` 或 `outline_agent_modify_content`，避免页面继续误显示为同一个 LLM。

### 回滚结果

- 本轮未单独执行针对 `src/agents/outline_agent.py`、`src/app_api.py`、`config/llm.yml`、`tests/test_hierarchy_draft_llm_invocation.py` 的回滚 dry-run。

### 真实命令链验证结果

- 已完成：
  - Python 语法校验
  - 节点级独立 LLM 名回归
  - Prompt 合同去重回归
  - 长纲过度压缩失败路径回归
- 证据：
  - `.venv/bin/python` 输出 `OK: dedicated_llm_initial`
  - `.venv/bin/python` 输出 `OK: dedicated_llm_modify`
  - `.venv/bin/python` 输出 `OK: outline_prompt_contract_v3`
  - `.venv/bin/python` 输出 `OK: outline_rejects_oversimplified_v2`

### 失败项

- 当前无新增测试失败项。
- 但本轮没有重新跑 live LLM 的大纲工作流端到端，因此不能把这次结果当作页面与模型链路已全部闭环。

### 缺陷归因

- 直接根因 1：`outline_agent` 之前把 `initial_expansion` 与 `modify_content` 都绑定到同一个 `agent_name=outline_agent`，导致页面读回与调试信息无法区分两个节点。
- 直接根因 2：Prompt 输入区重复同时写了单独字段和完整 payload，输出合同又把真实输入值再次回填到示例中，造成噪音、重复和任务边界模糊。
- 直接根因 3：Prompt 模板没有把“输入上下文”和“输出合同”严格分离，示例更像带实参的样板，而不是可执行的结构约束。

### 是否满足完成口径

- 结论：尚未达到交付标准。
- 说明：
  - 已完成代码修正和节点级回归验证，证明这三个 Prompt/agent 名问题已被修正到代码层。
  - 但本轮没有重新跑 live LLM 的大纲 workflow 全链路读回，因此不能宣称大纲工作流已完成端到端验证。

## 2026-06-18 分卷大纲直接新增入口补充

### 本次测试范围

- `/novels/:novelId/outlines` 新增分卷大纲从“跳工作流”改为“页面内直接创建”。
- 真实浏览器验证直写 `outlines`，并确认页面停留在分卷大纲页，不跳转到 `/workflow/outline`。

### 本次执行时间

- 2026-06-18 22:04:06 CST

### 执行环境

- 前端：Vite dev server `http://127.0.0.1:5174`
- 后端：Flask API `http://127.0.0.1:5006`
- 浏览器自动化：系统 Chrome + `playwright`
- Python：`python3`

### 执行命令或测试入口

1. `cd frontend && npm run build`
2. `python3 - <<'PY' ... /api/worlds/create + /api/novels/create + /api/worldviews/list ... PY`
3. `node_repl` 打开 `http://127.0.0.1:5174/novels/<temp_novel_id>/outlines`，点击“新增分卷大纲”，填表后点击“直接创建”
4. `python3 - <<'PY' ... DELETE /api/worlds/delete(cascade=true) + GET /api/novels/get ... PY`

### 正常情况结果

- `NOVEL-OUTLINE-DIRECT-001`
  - 测试内容：页面内直接新增分卷大纲，不走工作流。
  - 执行方式：浏览器打开临时小说的分卷大纲页，点击“新增分卷大纲”，在弹窗内填写卷名和简介，点击“直接创建”。
  - 结果：通过。
  - 断言结果：
    - 页面出现提示 `这个入口直接写入 outlines，不走工作流。`
    - 真实请求发到 `/api/outlines/create`
    - 请求体包含 `name`、`summary`、`worldview_id`、`novel_id`、`world_id`
    - 成功提示显示 `分卷大纲已直接创建，并完成真实接口回查`
    - 新增行 `第一卷：直接新增验证` 在表格中可见
    - 页面 URL 仍停留在 `/novels/<temp_novel_id>/outlines`

### 异常情况结果

- `NOVEL-OUTLINE-DIRECT-002`
  - 测试内容：临时验证数据清理后，不残留小说记录。
  - 执行方式：调用 `DELETE /api/worlds/delete`，随后 `GET /api/novels/get?novel_id=<temp_novel_id>`
  - 结果：通过。
  - 断言结果：
    - 删除世界返回 `200`
    - 删除后再查临时小说返回 `404`

### 特殊情况结果

- `NOVEL-OUTLINE-DIRECT-003`
  - 测试内容：前端改动后的 TypeScript/Vite 构建稳定性。
  - 执行方式：`cd frontend && npm run build`
  - 结果：通过。
  - 断言结果：`tsc` 与 `vite build` 退出码均为 `0`

### 降级结果

- 当前只把“新增分卷大纲”改成直写入口；“修改分卷大纲”仍沿用工作流入口，避免本轮把两个链路混在一起。

### 回滚结果

- 本轮未单独执行针对 `frontend/src/pages/NovelOutlineManagement.tsx` 的回滚 dry-run。

### 真实命令链验证结果

- 已完成：
  - 前端构建
  - 真实创建临时 world / novel / worldview 上下文
  - 真实浏览器点击并提交“直接创建”
  - 真实读回新增成功提示与表格结果
  - 真实删除临时 world，并验证 novel 已不存在
- 证据：
  - 浏览器自动化读回 `requestUrl=http://127.0.0.1:5174/api/outlines/create`
  - 浏览器自动化读回 `rowVisible=true`
  - 清理脚本输出 `delete_world 200`
  - 清理后 `GET /api/novels/get` 返回 `404`

### 失败项

- 当前无新增失败项。

### 缺陷归因

- 直接根因：分卷大纲管理页原本只有 workflow 入口，缺少页面内直写创建能力，导致简单新增也必须走完整工作流。

### 是否满足完成口径

- 结论：就“分卷大纲直接新增入口”这一项，已完成实现和页面级真实验证。
- 说明：
  - 本轮证据已经覆盖前端构建、真实页面交互、真实接口写入、真实读回和临时数据清理。
  - 该结论只覆盖“新增分卷大纲直写入口”，不覆盖大纲 workflow 其他链路。

## 2026-06-18 分卷大纲双新增入口补充

### 本次测试范围

- `/novels/:novelId/outlines` 同时保留两个新增入口：
  - 工作流新增分卷大纲
  - 直接新增分卷大纲

### 本次执行时间

- 2026-06-18 22:10:55 CST

### 执行环境

- 前端：Vite dev server `http://127.0.0.1:5174`
- 后端：Flask API `http://127.0.0.1:5006`
- 浏览器自动化：系统 Chrome + `playwright`
- Python：`python3`

### 执行命令或测试入口

1. `cd frontend && npm run build`
2. `python3 - <<'PY' ... /api/worlds/create + /api/novels/create ... PY`
3. `node_repl` 打开 `http://127.0.0.1:5174/novels/<temp_novel_id>/outlines`
4. 点击“工作流新增分卷大纲”，确认跳转到 `/workflow/outline?action=create...`
5. 返回上一页，点击“直接新增分卷大纲”，确认出现直写弹窗
6. `python3 - <<'PY' ... DELETE /api/worlds/delete(cascade=true) ... PY`

### 正常情况结果

- `NOVEL-OUTLINE-DUAL-001`
  - 测试内容：工作流新增入口仍保留。
  - 执行方式：浏览器打开临时小说的分卷大纲页，点击“工作流新增分卷大纲”。
  - 结果：通过。
  - 断言结果：页面跳转到 `/workflow/outline?action=create&world_id=<temp_world_id>&novel_id=<temp_novel_id>`
- `NOVEL-OUTLINE-DUAL-002`
  - 测试内容：直接新增入口与工作流入口并存。
  - 执行方式：从 workflow 页返回分卷大纲页，点击“直接新增分卷大纲”。
  - 结果：通过。
  - 断言结果：
    - 页面仍停留在 `/novels/<temp_novel_id>/outlines`
    - 出现提示 `这个入口直接写入 outlines，不走工作流。`
    - 两个按钮都可见

### 异常情况结果

- `NOVEL-OUTLINE-DUAL-003`
  - 测试内容：临时验证数据清理。
  - 执行方式：调用 `DELETE /api/worlds/delete`
  - 结果：通过。
  - 断言结果：删除世界返回 `200`

### 特殊情况结果

- `NOVEL-OUTLINE-DUAL-004`
  - 测试内容：前端改动后的构建稳定性。
  - 执行方式：`cd frontend && npm run build`
  - 结果：通过。
  - 断言结果：`tsc` 与 `vite build` 退出码均为 `0`

### 降级结果

- 当前双入口只覆盖“新增分卷大纲”；修改分卷大纲仍然只有 workflow 入口。

### 回滚结果

- 本轮未单独执行针对 `frontend/src/pages/NovelOutlineManagement.tsx` 的回滚 dry-run。

### 真实命令链验证结果

- 已完成：
  - 前端构建
  - 真实创建临时 world / novel
  - 真实浏览器验证 workflow 新增入口跳转
  - 真实浏览器验证 direct 新增入口弹窗
  - 真实删除临时 world
- 证据：
  - 浏览器自动化读回 `workflowUrl=http://127.0.0.1:5174/workflow/outline?action=create&world_id=...&novel_id=...`
  - 浏览器自动化读回 `directModalVisible=true`
  - 浏览器自动化读回 `workflowButtonVisible=true`
  - 浏览器自动化读回 `directButtonVisible=true`
  - 清理脚本输出 `delete_world 200`

### 失败项

- 当前无新增失败项。

### 缺陷归因

- 直接根因：上一轮把“新增分卷大纲”入口直接替换成了直写入口，误删了用户仍然需要保留的 workflow 新增入口。

### 是否满足完成口径

- 结论：就“分卷大纲双新增入口”这一项，已完成实现和页面级真实验证。
- 说明：
  - 当前页面已同时提供 workflow 新增与 direct 新增两个入口，并已做真实浏览器验证。

## 2026-06-18 大纲初始扩写提示词替换补充

### 本次测试范围

- `outline_agent` 初始扩写提示词替换为“保留原文并扩写”的新模板。
- 保持现有输入输出合同：`payload.summary` 为扩写后的完整大纲，`expanded_input.summary_seed` 为原始大纲全文。

### 本次执行时间

- 2026-06-18 22:14:24 CST

### 执行环境

- Python：`.venv/bin/python`

### 执行命令或测试入口

1. `.venv/bin/python -m py_compile src/agents/outline_agent.py tests/test_hierarchy_draft_llm_invocation.py`
2. `.venv/bin/python - <<'PY' ... test_outline_prompts_require_preserving_long_form_outline_content(); test_outline_generation_rejects_over_simplified_long_form_output() ... PY`

### 正常情况结果

- `OUTLINE-PROMPT-V4-001`
  - 测试内容：初始扩写提示词已切换为新模板风格。
  - 执行方式：直接执行 `test_outline_prompts_require_preserving_long_form_outline_content()`
  - 结果：通过。
  - 断言结果：
    - Prompt 包含 `你是一名小说大纲扩写编辑`
    - Prompt 包含 `保留原有大纲全部内容`
    - Prompt 包含 `本任务是：扩写（Expand）`
    - Prompt 包含 `Review / Expand / Validate`
    - Prompt 明确 `expanded_input.summary_seed` 保存原始大纲全文
    - 输出合同使用占位符，不把真实输入正文回填进示例

### 异常情况结果

- `OUTLINE-PROMPT-V4-002`
  - 测试内容：即使提示词替换后，长纲被压缩的防线仍然有效。
  - 执行方式：直接执行 `test_outline_generation_rejects_over_simplified_long_form_output()`
  - 结果：通过。
  - 断言结果：
    - `generate_initial_expansion()` 仍会拒绝过度压缩结果
    - `generate_content_modification()` 仍会拒绝过度压缩结果

### 特殊情况结果

- `OUTLINE-PROMPT-V4-003`
  - 测试内容：相关 Python 文件语法有效。
  - 执行方式：`.venv/bin/python -m py_compile src/agents/outline_agent.py tests/test_hierarchy_draft_llm_invocation.py`
  - 结果：通过。
  - 断言结果：退出码 `0`

### 降级结果

- 本轮只替换了“初始扩写”提示词；修改节点提示词保持当前版本，避免一次同时改两套合同造成行为混淆。

### 回滚结果

- 本轮未单独执行针对 `src/agents/outline_agent.py` 与 `tests/test_hierarchy_draft_llm_invocation.py` 的回滚 dry-run。

### 真实命令链验证结果

- 已完成：
  - Python 语法校验
  - 初始扩写 Prompt 合同回归
  - 长纲防压缩失败路径回归
- 证据：
  - `.venv/bin/python` 输出 `OK: outline_prompt_contract_v4`
  - `.venv/bin/python` 输出 `OK: outline_rejects_oversimplified_v3`

### 失败项

- 当前无新增失败项。
- 但本轮没有重新跑 live LLM 的大纲 workflow，因此不能把这次提示词替换当成完整端到端验证。

### 缺陷归因

- 直接根因：旧版初始扩写 Prompt 虽然强调“长纲保留”，但整体风格仍偏抽象合同式，不够贴近你要的“保留原文并扩写”的直接任务表达。

### 是否满足完成口径

- 结论：就“大纲初始扩写提示词替换”这一项，已完成代码修改和定向回归验证。
- 说明：
  - 该结论只覆盖提示词合同与本地回归，不覆盖 live LLM 全链路表现。

## 2026-06-18 大纲修改与章节扩写/修改提示词重写补充

### 本次测试范围

- `outline_agent` 修改提示词改成“保留原文 + 按意见修正 + 相关位置补强”的新风格。
- `chapter_agent` 扩写提示词改成“保留原文并扩写”的新风格。
- `chapter_agent` 修改提示词改成“保留原文 + 按意见修正 + 相关位置补强”的新风格。
- `chapter_agent` 的初始扩写与修改节点拆成两个独立 LLM agent 名。

### 本次执行时间

- 2026-06-18 22:20:44 CST

### 执行环境

- Python：`.venv/bin/python`

### 执行命令或测试入口

1. `.venv/bin/python -m py_compile src/agents/outline_agent.py src/agents/chapter_agent.py src/app_api.py tests/test_hierarchy_draft_llm_invocation.py`
2. `.venv/bin/python - <<'PY' ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm(); test_all_hierarchy_modules_content_modification_calls_dedicated_llm(); test_outline_prompts_require_preserving_long_form_outline_content(); test_chapter_prompts_preserve_long_form_content_and_keep_modify_separate(); test_outline_generation_rejects_over_simplified_long_form_output(); test_chapter_generation_rejects_over_simplified_long_form_output() ... PY`

### 正常情况结果

- `OUTLINE-PROMPT-V5-001`
  - 测试内容：大纲修改提示词已切换为独立的“Modify + Expand”风格，而不是沿用扩写提示词。
  - 执行方式：直接执行 `test_outline_prompts_require_preserving_long_form_outline_content()`
  - 结果：通过。
  - 断言结果：
    - Prompt 包含 `你是一名小说大纲修订编辑`
    - Prompt 包含 `本任务是：修改并扩写（Modify + Expand）`
    - Prompt 包含 `不得删除未被点名修改的内容`
    - Prompt 包含 `Modify / Expand / Validate`
    - 输出合同使用 `[按修改意见修正并补强后的完整大纲]`
- `CHAPTER-PROMPT-V1-001`
  - 测试内容：章节扩写与章节修改提示词已拆分成两套不同风格。
  - 执行方式：直接执行 `test_chapter_prompts_preserve_long_form_content_and_keep_modify_separate()`
  - 结果：通过。
  - 断言结果：
    - 扩写 Prompt 包含 `你是一名小说章节扩写编辑`
    - 修改 Prompt 包含 `你是一名小说章节修订编辑`
    - 扩写 Prompt 明确 `本任务是：扩写（Expand）`
    - 修改 Prompt 明确 `本任务是：修改并扩写（Modify + Expand）`
    - 两套 Prompt 都要求保留原文、不允许摘要化
- `CHAPTER-LLM-SPLIT-001`
  - 测试内容：章节初始扩写与修改节点调用独立 LLM 名。
  - 执行方式：执行 `test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm()` 与 `test_all_hierarchy_modules_content_modification_calls_dedicated_llm()`
  - 结果：通过。
  - 断言结果：
    - `chapter_agent.generate_initial_expansion()` 调用 `chapter_agent_initial_expansion`
    - `chapter_agent.generate_content_modification()` 调用 `chapter_agent_modify_content`

### 异常情况结果

- `CHAPTER-PROMPT-V1-002`
  - 测试内容：章节长文本被模型明显压缩时，后端必须拒绝该结果。
  - 执行方式：直接执行 `test_chapter_generation_rejects_over_simplified_long_form_output()`
  - 结果：通过。
  - 断言结果：
    - `generate_initial_expansion()` 抛出 `compressed long chapter too aggressively`
    - `generate_content_modification()` 抛出 `compressed long chapter too aggressively`
- `OUTLINE-PROMPT-V5-002`
  - 测试内容：大纲长文本被模型明显压缩时，现有防线仍有效。
  - 执行方式：直接执行 `test_outline_generation_rejects_over_simplified_long_form_output()`
  - 结果：通过。
  - 断言结果：
    - `generate_initial_expansion()` 仍拒绝过度压缩结果
    - `generate_content_modification()` 仍拒绝过度压缩结果

### 特殊情况结果

- `PROMPT-REWRITE-001`
  - 测试内容：相关 Python 文件语法有效。
  - 执行方式：`.venv/bin/python -m py_compile src/agents/outline_agent.py src/agents/chapter_agent.py src/app_api.py tests/test_hierarchy_draft_llm_invocation.py`
  - 结果：通过。
  - 断言结果：退出码 `0`

### 降级结果

- 本轮只重写了 `outline` 修改 Prompt 和 `chapter` 扩写/修改 Prompt；没有同步重写 `chapter` 之外的其他 Agent。

### 回滚结果

- 本轮未单独执行针对 `src/agents/outline_agent.py`、`src/agents/chapter_agent.py`、`src/app_api.py`、`config/llm.yml`、`tests/test_hierarchy_draft_llm_invocation.py` 的回滚 dry-run。

### 真实命令链验证结果

- 已完成：
  - Python 语法校验
  - 大纲修改 Prompt 合同回归
  - 章节扩写/修改 Prompt 合同回归
  - 章节独立 LLM 名回归
  - 大纲/章节防压缩失败路径回归
- 证据：
  - `.venv/bin/python` 输出 `OK: dedicated_llm_initial_v2`
  - `.venv/bin/python` 输出 `OK: dedicated_llm_modify_v2`
  - `.venv/bin/python` 输出 `OK: outline_prompt_contract_v5`
  - `.venv/bin/python` 输出 `OK: chapter_prompt_contract_v1`
  - `.venv/bin/python` 输出 `OK: outline_rejects_oversimplified_v4`
  - `.venv/bin/python` 输出 `OK: chapter_rejects_oversimplified_v1`

### 失败项

- 当前无新增失败项。
- 但本轮没有重新跑 live LLM 的 outline/chapter workflow，因此不能把这次提示词重写当成完整端到端验证。

### 缺陷归因

- 直接根因 1：旧版 `outline` 修改 Prompt 和 `chapter` 两套 Prompt 仍偏结构化摘要模板，任务边界不够直接，容易把“扩写”退化成“整理/概括”。
- 直接根因 2：`chapter_agent` 之前仍把扩写与修改都记成同一个 `chapter_agent`，导致调试信息与节点职责不够清晰。

### 是否满足完成口径

- 结论：就“大纲修改 Prompt + 章节扩写/修改 Prompt 重写”这一项，已完成代码修改和定向回归验证。
- 说明：
  - 该结论只覆盖提示词合同、节点拆分和本地失败路径，不覆盖 live LLM 全链路表现。

## 2026-06-18 `/novels/:novelId/chapters` 直接新增分卷大纲入口补充

### 项目名称

- `novel_agent`

### 测试范围

- `frontend/src/pages/NovelChapterManagement.tsx`
- 页面 `/novels/:novelId/chapters`
- 从章节页直接调用 `/api/outlines/create` 新增分卷大纲
- 创建后 URL / 筛选状态 / “新增分卷章节大纲”按钮状态同步

### 不在范围

- `/workflow/outline` 的 live LLM 全链路
- 分卷大纲修改链路
- 分卷章节大纲新增后的章节内容工作流

### 风险清单

- 章节页原本依赖已有 `outline` 才能继续创建章节大纲，新增直写入口后如果不做真实读回，容易出现“提示成功但筛选没切过去”的假完成。
- 章节页与分卷页都能创建 `outline`，如果上下文字段不一致，可能把大纲写到错误小说或错误世界观。
- 新增 modal 后如果只验证桌面视口，不排除窄屏下按钮可见但无法操作。

### 测试目标

- 证明章节页在没有任何分卷时，仍可直接新增分卷大纲，不必跳工作流。
- 证明空卷名会被前端真实拦截，不会伪造成功提示。
- 证明创建成功后页面 URL 带上新的 `outline_id`，筛选框切到新分卷，章节工作流按钮变为可用。
- 证明移动端窄屏下入口仍可见，不因新增按钮而破版。

### 测试环境

- 前端：Vite dev server `http://127.0.0.1:5174`
- 后端：Flask API `http://127.0.0.1:5006`
- 浏览器自动化：in-app Browser
- Python：`python3`

### 测试入口

- 页面入口 1：`http://127.0.0.1:5174/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
- 页面入口 2：`http://127.0.0.1:5174/novels/<temp_novel_id>/chapters`
- API 入口：`POST /api/worlds/create`、`POST /api/novels/create`、`POST /api/outlines/create`、`GET /api/outlines/list`、`DELETE /api/worlds/delete`

### 测试数据与前置准备

- 临时世界 1：`world_874c7057`
- 临时小说 1：`novel_b83e82b1`
- 临时世界观 1：`wv_world_874c7057`
- 临时分卷 1：`outline_ec6e7f9d`
- 临时世界 2：`world_c2e7d06b`
- 临时小说 2：`novel_94569c2a`
- 临时世界观 2：`wv_world_c2e7d06b`
- 临时分卷 2：`outline_ea120bfc`
- 截图证据：
  - `/tmp/novel-agent-chapter-direct-outline-desktop.png`
  - `/tmp/novel-agent-chapter-direct-outline-mobile.png`

### 用例总表

- `NOVEL-CHAPTER-OUTLINE-DIRECT-001`
  - 用例名称：章节页在无分卷时直接新增分卷大纲
  - 用例类型：正常
  - 测试目标：证明章节页无需先跳 workflow 也能创建分卷，并把当前筛选切到新分卷
  - 前置准备：创建临时 `world_874c7057`、`novel_b83e82b1`
  - 执行命令或测试入口：浏览器打开 `/novels/novel_b83e82b1/chapters`，点击“直接新增分卷大纲”，填写卷名与正文后点击“直接创建”
  - 验证点：成功提示、URL、筛选框、按钮可用状态
  - 断言点：`POST /api/outlines/create` 成功；URL 变为 `/novels/novel_b83e82b1/chapters?outline_id=outline_ec6e7f9d`；筛选框显示 `第一卷：章节页直加分卷 (outline_ec6e7f9d)`；“新增分卷章节大纲”按钮变为可用
  - 观察日志：Browser DOM snapshot；后端 `GET /api/outlines/list` 读回
  - 预期副作用：新增一条 `outlines` 记录
  - 降级预期：若当前没有分卷，仍能通过 direct modal 先建分卷
  - 回滚预期：移除章节页 direct modal 和创建逻辑后，页面恢复为仅 workflow 入口
  - 失败归因方法：若 API 读回存在但页面不切筛选，判定前端状态同步缺陷；若页面提交成功但 API 无记录，判定产品缺陷；若 5006 不可达，判定环境缺失
  - 执行结果：通过
  - 最近一次执行时间：`2026-06-18 22:31:46 CST`

- `NOVEL-CHAPTER-OUTLINE-DIRECT-002`
  - 用例名称：章节页 direct modal 空卷名拦截
  - 用例类型：异常
  - 测试目标：证明空卷名不会发起伪成功创建
  - 前置准备：浏览器停留在 `/novels/novel_b83e82b1/chapters` direct modal
  - 执行命令或测试入口：不填写“卷名”，直接点击“直接创建”
  - 验证点：错误提示、modal 保持打开
  - 断言点：页面出现 `分卷大纲名称不能为空。`；modal 仍可见
  - 观察日志：Browser DOM snapshot
  - 预期副作用：不应新增新的 `outline`
  - 降级预期：前端在本地校验失败时直接阻断，不伪装成后端成功
  - 回滚预期：去掉该校验后该用例应失败，作为回滚后风险提示
  - 失败归因方法：若没有错误提示且记录被创建，判定产品缺陷；若按钮点击无效且无提示，判定前端交互缺陷
  - 执行结果：通过
  - 最近一次执行时间：`2026-06-18 22:31:46 CST`

- `NOVEL-CHAPTER-OUTLINE-DIRECT-003`
  - 用例名称：章节页 direct 入口在移动端窄屏仍可见
  - 用例类型：特殊
  - 测试目标：证明新增按钮不会在窄屏下消失或挤坏主操作区
  - 前置准备：页面已创建 `outline_ec6e7f9d`，Browser viewport 调成 `390x844`
  - 执行命令或测试入口：重载 `/novels/novel_b83e82b1/chapters?outline_id=outline_ec6e7f9d`
  - 验证点：两个主按钮可见，筛选框仍保留新分卷文本
  - 断言点：`直接新增分卷大纲` 可见；`新增分卷章节大纲` 可见；筛选框仍显示 `第一卷：章节页直加分卷 (outline_ec6e7f9d)`
  - 观察日志：Browser DOM snapshot；截图 `/tmp/novel-agent-chapter-direct-outline-mobile.png`
  - 预期副作用：无
  - 降级预期：若窄屏空间不足，至少不能让 direct 入口不可见
  - 回滚预期：回滚后该入口消失，此用例应失败
  - 失败归因方法：若桌面可见但移动端丢失，判定前端响应式缺陷
  - 执行结果：通过
  - 最近一次执行时间：`2026-06-18 22:31:46 CST`

- `NOVEL-CHAPTER-OUTLINE-DIRECT-004`
  - 用例名称：前端构建稳定性与二次回归
  - 用例类型：特殊
  - 测试目标：证明最终代码在重新收口后仍能构建并再次完成真实创建
  - 前置准备：创建临时 `world_c2e7d06b`、`novel_94569c2a`
  - 执行命令或测试入口：`cd frontend && npm run build`；浏览器打开 `/novels/novel_94569c2a/chapters` 走一遍 direct 创建
  - 验证点：构建退出码、成功提示、URL、筛选框
  - 断言点：`npm run build` 退出码 `0`；URL 变为 `/novels/novel_94569c2a/chapters?outline_id=outline_ea120bfc`；筛选框显示 `第一卷：二次验证 (outline_ea120bfc)`
  - 观察日志：终端构建输出；Browser 读回
  - 预期副作用：新增一条临时 `outline`，随后被清理
  - 降级预期：若 direct 创建失败，不应误报成功
  - 回滚预期：若回滚到无 direct 入口版本，此用例应无法完成
  - 失败归因方法：若构建失败，判定代码缺陷；若构建通过但页面不切换，判定前端状态同步缺陷
  - 执行结果：通过
  - 最近一次执行时间：`2026-06-18 22:31:46 CST`

### 正常情况测试

- `NOVEL-CHAPTER-OUTLINE-DIRECT-001`
  - 测了什么：章节页从 0 个分卷直接新增第一个分卷
  - 怎么测：真实创建临时 world/novel，浏览器进入 `/novels/novel_b83e82b1/chapters`，填写卷名 `第一卷：章节页直加分卷` 和正文，点击“直接创建”
  - 看到什么结果：页面出现 `分卷大纲已直接创建，并完成真实接口回查：outline_ec6e7f9d`
  - 结果是否通过：通过

### 异常情况测试

- `NOVEL-CHAPTER-OUTLINE-DIRECT-002`
  - 故障或错误输入：卷名留空
  - 系统反应：页面显示 `分卷大纲名称不能为空。`，modal 保持打开
  - 是否正确拦截或降级：是，正确拦截，没有伪造成功

### 特殊情况测试

- `NOVEL-CHAPTER-OUTLINE-DIRECT-003`
  - 边界条件：移动端窄屏 `390x844`
  - 与正常路径不同：验证的是按钮可见性和主操作区布局，而不是创建流程本身
  - 是否引出额外问题：未发现 direct 入口消失或筛选框状态丢失

- `NOVEL-CHAPTER-OUTLINE-DIRECT-004`
  - 边界条件：移除一次手动 `loadData()` 后重新做真实创建
  - 与正常路径不同：验证的是最终收口后的状态同步稳定性
  - 是否引出额外问题：未发现 URL/筛选不同步

### 降级验证

- 当前 direct 入口只负责“先补分卷”；分卷修改与章节正文生成仍保留原有 workflow，不把两类链路强行合并。
- 当页面尚无任何分卷时，workflow 章节按钮继续保持禁用，但 direct 分卷按钮可用，避免系统卡死在“必须先有 outline”。

### 回滚验证

- 本轮未单独执行 `git apply -R` 或页面回滚 dry-run。
- 已记录回滚预期：回滚后章节页应只剩 workflow 入口，本次新增的 4 个用例将失效。

### 真实命令链验证

1. `cd /Users/harry/Documents/git/novel_agent/frontend && npm run build`
2. `python3 - <<'PY' ... POST /api/worlds/create + POST /api/novels/create ... PY`
3. Browser 打开 `http://127.0.0.1:5174/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
4. Browser 打开 `http://127.0.0.1:5174/novels/novel_b83e82b1/chapters`
5. Browser 点击“直接新增分卷大纲”，空卷名提交一次，再填写有效卷名提交一次
6. Browser 切换到 `390x844` 视口并重载页面
7. `python3 - <<'PY' ... GET /api/outlines/list + DELETE /api/worlds/delete ... PY`
8. Browser 打开 `http://127.0.0.1:5174/novels/novel_94569c2a/chapters` 做二次真实创建回归

### 缺陷记录区

- 当前无新增产品缺陷。
- `TEST-POLLUTION-001`
  - 缺陷编号：`TEST-POLLUTION-001`
  - 缺陷等级：`P3`
  - 发现时间：`2026-06-18 22:31:46 CST`
  - 发现测试项：API 读回清理脚本
  - 复现命令或复现入口：首次把 `/api/outlines/list` 响应当作 `{data: ...}` 结构解析
  - 复现条件：使用错误的返回结构假设
  - 失败现象：`TypeError: list indices must be integers or slices, not str`
  - 失败位置：临时 Python 验证脚本
  - 当前影响范围：仅影响第一次验证脚本，不影响产品功能
  - 是否影响交付判断：否
  - 当前判断：测试污染
  - 当前状态：已修复已验证

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- API 真实读回：通过
- 临时数据清理：通过
- 回滚验证：未执行

### 最近一次执行时间

- `2026-06-18 22:31:46 CST`

### 本次测试范围

- `/novels/:novelId/chapters` 增加“直接新增分卷大纲”入口
- 真实浏览器验证 direct modal、空卷名拦截、成功创建后的 URL/筛选同步、移动端可见性

### 本次执行时间

- `2026-06-18 22:31:46 CST`

### 执行环境

- 前端：`http://127.0.0.1:5174`
- 后端：`http://127.0.0.1:5006`
- Browser：in-app Browser
- Python：`python3`

### 执行命令或测试入口

1. `cd /Users/harry/Documents/git/novel_agent/frontend && npm run build`
2. `python3 - <<'PY' ... POST /api/worlds/create + POST /api/novels/create ... PY`
3. Browser 打开 `/novels/novel_b83e82b1/chapters`
4. Browser 点击“直接新增分卷大纲”并执行空卷名提交
5. Browser 再次填写有效卷名并提交
6. `python3 - <<'PY' ... GET /api/outlines/list + DELETE /api/worlds/delete ... PY`
7. Browser 打开 `/novels/novel_94569c2a/chapters` 执行二次回归

### 正常情况结果

- 测了什么：无分卷小说在章节页直接新增分卷
- 怎么测：浏览器在 `novel_b83e82b1` 和 `novel_94569c2a` 上各跑一遍真实 direct 创建
- 看到什么结果：
  - 第一次创建后 URL 为 `http://127.0.0.1:5174/novels/novel_b83e82b1/chapters?outline_id=outline_ec6e7f9d`
  - 第二次创建后 URL 为 `http://127.0.0.1:5174/novels/novel_94569c2a/chapters?outline_id=outline_ea120bfc`
  - 两次筛选框都切到新分卷，章节 workflow 按钮可用
- 结果是否通过：通过

### 异常情况结果

- 故障或错误输入是什么：空卷名提交
- 系统反应是什么：显示 `分卷大纲名称不能为空。`，modal 不关闭
- 是否正确拦截或降级：是

### 特殊情况结果

- 边界条件是什么：移动端 `390x844` 视口；以及移除手动 `loadData()` 后再做真实创建回归
- 与正常路径有什么不同：验证的是响应式可见性和状态同步收口，不只是新增 API
- 是否引出额外问题：没有

### 降级结果

- direct 入口只补“先建分卷”；章节正文生成仍保持 workflow 路径
- 这保证了新需求落地时不把已有 workflow 逻辑一并改坏

### 回滚结果

- 未执行单独回滚 dry-run

### 真实命令链验证结果

- 通过：
  - `npm run build`
  - Browser 真实页面交互
  - `/api/outlines/list` 真实读回
  - `/api/worlds/delete` 真实清理
- 证据：
  - Browser 读回 `http://127.0.0.1:5174/novels/novel_b83e82b1/chapters?outline_id=outline_ec6e7f9d`
  - Browser 读回 `第一卷：章节页直加分卷 (outline_ec6e7f9d)`
  - Browser 读回 `http://127.0.0.1:5174/novels/novel_94569c2a/chapters?outline_id=outline_ea120bfc`
  - Python 读回 `outline_count 1`
  - Python 清理后读回 `novel_after_delete 404`

### 失败项

- 产品失败项：无
- 测试失败项：`TEST-POLLUTION-001`，已修正，不保留为产品失败结论

### 缺陷归因

- 直接根因：章节页以前只允许“基于已有分卷继续新增章节大纲”，缺少“在当前页直接补一个分卷”的入口，导致用户必须先跳回分卷页或走 workflow。
- 修复方式：在章节页复用分卷页已有的 direct create 模式，真实写入 `outlines`，创建成功后把页面切到新 `outline_id`。

### 是否满足完成口径

- 结论：该功能已完成实现，并拿到正常 / 异常 / 特殊情况与真实命令链证据；但本轮未单独执行回滚 dry-run。
- 说明：
  - 如果只看“章节页直接新增分卷大纲”这一项，当前行为已经真实可用。
  - 如果严格按仓库全量测试硬口径，本轮仍缺单独回滚验证证据，因此不把整个项目表述为“已交付”。

## 2026-06-18 总结工作流 6 路新增补充

### 本次测试范围

- 新增 6 个独立总结工作流后端模块：
  - `outline_summary_create`
  - `outline_summary_update`
  - `chapter_outline_summary_create`
  - `chapter_outline_summary_update`
  - `chapter_content_summary_create`
  - `chapter_content_summary_update`
- 扩展 `/api/hierarchy-agent/*` 对这 6 个类型的识别与推进
- 扩展通用工作流页 `HierarchyWorkflow.tsx` 与前端类型声明
- 新增 `downstream_summaries` 列表接口

### 本次执行时间

- `2026-06-18 23:08:41 CST`

### 执行环境

- 工作目录：`/Users/harry/Documents/git/novel_agent`
- Python：`.venv/bin/python`
- 前端构建：`npm run build`
- 后端验证方式：模块级回归 + `app_api._run_until_human(...)` 运行器回归

### 执行命令或测试入口

1. `.venv/bin/python -m py_compile src/agents/outline_summary_create_agent.py src/agents/outline_summary_update_agent.py src/agents/chapter_outline_summary_create_agent.py src/agents/chapter_outline_summary_update_agent.py src/agents/chapter_content_summary_create_agent.py src/agents/chapter_content_summary_update_agent.py src/app_api.py tests/test_summary_workflow_requests.py`
2. `.venv/bin/python - <<'PY' ... tests.test_summary_workflow_requests::* PY`
3. `cd /Users/harry/Documents/git/novel_agent/frontend && npm run build`

### 正常情况结果

- 测了什么：6 个总结工作流的独立 LLM 调用、独立 prompt、独立 review 节点与后端运行器识别
- 怎么测：直接调用新增测试 `tests/test_summary_workflow_requests.py`
- 看到什么结果：
  - `OK: summary_dedicated_llm_names`
  - `OK: summary_prompts_are_separate`
  - `OK: app_api_registers_summary_types`
  - `OK: app_api_outline_summary_run_until_human`
- 结果是否通过：通过

### 异常情况结果

- 故障或错误输入是什么：给检查节点喂入过短的 `downstream_summary`
- 系统反应是什么：`review_node` 返回 `review_passed=False`，并把 `current_node` 切到 `modify_content`
- 是否正确拦截或降级：是

### 特殊情况结果

- 边界条件是什么：同一套 `chapter/prose` 现存边界同时承载“章节大纲总结”和“章节内容总结”两类 workflow type
- 与正常路径有什么不同：不是只验证单一 agent，而是验证通用工作流页和后端注册表能同时接受多种新 type
- 是否引出额外问题：没有新增编译错误；前端 `tsc + vite build` 通过

### 降级结果

- 当前只完成了通用工作流页 `type=...` 的接入，没有额外挂新的管理页按钮
- 这意味着功能可通过通用 workflow 页启动，但还没有专门入口页

### 回滚结果

- 未执行单独回滚 dry-run

### 真实命令链验证结果

- 通过：
  - `.venv/bin/python -m py_compile ...`
  - `.venv/bin/python - <<'PY' ... tests.test_summary_workflow_requests::* PY`
  - `npm run build`
- 证据：
  - `OK: summary_dedicated_llm_names`
  - `OK: summary_review_blocks_oversimplified`
  - `OK: summary_prompts_are_separate`
  - `OK: app_api_registers_summary_types`
  - `OK: app_api_outline_summary_run_until_human`
  - `vite build ... ✓ built in 10.92s`

### 失败项

- 产品失败项：无新增已确认产品缺陷
- 测试失败项：第一次用系统 `python3` 跑回归时缺少 `langgraph`，属于环境差异，不保留为产品失败结论

### 缺陷归因

- 当前仓库里“章节大纲 / 章节正文”仍复用 `chapter/prose` 边界
- 本轮处理方式不是强行拆库，而是在 workflow type 和 `downstream_summaries` 维度把 6 条总结链路隔离开

### 是否满足完成口径

- 结论：已完成代码实现、模块回归、运行器回归和前端编译验证；但未完成 live 页面端到端点击验证，因此当前不能宣称“已交付”。
- 说明：
  - 后端和前端编译层面已经可用
  - 仍缺浏览器真实启动 `/workflow?type=...` 并走完人工确认到写库的端到端证据

## 2026-06-18 章节直接内容检查工作流

### 本次测试范围

- `/api/hierarchy-agent/start` 新增 `agent_type=chapter, action=check`
- `src/agents/chapter_check_agent.py` 六段检查链与输出节点
- `src/agents/review_agent.py` 新增 `chapter_chapter_outline_rules`、`chapter_plot_errors` 提示词与上下文拼装
- `frontend/src/pages/HierarchyWorkflow.tsx` `action=check` 表单、节点图、结果区、行级预填
- `frontend/src/pages/NovelChapterManagement.tsx` 顶部“检查直接内容”和表格行“检查内容”入口
- `tests/test_real_chapter_check_workflow_requests.py` 真实请求回归

### 本次执行时间

- `2026-06-18 23:14:40 CST`

### 执行环境

- 工作目录：`/Users/harry/Documents/git/novel_agent`
- 后端：`PYTHONPATH=. .venv/bin/python -m src.app_api`
- 前端：本地 dev server `http://127.0.0.1:5174`
- 浏览器验证：Codex in-app Browser
- 真实 API：`http://127.0.0.1:5006`

### 执行命令或测试入口

1. `python3 -m py_compile src/app_api.py src/agents/review_agent.py src/agents/chapter_check_agent.py tests/test_real_chapter_check_workflow_requests.py`
2. `pytest -q tests/test_real_chapter_check_workflow_requests.py`
3. `cd frontend && npm run build`
4. `git diff -- src/app_api.py src/agents/review_agent.py src/agents/chapter_check_agent.py frontend/src/api/client.ts frontend/src/pages/HierarchyWorkflow.tsx frontend/src/pages/NovelChapterManagement.tsx tests/test_real_chapter_check_workflow_requests.py | git apply --check -R -`
5. Browser 入口一：`/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
6. Browser 入口二：临时验证数据 `/novels/novel_ui_check_903c1abd/chapters?outline_id=outline_ui_check_903c1abd`

### 正常情况结果

- 测了什么：真实 `chapter check` 工作流能从输入跑完“世界规则 -> 小说规则 -> 世界观 -> 分卷大纲 -> 章节大纲 -> 剧情错误 -> 输出结果”
- 怎么测：
  - 直接 Python 命令链创建 world/worldview/novel/outline/chapter outline，再 `POST /api/hierarchy-agent/start`
  - `pytest -q tests/test_real_chapter_check_workflow_requests.py`
  - Browser 从章节管理页点击“检查直接内容”，填入章节大纲与直接内容后启动检查
- 看到什么结果：
  - HTTP 返回 `status success`
  - `run_status completed current_node output committed False`
  - 节点顺序为 `input -> world_review -> novel_review -> worldview_review -> outline_review -> chapter_outline_review -> plot_review -> output`
  - `pytest` 结果 `2 passed in 113.56s`
  - Browser 页面出现 `当前节点：output`、`已输出检查结果`
- 结果是否通过：通过

### 异常情况结果

- 故障或错误输入是什么：只提供 `outline_id` 和直接内容，不提供 `chapter_outline`，也不指定目标章节
- 系统反应是什么：
  - 真实 API 立即返回 `400`
  - 返回体：`{'error': '章节检查工作流必须提供章节大纲，或指定可读取的目标章节', 'status': 'error'}`
- 是否正确拦截或降级：是，未进入 LLM 检查链

### 特殊情况结果

- 边界条件是什么：从章节管理表格行点击“检查内容”进入 workflow，要求自动带入“章节大纲来源 + 章节大纲正文”
- 与正常路径有什么不同：不是手工填表，而是依赖列表页携带 `id` 参数并在 workflow 页自动预填
- 是否引出额外问题：
  - 首次浏览器验证发现真实缺陷：`action=check` 未进入旧的目标记录预填分支，导致行级入口只选中了来源但未回填 `章节大纲`
  - 已修复后复验：Browser 读回 `hasPrefilledOutline=true`、`hasValidationError=false`、`hasSelectedSource=true`

### 降级验证

- 缺少 `chapter_outline` 且没有目标章节时，接口直接 400，前端不会伪造检查结果
- `action=check` 页面不显示“用户处理/批准写库”，明确降级为只读检查流

### 回滚验证

- 已执行本次改动范围内的 reverse-apply dry-run
- 结果：`git apply --check -R -` 退出码 `0`
- 备注：输出包含 `trailing whitespace` warning 1 条，但不阻断反向应用检查

### 真实命令链验证

- 通过：
  - `python3 -m py_compile ...`
  - `pytest -q tests/test_real_chapter_check_workflow_requests.py`
  - `cd frontend && npm run build`
  - 真实 Browser 点击章节页按钮进入 check workflow
  - 真实 Browser 在 workflow 页填入内容并拿到 `已输出检查结果`
  - 真实 Browser 从表格行“检查内容”进入 workflow 并验证自动回填
- 关键证据：
  - `start check success 200`
  - `run_status completed current_node output committed False`
  - `chapter_outline_review reviewer chapter_chapter_outline_review_agent`
  - `plot_review reviewer chapter_plot_review_agent`
  - `chapter_outline_prompt_has_keyword True`
  - `plot_prompt_has_keyword True`
  - `.. 2 passed in 113.56s (0:01:53)`

### 缺陷记录区

- `BUG-CHAPTER-CHECK-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-18 23:11 CST`
  - 发现测试项：章节管理表格行“检查内容” Browser 端到端验证
  - 复现命令或复现入口：Browser 打开 `/novels/novel_ui_check_903c1abd/chapters?outline_id=outline_ui_check_903c1abd`，点击表格行“检查内容”
  - 复现条件：workflow 页以 `action=check&id=<chapter_id>` 打开
  - 失败现象：`章节大纲来源` 已选中，但 `章节大纲` 文本框为空，页面提示“章节检查工作流必须提供章节大纲”
  - 失败位置：`frontend/src/pages/HierarchyWorkflow.tsx`
  - 当前影响范围：所有依赖列表行快捷入口进入章节检查的用户
  - 是否影响交付判断：是
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级验证：通过
- 回滚验证：通过
- 真实命令链验证：通过

### 最近一次执行时间

- `2026-06-18 23:14:40 CST`

## 2026-06-18 章节页补充分卷大纲工作流入口

### 本次测试范围

- `frontend/src/pages/NovelChapterManagement.tsx`
- 章节页 `/novels/:novelId/chapters?outline_id=...` 顶部工具栏
- 章节页到 `/workflow/outline` 的真实跳转

### 本次执行时间

- `2026-06-18 23:29:00 CST`

### 执行环境

- 工作目录：`/Users/harry/Documents/git/novel_agent`
- 前端：`http://127.0.0.1:5174`
- 浏览器验证：Codex in-app Browser

### 执行命令或测试入口

1. `cd frontend && npm run build`
2. `git diff -- frontend/src/pages/NovelChapterManagement.tsx | git apply --check -R -`
3. Browser 打开 `/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
4. Browser 点击 `工作流修改当前分卷`

### 正常情况结果

- 测了什么：章节页在已选中分卷时，是否暴露分卷大纲 workflow 修改入口
- 怎么测：Browser 打开 `/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`，读取工具栏按钮，再点击 `工作流修改当前分卷`
- 看到什么结果：
  - 工具栏按钮集合包含 `工作流修改当前分卷`
  - 点击后 URL 变为 `/workflow/outline?action=update&...&id=outline_f8bd96b0...`
  - 页面进入 outline workflow 修改页
- 结果是否通过：通过

### 异常情况结果

- 故障或错误输入是什么：章节页没有当前选中分卷时，不应错误跳到 update 模式
- 系统反应是什么：按钮文案按实现会退回 `工作流新增分卷大纲`
- 是否正确拦截或降级：是，未伪造当前分卷修改状态

### 特殊情况结果

- 边界条件是什么：章节页本身没有 outline 列表表格，只有当前筛选分卷上下文
- 与正常路径有什么不同：workflow 按钮必须复用当前 `selectedOutline`，而不是强迫用户先跳回分卷页
- 是否引出额外问题：首次代码补丁缺少 `Outline.summary` 类型字段，`tsc` 报错；已修复并重新构建通过

### 降级验证

- 没有选中分卷时，按钮仍保留 workflow create 入口，不会把空 outline 强行塞进 update

### 回滚验证

- 已执行 `git diff -- frontend/src/pages/NovelChapterManagement.tsx | git apply --check -R -`
- 结果：退出码 `0`

### 真实命令链验证

- 通过：
  - `cd frontend && npm run build`
  - Browser 读取章节页按钮文本
  - Browser 点击 `工作流修改当前分卷`
- 关键证据：
  - 按钮文本集合包含 `工作流修改当前分卷`
  - 跳转 URL 包含 `workflow/outline?action=update`
  - 跳转 URL 包含 `id=outline_f8bd96b0`

### 缺陷记录区

- `BUG-CHAPTER-OUTLINE-WORKFLOW-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-18 23:24 CST`
  - 发现测试项：章节页工具栏工作流入口检查
  - 复现命令或复现入口：Browser 打开 `/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
  - 复现条件：查看章节页顶部工具栏
  - 失败现象：只有 `直接新增分卷大纲`，缺少任何分卷大纲 workflow 入口
  - 失败位置：`frontend/src/pages/NovelChapterManagement.tsx`
  - 当前影响范围：用户必须先跳去分卷页才能走 outline workflow
  - 是否影响交付判断：是
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级验证：通过
- 回滚验证：通过
- 真实命令链验证：通过

### 最近一次执行时间

- `2026-06-18 23:29:00 CST`

## 2026-06-18 章节页纠正为分卷章节大纲创建工作流

### 本次测试范围

- `frontend/src/pages/NovelChapterManagement.tsx`
- 章节页顶部按钮语义与目标路由
- `/workflow/chapter?action=create...` 真实跳转

### 本次执行时间

- `2026-06-18 23:32:57 CST`

### 执行环境

- 工作目录：`/Users/harry/Documents/git/novel_agent`
- 前端：`http://127.0.0.1:5174`
- 浏览器验证：Codex in-app Browser

### 执行命令或测试入口

1. `cd frontend && npm run build`
2. `git diff -- frontend/src/pages/NovelChapterManagement.tsx | git apply --check -R -`
3. Browser 打开 `/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
4. Browser 点击 `工作流新增分卷章节大纲`

### 正常情况结果

- 测了什么：章节页是否提供“创建分卷章节大纲工作流”而不是错误的分卷大纲 workflow
- 怎么测：Browser 读取章节页按钮文本，再点击 `工作流新增分卷章节大纲`
- 看到什么结果：
  - 顶部按钮集合为 `返回分卷大纲页 / 刷新 / 直接新增分卷大纲 / 检查直接内容 / 工作流新增分卷章节大纲 / 直接新增分卷章节大纲`
  - 点击后进入 `/workflow/chapter?action=create&world_id=world_diag_8e262840&novel_id=novel_3f72502f&outline_id=outline_f8bd96b0&worldview_id=wv_world_diag_8e262840`
  - 页面保持 chapter workflow，而不是 outline workflow
- 结果是否通过：通过

### 异常情况结果

- 故障或错误输入是什么：章节页不应再暴露把章节页内容误导到分卷大纲 workflow 的按钮
- 系统反应是什么：顶部不再出现 `工作流修改当前分卷` / `工作流新增分卷大纲` 这种误导性 workflow 按钮
- 是否正确拦截或降级：是，章节页现在只保留 chapter-outline 相关 workflow

### 特殊情况结果

- 边界条件是什么：该页同时还保留“直接新增分卷大纲”，因为创建父级分卷仍然是合法需求
- 与正常路径有什么不同：它是 direct-create，不是 workflow create；与本次新增的 chapter workflow 入口并列存在
- 是否引出额外问题：没有新增编译错误；`vite build` 通过

### 降级验证

- 若用户只需要先补父级分卷，仍可使用 `直接新增分卷大纲`
- 若用户要新增当前页展示的分卷章节大纲，使用 `工作流新增分卷章节大纲`，不会再误入 outline workflow

### 回滚验证

- 已执行 `git diff -- frontend/src/pages/NovelChapterManagement.tsx | git apply --check -R -`
- 结果：退出码 `0`

### 真实命令链验证

- 通过：
  - `cd frontend && npm run build`
  - Browser 读取章节页按钮文本
  - Browser 点击 `工作流新增分卷章节大纲`
- 关键证据：
  - 页面按钮文本集合包含 `工作流新增分卷章节大纲`
  - 不再包含误导性的分卷大纲 workflow 按钮
  - 跳转 URL 为 `workflow/chapter?action=create`
  - 跳转 URL 仍携带 `outline_id=outline_f8bd96b0`

### 缺陷记录区

- `BUG-CHAPTER-WORKFLOW-LABEL-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-18 23:31 CST`
  - 发现测试项：章节页 workflow 入口 Browser 复核
  - 复现命令或复现入口：Browser 打开 `/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
  - 复现条件：查看章节页顶部 workflow 按钮
  - 失败现象：页面出现误导性的分卷大纲 workflow 入口，同时真正的 chapter workflow 入口文案仅为 `工作流新增章节`
  - 失败位置：`frontend/src/pages/NovelChapterManagement.tsx`
  - 当前影响范围：用户会把“分卷章节大纲页”误认为跳去修改分卷大纲正文
  - 是否影响交付判断：是
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级验证：通过
- 回滚验证：通过
- 真实命令链验证：通过

### 最近一次执行时间

- `2026-06-18 23:32:57 CST`

## 2026-06-18 删除章节页直接新增分卷大纲入口

### 本次测试范围

- `frontend/src/pages/NovelChapterManagement.tsx`
- 章节页 `/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0` 顶部工具栏

### 本次执行时间

- `2026-06-18 23:38:01 CST`

### 执行环境

- 工作目录：`/Users/harry/Documents/git/novel_agent`
- 前端：`http://127.0.0.1:5174`
- 浏览器验证：Codex in-app Browser

### 执行命令或测试入口

1. `cd frontend && npm run build`
2. `git diff -- frontend/src/pages/NovelChapterManagement.tsx | git apply --check -R -`
3. Browser 打开 `/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
4. Browser 读取顶部按钮文本
5. Browser 点击 `工作流新增分卷章节大纲`

### 正常情况结果

- 测了什么：章节页是否删除 `直接新增分卷大纲`，同时保留章节大纲相关入口
- 怎么测：Browser 打开章节页后读取所有按钮文本
- 看到什么结果：
  - 按钮集合为 `返回分卷大纲页 / 刷新 / 检查直接内容 / 工作流新增分卷章节大纲 / 直接新增分卷章节大纲`
  - `直接新增分卷大纲` 已不存在
  - `工作流新增分卷章节大纲` 点击后仍跳到 `/workflow/chapter?action=create...&outline_id=outline_f8bd96b0`
- 结果是否通过：通过

### 异常情况结果

- 故障或错误输入是什么：章节页不应再保留会创建父级分卷的快捷入口
- 系统反应是什么：顶部不再出现 `直接新增分卷大纲`
- 是否正确拦截或降级：是，用户若要新增父级分卷，必须回到分卷大纲页

### 特殊情况结果

- 边界条件是什么：删除父级分卷入口后，章节页仍必须保留 chapter workflow create 和 direct create
- 与正常路径有什么不同：不是只验证“按钮少了”，还要证明章节工作流入口没有被误删
- 是否引出额外问题：没有；`vite build` 通过，真实 chapter workflow 仍能打开

### 降级验证

- 当前页不再承载分卷大纲 direct create；父级分卷创建入口收敛回分卷大纲页

### 回滚验证

- 已执行 `git diff -- frontend/src/pages/NovelChapterManagement.tsx | git apply --check -R -`
- 结果：退出码 `0`

### 真实命令链验证

- 通过：
  - `cd frontend && npm run build`
  - Browser 读取章节页按钮文本
  - Browser 点击 `工作流新增分卷章节大纲`
- 关键证据：
  - 按钮文本集合不再包含 `直接新增分卷大纲`
  - 仍包含 `工作流新增分卷章节大纲`
  - 点击后 URL 为 `/workflow/chapter?action=create...`

### 缺陷记录区

- `BUG-CHAPTER-PAGE-OUTLINE-DIRECT-CREATE-001`
  - 缺陷等级：`P2`
  - 发现时间：`2026-06-18 23:36 CST`
  - 发现测试项：章节页工具栏入口 Browser 复核
  - 复现命令或复现入口：Browser 打开 `/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
  - 复现条件：查看顶部工具栏
  - 失败现象：页面仍暴露 `直接新增分卷大纲`，与“章节页只管理分卷章节大纲”的口径冲突
  - 失败位置：`frontend/src/pages/NovelChapterManagement.tsx`
  - 当前影响范围：用户可能在章节页误创建父级分卷
  - 是否影响交付判断：否
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级验证：通过
- 回滚验证：通过
- 真实命令链验证：通过

### 最近一次执行时间

- `2026-06-18 23:38:01 CST`

## 2026-06-18 章节内容页与四个操作按钮

### 本次测试范围

- `frontend/src/App.tsx`
- `frontend/src/api/client.ts`
- `frontend/src/pages/HierarchyWorkflow.tsx`
- `frontend/src/pages/NovelChapterManagement.tsx`
- `frontend/src/pages/NovelChapterContentManagement.tsx`
- `src/agents/chapter_agent.py`
- `src/app_api.py`
- 章节大纲页到章节内容页的真实跳转
- 章节内容页的直接新增 / 工作流新增 / 直接修改 / 工作流修改四个按钮
- `chapter_outline_id` 根记录与子记录拆分查询

### 本次执行时间

- `2026-06-18 23:52:42 CST`

### 执行环境

- 工作目录：`/Users/harry/Documents/git/novel_agent`
- 前端：`http://127.0.0.1:5174`
- 后端：`http://127.0.0.1:5006`
- 后端启动命令：`env PYTHONPATH=.:src .venv/bin/python src/app_api.py`
- 浏览器验证：Playwright（Codex `node_repl`）

### 执行命令或测试入口

1. `python3 -m py_compile src/app_api.py src/agents/chapter_agent.py`
2. `cd frontend && npm run build`
3. `env PYTHONPATH=.:src .venv/bin/python src/app_api.py`
4. Browser 打开 `/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
5. Browser 点击行内 `查看章节内容`
6. Browser 在 `/novels/novel_3f72502f/chapter-contents?outline_id=outline_f8bd96b0&chapter_outline_id=prose_mqjnwlcke90u4d` 依次执行：
   - `直接添加章节内容`
   - `直接修改章节内容`
   - `工作流添加章节内容`
   - `工作流修改章节内容`
   - `删除`
7. `python3 - <<'PY' ... chapter_outline_filter_split ... PY`
8. `git diff -- frontend/src/App.tsx frontend/src/api/client.ts frontend/src/pages/HierarchyWorkflow.tsx frontend/src/pages/NovelChapterManagement.tsx frontend/src/pages/NovelChapterContentManagement.tsx src/agents/chapter_agent.py src/app_api.py 测试计划文件.md | git apply --check -R -`

### 正常情况结果

- 测了什么：章节大纲表格行内是否新增 `查看章节内容`，以及章节内容页四个主按钮是否真实可用
- 怎么测：
  - Browser 打开 `/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
  - 点击行内 `查看章节内容`
  - 在章节内容页直接新增一条临时正文，再直接修改，再分别点击两个 workflow 按钮验证跳转参数，最后删除清理
- 看到什么结果：
  - 章节大纲页按钮集合包含 `查看章节内容`
  - 点击后进入 `/novels/novel_3f72502f/chapter-contents?outline_id=outline_f8bd96b0&chapter_outline_id=prose_mqjnwlcke90u4d`
  - 章节内容页按钮集合为 `直接添加章节内容 / 工作流添加章节内容 / 直接修改章节内容 / 工作流修改章节内容`
  - 直接新增后出现 `章节内容已直接创建，并完成真实接口回查`
  - 列表新增 `chapter_content_mqjogb97zpfpmy / 第一章正文：页面直录`
  - 直接修改后出现 `章节内容已直接修改，并完成真实接口回查`
  - 列表更新为 `第一章正文：页面已直改`
  - `工作流添加章节内容` 跳到 `/workflow/chapter?action=create...&chapter_outline_id=prose_mqjnwlcke90u4d`
  - `工作流修改章节内容` 跳到 `/workflow/chapter?action=update...&chapter_outline_id=prose_mqjnwlcke90u4d&id=chapter_content_mqjogb97zpfpmy`
  - 删除后出现 `章节内容已删除，并完成真实接口回查`，列表恢复 `当前章节大纲下暂无章节内容。`
- 结果是否通过：通过

### 异常情况结果

- 故障或错误输入是什么：当前章节大纲下没有任何章节内容记录时，用户仍可能点击两个“修改章节内容”按钮
- 系统反应是什么：
  - 页面显示 `当前章节大纲下暂无章节内容。`
  - `直接修改章节内容` 为 disabled
  - `工作流修改章节内容` 为 disabled
- 是否正确拦截或降级：是，没有选中内容时不允许误改、不伪造跳转

### 特殊情况结果

- 边界条件是什么：同一个 `prose` 集合里同时存在“章节大纲根记录”和“章节内容子记录”
- 与正常路径有什么不同：不仅验证页面按钮，还验证后端查询必须把两类记录拆开，不能在章节大纲页泄漏子记录，也不能在章节内容页混入根记录
- 怎么测：执行 `python3 - <<'PY' ... chapter_outline_filter_split ... PY`，先直写一个根记录 `tmp_outline_child_root_20260618` 和一个子记录 `tmp_outline_child_content_20260618`，再分别调用
  - `GET /api/lore/list?...&chapter_outline_mode=root`
  - `GET /api/lore/list?...&chapter_outline_id=tmp_outline_child_root_20260618`
- 看到什么结果：
  - root 查询命中根记录，不包含子记录
  - child 查询命中子记录，不包含根记录
  - 脚本输出 `OK: chapter_outline_filter_split`
- 是否引出额外问题：没有；临时测试数据已在脚本 `finally` 中真实删除

### 降级验证

- 章节内容页在空列表时明确显示 `当前章节大纲下暂无章节内容。`
- 没有选中内容时，两类“修改章节内容”按钮自动禁用
- 如果用户只需要查看父级章节大纲，页面仍保留“父级章节大纲正文”只读展示，不会强制写库

### 回滚验证

- 已执行 `git diff -- frontend/src/App.tsx frontend/src/api/client.ts frontend/src/pages/HierarchyWorkflow.tsx frontend/src/pages/NovelChapterManagement.tsx frontend/src/pages/NovelChapterContentManagement.tsx src/agents/chapter_agent.py src/app_api.py 测试计划文件.md | git apply --check -R -`
- 结果：退出码 `0`

### 真实命令链验证

- 通过：
  - `python3 -m py_compile src/app_api.py src/agents/chapter_agent.py`
  - `cd frontend && npm run build`
  - `env PYTHONPATH=.:src .venv/bin/python src/app_api.py`
  - Browser 打开章节大纲页并点击 `查看章节内容`
  - Browser 在章节内容页执行真实新增、真实修改、真实 workflow 跳转、真实删除
  - `python3 - <<'PY' ... chapter_outline_filter_split ... PY`
- 关键证据：
  - 行内新增 `查看章节内容`
  - 新路由 `/novels/:novelId/chapter-contents`
  - 四个页面主按钮全部存在
  - 新增 / 修改 / 删除均出现“完成真实接口回查”成功文案
  - workflow create / update URL 均携带 `chapter_outline_id`
  - 后端过滤脚本输出 `OK: chapter_outline_filter_split`

### 失败项

- 本轮没有遗留失败项

### 缺陷归因

- `BUG-CHAPTER-CONTENT-PAGE-MISSING-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-18 23:40 CST`
  - 发现测试项：章节大纲表格行内操作 Browser 复核
  - 复现命令或复现入口：Browser 打开 `/novels/novel_3f72502f/chapters?outline_id=outline_f8bd96b0`
  - 复现条件：查看具体章节大纲操作栏
  - 失败现象：没有 `查看章节内容` 入口，也没有章节内容独立页面和四个操作按钮
  - 失败位置：`frontend/src/pages/NovelChapterManagement.tsx`、缺失 `frontend/src/pages/NovelChapterContentManagement.tsx`
  - 当前影响范围：用户无法从具体章节大纲进入章节内容管理，章节大纲与章节内容长期共用同页语义
  - 是否影响交付判断：是
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证

### 是否满足完成口径

- 是。本次改动已具备：
  - 真实页面端到端验证
  - 失败路径 / 空状态降级验证
  - 真实命令链验证
  - 回滚 dry-run 验证
  - 测试结果回填

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级验证：通过
- 回滚验证：通过
- 真实命令链验证：通过

### 最近一次执行时间

- `2026-06-18 23:52:42 CST`

## 2026-06-19 章节内容页新增检查与章节简介总结按钮

### 本次测试范围

- `frontend/src/api/client.ts`
- `frontend/src/pages/HierarchyWorkflow.tsx`
- `frontend/src/pages/NovelChapterContentManagement.tsx`
- 章节内容页顶部按钮区
- `检查章节内容` 按钮
- `生成章节简介与总结` 按钮

### 本次执行时间

- `2026-06-19 00:04:01 CST`

### 执行环境

- 工作目录：`/Users/harry/Documents/git/novel_agent`
- 前端：`http://127.0.0.1:5174`
- 后端：`http://127.0.0.1:5006`
- 后端启动命令：`env PYTHONPATH=.:src .venv/bin/python src/app_api.py`
- 浏览器验证：Playwright（Codex `node_repl`）
- Python 验证：仓库 `.venv`

### 执行命令或测试入口

1. `cd frontend && npm run build`
2. Browser 打开 `/novels/novel_3f72502f/chapter-contents?outline_id=outline_f8bd96b0&chapter_outline_id=prose_mqjnwlcke90u4d`
3. Browser 在空列表状态读取按钮文本与 disabled 状态
4. Browser 直接新增临时章节内容后，点击：
   - `检查章节内容`
   - `生成章节简介与总结`
5. `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... chapter_content_summary_create_end_to_end ... PY`
6. `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... chapter_check_workflow_end_to_end ... PY`
7. `git diff -- frontend/src/api/client.ts frontend/src/pages/HierarchyWorkflow.tsx frontend/src/pages/NovelChapterContentManagement.tsx 测试计划文件.md | git apply --check -R -`

### 正常情况结果

- 测了什么：章节内容页是否新增 `检查章节内容` 与 `生成章节简介与总结` 两个主按钮，并且总结按钮能真实跑到 `downstream_summaries` 写库
- 怎么测：
  - Browser 打开章节内容页，确认顶部按钮集合
  - Browser 新增临时正文 `第一章正文：检查与总结验证`
  - 点击 `检查章节内容`，验证跳到 `/workflow/chapter?action=check...`
  - 点击 `生成章节简介与总结`，验证跳到 `/workflow?type=chapter_content_summary_create&action=create...`
  - 另用真实 API 执行 `chapter_content_summary_create` create -> waiting_human -> approve -> completed -> 查询 `downstream_summaries`
- 看到什么结果：
  - 按钮集合包含 `检查章节内容`、`生成章节简介与总结`
  - 有内容时两个按钮都由 disabled 变为 enabled
  - `检查章节内容` URL 带有 `action=check`、`chapter_outline_id=prose_mqjnwlcke90u4d`、`chapter_outline=`、`content=`
  - `生成章节简介与总结` URL 带有 `type=chapter_content_summary_create`、`action=create`、`target_id=chapter_content_...`
  - 真实 API summary 流程输出 `OK: chapter_content_summary_create_end_to_end`
  - `GET /api/downstream-summaries/list?summary_scope=chapter_content&target_id=chapter_content_summary_api_20260618...` 可读回 `summary_key=chapter_content_summary_create_agent:chapter_content_summary_api_20260618`
- 结果是否通过：通过

### 异常情况结果

- 故障或错误输入是什么：章节内容列表为空、没有当前选中章节内容时，用户点击两个新按钮
- 系统反应是什么：
  - `检查章节内容` 为 disabled
  - `生成章节简介与总结` 为 disabled
  - 页面仍显示 `当前章节大纲下暂无章节内容。`
- 是否正确拦截或降级：是，没有当前内容时不会伪造工作流启动

### 特殊情况结果

- 边界条件是什么：`生成章节简介与总结` 不只是打开页面，而是需要把 `target_id/chapter_id/chapter_outline_id` 一起传进 summary workflow
- 与正常路径有什么不同：不仅看按钮是否存在，还验证 `HierarchyWorkflow` 在 summary create 场景下会把 `target_id` 和 `chapter_id` 带进真实 payload
- 怎么测：
  - Browser 点击 `生成章节简介与总结` 后检查 URL
  - API 直接调用 `/api/hierarchy-agent/start` with `agent_type=chapter_content_summary_create`
  - 审批后查询 `/api/downstream-summaries/list`
- 看到什么结果：
  - summary workflow URL 含 `target_id=chapter_content_...`
  - 真实 API 可以走到 `waiting_human -> approve -> completed`
  - `downstream_summaries` 真正写入，再由清理脚本删除
- 是否引出额外问题：没有；临时正文与临时 summary 均已清理

### 降级验证

- 当章节内容为空时，两个新按钮自动禁用
- 当只需要先检查路由参数时，按钮仍可正确跳到对应 workflow 页面，不会跳错到章节大纲 workflow

### 回滚验证

- 已执行 `git diff -- frontend/src/api/client.ts frontend/src/pages/HierarchyWorkflow.tsx frontend/src/pages/NovelChapterContentManagement.tsx 测试计划文件.md | git apply --check -R -`
- 结果：退出码 `0`

### 真实命令链验证

- 通过：
  - `cd frontend && npm run build`
  - Browser 空列表状态读取两个新按钮并检查 disabled
  - Browser 新增临时正文后，点击两个新按钮并检查真实跳转 URL
  - `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... chapter_content_summary_create_end_to_end ... PY`
- 关键证据：
  - 页面按钮文本包含 `检查章节内容` 与 `生成章节简介与总结`
  - 章节检查按钮带 `action=check`
  - 总结按钮带 `type=chapter_content_summary_create`
  - summary API 端到端输出 `OK: chapter_content_summary_create_end_to_end`

### 失败项

- `CHECK-WORKFLOW-LATENCY-001`
  - 失败现象：直接 API 调用 `chapter?action=check` 的 live check workflow 在 90 秒内仍未返回，最终人工中断请求
  - 失败位置：`POST /api/hierarchy-agent/start` with `agent_type=chapter` + `action=check`
  - 影响范围：不影响本次按钮挂载与路由参数正确性，但影响“检查章节内容”底层 workflow 的完整耗时证据
  - 复现方式：`env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... chapter_check_workflow_end_to_end ... PY`
  - 当前判断：环境缺失 / live LLM 响应耗时异常
  - 是否保留失败结论：是

### 缺陷归因

- `BUG-CHAPTER-CONTENT-ACTIONS-MISSING-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-18 23:56 CST`
  - 发现测试项：章节内容页按钮 Browser 复核
  - 复现命令或复现入口：Browser 打开 `/novels/novel_3f72502f/chapter-contents?outline_id=outline_f8bd96b0&chapter_outline_id=prose_mqjnwlcke90u4d`
  - 复现条件：查看章节内容页顶部工具栏
  - 失败现象：页面缺少 `检查章节内容` 与 `生成章节简介与总结` 两个功能按钮
  - 失败位置：`frontend/src/pages/NovelChapterContentManagement.tsx`
  - 当前影响范围：用户不能从章节内容页直接触发章节检查和章节内容总结流程
  - 是否影响交付判断：是
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证

### 是否满足完成口径

- 尚未达到完全交付标准。
- 原因：
  - `生成章节简介与总结` 已具备真实 UI 跳转验证和真实 API 端到端写库读回验证。
  - `检查章节内容` 已具备真实 UI 跳转验证，但其直接 API live check workflow 在本次环境下 90 秒内未返回，被记录为环境/LLM 耗时异常，未形成完整完成性证据。

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级验证：通过
- 回滚验证：通过
- 真实命令链验证：部分通过

### 最近一次执行时间

- `2026-06-19 00:04:01 CST`

---

## 2026-06-19 章节检查工作流超时修复补充

### 本次测试范围

- `src/agents/review_agent.py` 章节检查 review prompt 按节点裁剪上下文，避免把世界/小说/分卷/章节/前文/RAG 设定一次性塞给所有 review 节点。
- `src/common/llm_factory.py` review agent 默认超时策略与 provider/base_url 诊断信息回填。
- `config/llm.yml` review agent 超时配置入口。
- `tests/test_review_agent_prompt_scoping.py` prompt 范围与错误分类断言。

### 本次执行时间

- `2026-06-19 00:41:33 CST - 2026-06-19 00:52:35 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- 后端地址：`http://127.0.0.1:5006`
- LLM Provider：`ollama / gemma4:e2b`
- Ollama 地址：`http://127.0.0.1:11434/v1`
- Python：仓库 `.venv/bin/python`

### 执行命令或测试入口

1. `env PYTHONPATH=.:src .venv/bin/python -m py_compile src/agents/review_agent.py src/common/llm_factory.py tests/test_review_agent_prompt_scoping.py`
2. `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... test_chapter_world_review_prompt_only_keeps_world_context() / test_chapter_plot_review_prompt_keeps_outline_and_previous_chapter_context() / test_review_exception_classification_and_timeout_defaults() ... PY`
3. `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... build_review_messages(...) for chapter_world_rules/chapter_novel_rules/chapter_worldview_rules/chapter_outline_rules/chapter_chapter_outline_rules/chapter_plot_errors ... PY`
4. `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... execute_llm_review_detail(db, 'chapter_world_rules', payload) ... PY`
5. `kill 22171 && sleep 2 && env PYTHONPATH=.:src .venv/bin/python src/app_api.py > /tmp/novel_agent_backend_5006.log 2>&1`
6. `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... test_real_chapter_check_workflow_requests() ... PY`
7. `ollama serve > /tmp/ollama_serve.log 2>&1`
8. `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... test_real_chapter_check_workflow_requests() ... PY`
9. `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... test_real_chapter_check_workflow_requires_chapter_outline() ... PY`
10. `git diff -- src/agents/review_agent.py src/common/llm_factory.py config/llm.yml tests/test_review_agent_prompt_scoping.py 测试计划文件.md | git apply --check -R -`

### 正常情况结果

- 测了什么：修复后的单节点 `chapter_world_rules` 是否不再携带无关上下文，并能在真实 Ollama 下返回结果。
- 怎么测：
  - 先直接调用 `build_review_messages(...)` 统计 prompt 字符数
  - 再直接调用 `execute_llm_review_detail(db, 'chapter_world_rules', payload)`
  - 最后执行真实 `test_real_chapter_check_workflow_requests()`
- 看到什么结果：
  - `chapter_world_rules` prompt 从修复前约 `6873` 字符降到修复后 `1036` 字符
  - 六个章节 review 节点 prompt 字符数分别为：
    - `chapter_world_rules=1123`
    - `chapter_novel_rules=1953`
    - `chapter_worldview_rules=1091`
    - `chapter_outline_rules=3486`
    - `chapter_chapter_outline_rules=1227`
    - `chapter_plot_errors=3737`
  - 单节点真实调用输出：`elapsed_sec=26.72`、`llm_invoked=True`、`passed=True`
  - 重启后端并重启 Ollama 后，真实 API 端到端输出：`OK: real_chapter_check_workflow_requests_retry`
- 结果是否通过：通过

### 异常情况结果

- 故障或错误输入是什么：Ollama 在章节检查链路中途掉线，`11434` 监听消失。
- 系统反应是什么：
  - `chapter_outline_review` / `chapter_plot_review` 节点不再给出模糊超时
  - 节点输出明确返回 `errors=['大模型连接失败：Connection error.']`
  - `llm_call` 中保留 `prompt`、`prompt_chars`、`provider=ollama`、`base_url=http://127.0.0.1:11434/v1`、`error_kind=connection_error`
- 是否正确拦截或降级：是，workflow 没有失控，失败节点被明确标记为 `failed`，并保留真实错误归因

### 特殊情况结果

- 边界条件是什么：章节检查 workflow 需要连续调用 6 个 review 节点，既要验证首节点不再超时，也要验证中后段节点提示词不会再次膨胀。
- 与正常路径有什么不同：不仅验证单节点恢复，还验证整条 `/api/hierarchy-agent/start` 的 `chapter?action=check` 链路。
- 怎么测：
  - 第一次直接执行真实 `test_real_chapter_check_workflow_requests()`，记录中途失败节点
  - 检查 `11434` 监听发现 Ollama 已掉线
  - 重新执行 `ollama serve`
  - 第二次重跑同一真实测试
- 看到什么结果：
  - 第一次真实测试在 `chapter_outline_review` 之后出现 `connection_error`
  - 第二次在重启 Ollama 后整条链路通过，输出 `OK: real_chapter_check_workflow_requests_retry`
- 是否引出额外问题：有，发现本机 Ollama 服务存在间歇性退出风险；当前归因为环境不稳定，不是本次代码逻辑回退

### 降级结果

- 当 Ollama 不可用时，节点输出会显式保留 `connection_error`、真实 prompt 与 base_url，而不是只返回空白失败。
- 当缺少章节大纲时，真实接口 `test_real_chapter_check_workflow_requires_chapter_outline()` 返回 `OK: real_chapter_check_workflow_requires_chapter_outline`，内部断言 `HTTP 400` 且错误文本包含“章节大纲”；该入口用于证明 workflow 前置校验未被本次修复破坏。

### 回滚结果

- 已执行：`git diff -- src/agents/review_agent.py src/common/llm_factory.py config/llm.yml tests/test_review_agent_prompt_scoping.py 测试计划文件.md | git apply --check -R -`
- 结果：退出码 `0`；存在 whitespace warning，但不阻断 reverse-apply dry-run

### 真实命令链验证结果

- 通过：
  - `py_compile` 语法校验通过
  - 直接 Python 断言输出 `OK: review_agent_prompt_scoping`
  - 单节点真实 LLM 调用恢复成功，耗时约 `26.72s`
  - 重启后端 + 重启 Ollama 后，真实 API 端到端输出 `OK: real_chapter_check_workflow_requests_retry`
- 部分失败：
  - 第一次真实 API 测试因 Ollama 服务掉线出现 `connection_error`

### 失败项

- `CHECK-WORKFLOW-OLLAMA-DROP-002`
  - 失败现象：第一次真实 `chapter_check` API 链路跑到中后段 review 节点时，Ollama 服务掉线，`11434` 不再监听
  - 失败位置：`chapter_outline_review` 首次复测；另一次出现在 `chapter_plot_review`
  - 影响范围：影响单次完整链路稳定性，但不再表现为旧的 prompt 超时问题
  - 复现方式：执行 `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... test_real_chapter_check_workflow_requests() ... PY`，同时观察 `lsof -nP -iTCP:11434 -sTCP:LISTEN`
  - 当前判断：环境缺失 / 环境不稳定
  - 是否保留失败结论：是

### 缺陷归因

- `BUG-CHAPTER-CHECK-PROMPT-BLOAT-002`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-18 22:57 CST`
  - 发现测试项：直接内容检查 workflow live log 与单节点复现
  - 复现命令或复现入口：`POST /api/outlines/create` 后触发章节检查链路；或 `execute_llm_review_detail(db, 'chapter_world_rules', payload)`
  - 复现条件：章节 review 节点把世界/小说/大纲/章节/前文/世界观参考一次性拼进同一个 prompt
  - 失败现象：`chapter_world_rules` 在修复前出现 `Request timed out.`
  - 失败位置：`src/agents/review_agent.py`
  - 当前影响范围：章节检查 workflow 首节点易超时，后续节点难以稳定完成
  - 是否影响交付判断：是
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证

### 是否满足完成口径

- 本次“章节检查 workflow prompt 超时修复”已完成实现与真实验证闭环，但不能宣称本机 Ollama 环境已稳定。
- 依据：
  - 正常情况：已拿到单节点真实成功和整条真实 API 成功
  - 异常情况：已拿到 Ollama 掉线后的显式失败与错误归因
  - 特殊情况：已验证 6 个 review 节点的 prompt 被按职责裁剪
- 保留风险：
  - Ollama 进程存在间歇性退出风险，属于环境问题，未随本次代码修改一并解决

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：通过
- 真实命令链验证结果：通过

### 最近一次执行时间

- `2026-06-19 00:52:35 CST`

---

## 2026-06-19 提交前校验补充

### 本次测试范围

- `src/app_api.py`、总结工作流 6 路新增 agent、`review_agent`、`chapter_check_agent` 的提交前语法校验。
- `tests/test_summary_workflow_requests.py` 的模块级回归。
- `tests/test_review_agent_prompt_scoping.py` 的 prompt 范围与超时配置回归。
- `tests/test_worldview_library_rules_requests.py` 的真实请求链路。
- `tests/test_real_chapter_check_workflow_requests.py` 的异常路径，以及完整 live 链路的提交前复跑尝试。
- `frontend` 的生产构建。
- 当前整批 diff 的 reverse-apply dry-run。

### 本次执行时间

- `2026-06-19 00:56:00 CST - 2026-06-19 01:04:39 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：仓库 `.venv/bin/python`
- 前端构建：`npm run build`
- 后端地址：`http://127.0.0.1:5006`
- LLM Provider：`ollama / gemma4:e2b`
- Ollama 地址：`http://127.0.0.1:11434`

### 执行命令或测试入口

1. `python3 -m pytest -q tests/test_summary_workflow_requests.py tests/test_review_agent_prompt_scoping.py`
2. `.venv/bin/python -m pytest -q tests/test_summary_workflow_requests.py tests/test_review_agent_prompt_scoping.py`
3. `env PYTHONPATH=.:src .venv/bin/python -m py_compile src/app_api.py src/common/llm_factory.py src/agents/review_agent.py src/agents/chapter_check_agent.py src/agents/outline_summary_create_agent.py src/agents/outline_summary_update_agent.py src/agents/chapter_outline_summary_create_agent.py src/agents/chapter_outline_summary_update_agent.py src/agents/chapter_content_summary_create_agent.py src/agents/chapter_content_summary_update_agent.py tests/test_summary_workflow_requests.py tests/test_review_agent_prompt_scoping.py tests/test_worldview_library_rules_requests.py tests/test_real_chapter_check_workflow_requests.py`
4. `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... test_summary_workflow_requests::* ... PY`
5. `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... test_review_agent_prompt_scoping::* ... PY`
6. `cd /Users/harry/Documents/git/novel_agent/frontend && npm run build`
7. `env PYTHONPATH=.:src API_BASE_URL=http://127.0.0.1:5006 .venv/bin/python - <<'PY' ... test_worldview_library_rules_requests::* ... PY`
8. `env PYTHONPATH=.:src API_BASE_URL=http://127.0.0.1:5006 .venv/bin/python - <<'PY' ... test_real_chapter_check_workflow_requires_chapter_outline() ... PY`
9. `env PYTHONPATH=.:src API_BASE_URL=http://127.0.0.1:5006 REQUEST_TIMEOUT=240 .venv/bin/python - <<'PY' ... test_real_chapter_check_workflow_requests() ... PY`
10. `git diff -- . ':(exclude).playwright-cli' | git apply --check -R -`
11. `which ollama && ollama serve > /tmp/ollama_serve_codex.log 2>&1 & sleep 2 && lsof -nP -iTCP:11434 -sTCP:LISTEN`

### 正常情况结果

- 测了什么：总结工作流 6 路新增模块、review prompt 范围、worldview 唯一库自动创建/禁用 create/delete 级联/自修复、前端构建。
- 怎么测：
  - 先执行 `py_compile`
  - 再用仓库文档里已有的内联 Python 入口直接调用 `tests/test_summary_workflow_requests.py` 与 `tests/test_review_agent_prompt_scoping.py`
  - 再执行真实 HTTP 的 `test_worldview_library_rules_requests::*`
  - 最后执行 `npm run build`
- 看到什么结果：
  - `OK: summary_dedicated_llm_names`
  - `OK: summary_review_blocks_oversimplified`
  - `OK: summary_prompts_are_separate`
  - `OK: app_api_registers_summary_types`
  - `OK: app_api_outline_summary_run_until_human`
  - `OK: review_agent_prompt_scoping`
  - `OK: worldview_library_auto_provision`
  - `OK: worldview_library_create_disabled`
  - `OK: worldview_library_delete_cascade`
  - `OK: worldview_library_self_heal`
  - `vite build ... ✓ built in 41.86s`
- 结果是否通过：通过

### 异常情况结果

- 故障或错误输入是什么：
  - 使用系统 `python3 -m pytest` 跑回归
  - 使用 `.venv/bin/python -m pytest` 跑回归
  - 真实章节检查 workflow 缺少 `chapter_outline`
- 系统反应是什么：
  - 系统 `python3` 在收集期报 `ModuleNotFoundError: langgraph` / `langchain_core`
  - `.venv` 中报 `No module named pytest`
  - `test_real_chapter_check_workflow_requires_chapter_outline()` 返回 `OK: real_chapter_check_workflow_requires_outline`
- 是否正确拦截或降级：
  - pytest 入口缺失时，已改走仓库文档中既有的内联 Python 真实入口
  - 缺少 `chapter_outline` 时，后端按预期返回 `HTTP 400`

### 特殊情况结果

- 边界条件是什么：
  - 提交前工作区改动很大，且包含前后端、agent、真实请求测试与测试文档联动。
  - `chapter_check` 完整 live 链路是长耗时请求，提交前复跑时需要依赖本机 `ollama` 保持稳定在线。
- 与正常路径有什么不同：
  - 本轮除了常规 build / py_compile，还执行了 `git apply --check -R -` 回滚 dry-run。
  - 完整 `test_real_chapter_check_workflow_requests()` 在本轮复跑中一直阻塞等待响应，未在本次提交前窗口内自然收口。
- 是否引出额外问题：
  - `git apply --check -R -` 通过，但提示当前 diff 自带 trailing whitespace warning。
  - `ollama` 端口在中途需要显式确认监听恢复后才继续验证。

### 降级结果

- 当 `pytest` 入口不可用时，提交前校验改为仓库文档已定义的 `.venv/bin/python` 内联执行方式，不把“测试工具没装好”误判成产品缺陷。
- 对 `chapter_check` 完整 live 链路，本轮只把 `requires_outline` 异常路径记为通过；长耗时完整链路未收口，不计入通过结论。

### 回滚结果

- 已执行：`git diff -- . ':(exclude).playwright-cli' | git apply --check -R -`
- 结果：退出码 `0`；存在 whitespace warning，但 reverse-apply dry-run 成功

### 真实命令链验证结果

- 通过：
  - `py_compile` 语法校验通过
  - 总结工作流内联回归通过
  - review prompt 范围内联回归通过
  - worldview 唯一库真实请求链路通过
  - `test_real_chapter_check_workflow_requires_chapter_outline()` 通过
  - `npm run build` 通过
  - reverse-apply dry-run 通过
- 未通过或未收口：
  - 系统 `python3 -m pytest` 因依赖缺失未通过
  - `.venv/bin/python -m pytest` 因缺少 `pytest` 未通过
  - `test_real_chapter_check_workflow_requests()` 本轮未自然收口，人工中断，不能计为通过

### 失败项

- `PREPUSH-PYTEST-ENTRY-001`
  - 失败现象：系统 `python3 -m pytest` 收集期缺少 `langgraph` / `langchain_core`，`.venv/bin/python -m pytest` 缺少 `pytest`
  - 失败位置：提交前测试入口
  - 影响范围：影响“直接复用 pytest 命令”这一路入口，不影响仓库文档中已有的 `.venv/bin/python` 内联回归入口
  - 复现方式：执行本节第 1、2 条命令
  - 当前判断：环境缺失 / 测试入口不一致
  - 是否保留失败结论：是
- `PREPUSH-CHAPTER-CHECK-LIVE-001`
  - 失败现象：`test_real_chapter_check_workflow_requests()` 本轮长时间等待响应，最终人工中断
  - 失败位置：真实 `POST /api/hierarchy-agent/start` 链路等待阶段
  - 影响范围：本轮无法把章节检查完整 live 链路记为“重新验证通过”
  - 复现方式：执行本节第 9 条命令
  - 当前判断：环境不稳定 / 长耗时链路本轮未收口
  - 是否保留失败结论：是

### 缺陷归因

- `PREPUSH-PYTEST-ENTRY-001`
  - 缺陷等级：`P2`
  - 发现时间：`2026-06-19 00:56 CST`
  - 发现测试项：提交前 pytest 入口验证
  - 复现命令或复现入口：本节第 1、2 条命令
  - 复现条件：直接使用系统 Python 或当前 `.venv` 的 `pytest` 入口
  - 失败现象：缺少依赖或缺少 `pytest`
  - 失败位置：测试执行环境
  - 当前影响范围：阻断直接 `pytest` 复跑，但不阻断 `.venv/bin/python` 内联入口
  - 是否影响交付判断：是
  - 当前判断：环境缺失
  - 当前状态：暂不处理
- `PREPUSH-CHAPTER-CHECK-LIVE-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-19 01:03 CST`
  - 发现测试项：章节检查完整 live 链路提交前复跑
  - 复现命令或复现入口：本节第 9 条命令
  - 复现条件：本机 `5006 + 11434` 环境在线，执行完整章节检查真实请求
  - 失败现象：请求长时间无返回，本轮人工中断
  - 失败位置：真实工作流响应等待阶段
  - 当前影响范围：本轮不能据此宣称章节检查完整链路已再次验证通过
  - 是否影响交付判断：是
  - 当前判断：环境不稳定 / 需继续观察
  - 当前状态：未修复

### 是否满足完成口径

- 本轮提交前校验尚未达到仓库硬口径下的“测试完成”。
- 依据：
  - 正常情况：总结工作流、review prompt、worldview 唯一库、前端构建均已拿到真实结果
  - 异常情况：`requires_outline` 已验证为显式 `HTTP 400`
  - 特殊情况：回滚 dry-run 已通过
  - 未闭环项：`pytest` 直接入口不可用，且 `chapter_check` 完整 live 链路本轮未重新跑通
- 允许表述：当前只能说明“已完成提交前部分实现验证，未完成测试闭环”，不能把本轮验证写成“已稳定”或“测试通过”

### 执行结果汇总

- 正常情况：通过
- 异常情况：部分通过
- 特殊情况：部分通过
- 降级结果：通过
- 回滚结果：通过
- 真实命令链验证结果：部分通过

### 最近一次执行时间

- `2026-06-19 01:04:39 CST`

## 本次补充测试（2026-06-19 章节简介与总结命名修复）

### 本次测试范围

- `frontend/src/pages/NovelChapterContentManagement.tsx` “生成章节简介与总结”按钮的工作流类型参数。
- `frontend/src/pages/HierarchyWorkflow.tsx` 旧 `chapter_content_summary_*` URL 的前端归一化、标题文案与徽标展示。
- `frontend/src/api/client.ts` hierarchy-agent 前端类型声明。
- `src/app_api.py` 新旧工作流类型兼容、能力标签与审查序列映射。
- `src/agents/chapter_content_summary_create_agent.py` / `src/agents/chapter_content_summary_update_agent.py` 的章节简介与总结输出合同。
- `tests/test_summary_workflow_requests.py` 定向回归覆盖。

### 本次执行时间

- `2026-06-19 01:33:00 CST - 2026-06-19 01:49:30 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- 前端地址：`http://127.0.0.1:5174`
- 后端地址：`http://127.0.0.1:5006`
- 浏览器验证：Codex in-app Browser（只读打开页面，不启动工作流，不写库）

### 执行命令或测试入口

- `env PYTHONPATH=.:src .venv/bin/python -m py_compile src/app_api.py src/agents/chapter_content_summary_create_agent.py src/agents/chapter_content_summary_update_agent.py tests/test_summary_workflow_requests.py`
- `env PYTHONPATH=.:src .venv/bin/python - <<'PY' ... tests/test_summary_workflow_requests.py 全量 test_* 定向执行 ... PY`
- `cd frontend && npm run build`
- 浏览器入口 1：`/workflow?type=chapter_content_summary_create&action=create&target_id=chapter_content_test&world_id=world_test&novel_id=novel_test&outline_id=outline_test&chapter_outline_id=chapter_outline_test&name=测试章节&content=运输舰坠落后林澈先封锁航道&message=生成章节简介与总结`
- 浏览器入口 2：`/novels/novel_3f72502f/chapter-contents?outline_id=outline_f8bd96b0`

### 正常情况结果

- 代码级正常路径：
  - 定向回归通过 `PASS test_summary_workflow_modules_use_dedicated_llm_names`
  - 定向回归通过 `PASS test_app_api_registers_all_summary_workflow_types`
  - 定向回归通过 `PASS test_app_api_normalizes_legacy_chapter_content_summary_types`
  - 说明：新类型 `chapter_intro_summary_create/update` 已注册，旧类型 `chapter_content_summary_create/update` 会被归一化到新类型。
- 页面级正常路径（只读）：
  - 旧 URL `type=chapter_content_summary_create` 打开后，页面标题为 `新增章节简介与总结工作流`
  - 徽标显示 `CHAPTER_INTRO_SUMMARY_CREATE`
  - 页面正文不再出现 `新增章节内容总结工作流`
  - 说明：旧链接兼容已生效，页面命名修复已生效。
- 结果判定：通过

### 异常情况结果

- 只读打开 `/novels/novel_3f72502f/chapter-contents?outline_id=outline_f8bd96b0` 时，页面仍显示：
  - `小说 novel_3f72502f 不存在，可能已被删除。请返回小说列表重新选择。`
- 在该异常数据状态下，按钮区仍显示：
  - `生成章节简介与总结`
  - 不显示旧文案 `生成章节内容总结`
- 结果判定：通过
- 归因：页面数据异常来自目标小说不存在，不是本次命名修复回归。

### 特殊情况结果

- 旧链接兼容验证：
  - 使用旧 `type=chapter_content_summary_create` URL 进入页面，最终展示的新类型标题与新类型徽标均正确。
  - 页面不再出现先前只读验证中抓到的 `新增新增章节简介与总结工作流` 重复前缀。
- 构建与语法验证：
  - `py_compile` 退出码 0
  - `npm run build` 退出码 0
- 结果判定：通过

### 降级结果

- 当页面绑定的小说不存在时，前端仍以显式错误文案降级，不白屏、不跳回旧命名。
- 结果判定：通过

### 回滚结果

- 本轮未执行反向补丁 dry-run。
- 当前判断：未验证，不能把本轮记为“已完成回滚验证”。

### 真实命令链验证结果

- `real`：
  - 浏览器只读打开旧 workflow URL，确认页面标题、徽标和文案都已切换到“章节简介与总结”。
  - 浏览器只读打开章节内容页，确认按钮文案为“生成章节简介与总结”。
- `partial`：
  - 本轮没有启动 live 工作流，也没有执行写库后的 readback。
  - 原因：按用户要求，本轮不对用户内容做新增、删除或清理操作。

### 失败项

- `CHAPTER-INTRO-SUMMARY-LIVE-001`
  - 失败现象：本轮未执行 live `startHierarchyAgent` -> LLM 生成 -> readback 闭环
  - 失败位置：真实写库验证阶段未进入
  - 影响范围：不能宣称本次修复已完成端到端写库闭环验证
  - 复现方式：本轮未执行，属有意跳过
  - 当前判断：用户约束 / 验证范围受限
  - 是否保留失败结论：是

### 缺陷归因

- `CHAPTER-INTRO-SUMMARY-LIVE-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-19 01:49 CST`
  - 发现测试项：章节简介与总结 live 工作流闭环验证
  - 复现命令或复现入口：应为真实 `POST /api/hierarchy-agent/start` + 人工批准后 readback
  - 复现条件：允许执行真实工作流并允许产生 run / 写库副作用
  - 失败现象：本轮未执行，不具备闭环证据
  - 失败位置：验证范围
  - 当前影响范围：阻断“测试完成”与“端到端完成”表述
  - 是否影响交付判断：是
  - 当前判断：用户约束 / 范围受限
  - 当前状态：未验证

### 是否满足完成口径

- 本轮实现修复已完成，但按仓库硬口径，测试闭环仍 `尚未完成`。
- 依据：
  - 正常情况：代码定向回归、浏览器只读命名验证、前端构建、Python 语法检查均通过
  - 异常情况：小说不存在时按钮文案与错误降级都正确
  - 特殊情况：旧链接兼容、新标题去重已验证
  - 未闭环项：未执行 live 工作流启动、人工批准、写库后 readback

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：未验证
- 真实命令链验证结果：部分通过

### 最近一次执行时间

- `2026-06-19 01:49:30 CST`

## 本次补充测试（2026-06-19 默认 LLM Provider 切换为 local）

### 本次测试范围

- `config/llm.yml` 默认 `LLM_PROVIDER` 与 `DEFAULT_MODEL`
- `.env` 中会覆盖文件配置的 `LLM_PROVIDER` 与 `DEFAULT_MODEL`
- `src/common/config_utils.py` 缺省回退默认值
- `src/common/llm_factory.py` provider 与 default model 的代码级 fallback

### 本次执行时间

- `2026-06-19 02:03:00 CST - 2026-06-19 02:12:00 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- 解释器 1：系统 `python3`
- 解释器 2：仓库 `.venv/bin/python3` 已检查，但为坏符号链接，未用于执行

### 执行命令或测试入口

- `env PYTHONPATH=.:src python3 -m py_compile src/common/config_utils.py src/common/llm_factory.py`
- `env PYTHONPATH=.:src python3 - <<'PY' ... from src.common.config_utils import load_config ... PY`
- `nl -ba .env | sed -n '1,6p'`
- `sed -n '1,40p' config/llm.yml`

### 正常情况结果

- 文件配置验证：
  - `config/llm.yml` 已变为 `LLM_PROVIDER: local`
  - `config/llm.yml` 已变为 `DEFAULT_MODEL: gemini-3-flash`
  - `.env` 已变为 `LLM_PROVIDER=local`
  - `.env` 已变为 `DEFAULT_MODEL=gemini-3-flash`
- 运行时读取验证：
  - `load_config()` 返回 `runtime_LLM_PROVIDER=local`
  - `load_config()` 返回 `runtime_DEFAULT_MODEL=gemini-3-flash`
  - `LLM_MODELS.local.default=gemini-3-flash`
- 结果判定：通过

### 异常情况结果

- 解释器环境异常 1：
  - 系统 `python3` 在直接导入 `src.common.llm_factory` 时缺少 `langchain_core`
- 解释器环境异常 2：
  - `.venv/bin/python3` 是坏符号链接，指向缺失的 `/opt/homebrew/opt/python@3.12/bin/python3.12`
- 在上述异常下，`config_utils` 仍可独立完成真实配置读检
- 结果判定：部分通过

### 特殊情况结果

- 覆盖优先级验证：
  - 首轮读检曾得到 `runtime_LLM_PROVIDER=ollama`
  - 进一步定位发现根因是仓库 `.env` 第 2 行覆盖了 YAML 配置
  - 修复 `.env` 后再次读检，运行时已切到 `local`
- 结果判定：通过

### 降级结果

- 当 `llm_factory` 运行时依赖不可用时，本轮仍能退回到 `config_utils` 真读配置边界验证，不把“未验证”包装成“已通过”。
- 结果判定：通过

### 回滚结果

- 本轮未执行反向补丁 dry-run。
- 当前判断：未验证，不能宣称已完成回滚验证。

### 真实命令链验证结果

- `real`：
  - `python3` 调用 `load_config()`，实际读到 `LLM_PROVIDER=local`、`DEFAULT_MODEL=gemini-3-flash`
  - `py_compile` 通过，证明修改后的配置工具链与工厂代码语法有效
- `blocked`：
  - `llm_factory` 深层运行时实例化未完成 live provider 调用验证，受 `langchain_core` 缺失与坏虚拟环境阻断

### 失败项

- `LLM-PROVIDER-RUNTIME-001`
  - 失败现象：无法在当前解释器环境下完成 `llm_factory` 的深层运行时导入验证
  - 失败位置：依赖与解释器环境
  - 影响范围：本轮不能把“provider live 实例化”记为已验证
  - 复现方式：执行 `env PYTHONPATH=.:src python3 - <<'PY' from src.common.llm_factory import get_provider_info PY`
  - 当前判断：环境缺失
  - 是否保留失败结论：是

### 缺陷归因

- `LLM-PROVIDER-RUNTIME-001`
  - 缺陷等级：`P2`
  - 发现时间：`2026-06-19 02:10 CST`
  - 发现测试项：默认 LLM Provider 运行时深层导入验证
  - 复现命令或复现入口：`env PYTHONPATH=.:src python3 - <<'PY' from src.common.llm_factory import get_provider_info PY`
  - 复现条件：当前机器缺少 `langchain_core`，且 `.venv/bin/python3` 坏链接
  - 失败现象：`ModuleNotFoundError: No module named 'langchain_core'`
  - 失败位置：测试执行环境
  - 当前影响范围：阻断 `llm_factory` 深层运行时验证，不阻断配置文件与 `config_utils` 读取验证
  - 是否影响交付判断：是
  - 当前判断：环境缺失
  - 当前状态：暂不处理

### 是否满足完成口径

- 本轮配置修改已完成，但按仓库硬口径，测试闭环 `尚未完成`。
- 依据：
  - 正常情况：文件配置与 `load_config()` 真实读取均已切换到 `local`
  - 异常情况：已明确识别 `.env` 覆盖与解释器环境问题
  - 未闭环项：未完成 `llm_factory` 深层运行时导入与 live provider 实例化验证

### 执行结果汇总

- 正常情况：通过
- 异常情况：部分通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：未验证
- 真实命令链验证结果：部分通过

### 最近一次执行时间

- `2026-06-19 02:12:00 CST`

## 本次补充测试（2026-06-19 make restart 误报 5006 端口占用）

### 本次测试范围

- `Makefile` 的 `SYSTEM_PYTHON` 解析逻辑
- `Makefile` 的 `check-ports` / `ensure-venv` 前置检查
- `.venv` 重建后的 `make restart` 真实命令链

### 本次执行时间

- `2026-06-19 03:05:00 CST - 2026-06-19 03:19:00 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python 3.12 可执行文件：`/Users/harry/.pyenv/versions/3.12.9/bin/python3.12`
- 前端地址：`http://127.0.0.1:5174`
- 后端地址：`http://127.0.0.1:5006`

### 执行命令或测试入口

- `lsof -nP -iTCP:5006 -sTCP:LISTEN`
- `./.venv/bin/python -V`
- `make check-ports PORTS='5006'`
- `make install`
- `make restart`
- `lsof -nP -iTCP:5006 -sTCP:LISTEN`
- `lsof -nP -iTCP:5174 -sTCP:LISTEN`
- `curl -I -s http://127.0.0.1:5174/ | head -n 5`
- `curl -i -s http://127.0.0.1:5006/api/system/health | sed -n '1,20p'`
- `make status`

### 正常情况结果

- 根因复现：
  - `lsof -nP -iTCP:5006 -sTCP:LISTEN` 为空，证明 5006 当时并未被真实占用
  - `./.venv/bin/python -V` 返回 `no such file or directory`
  - `make check-ports PORTS='5006'` 却报 `Port 5006 is already in use`
  - 结论：原问题是坏 `.venv` 被 `check-ports` 误报成端口占用
- 修复后真实重启：
  - `make install` 成功重建 `.venv` 并安装依赖
  - `make restart` 成功启动后端与前端
  - `lsof` 读到 `127.0.0.1:5006` 与 `127.0.0.1:5174` 均在监听
  - `curl -I http://127.0.0.1:5174/` 返回 `HTTP/1.1 200 OK`
- 结果判定：通过

### 异常情况结果

- 修复后的异常路径：
  - 当 `.venv` 缺失或失效时，`make check-ports` 不再误报“端口占用”
  - 现在会显式报：
    - `Virtualenv interpreter is missing or invalid: ./.venv/bin/python`
    - `Run 'make install' to rebuild .venv before starting services.`
- 结果判定：通过

### 特殊情况结果

- 解释器发现逻辑：
  - `python3.12` 命令名在当前 shell 中会命中 pyenv shim，但该 shim 未绑定可执行版本
  - `Makefile` 改为优先使用 pyenv 真实路径 `/Users/harry/.pyenv/versions/3.12.9/bin/python3.12`
- 非阻断旁支：
  - `curl http://127.0.0.1:5006/api/system/health` 返回 `404 NOT FOUND`
  - `rg` 未发现该路由定义
  - 当前判断：这是接口缺失事实，不影响本轮 `make restart` 问题的修复结论
- 结果判定：通过

### 降级结果

- 当虚拟环境坏掉时，启动链路现在会明确降级为“先修复 .venv”，而不是给出错误的端口占用结论。
- 结果判定：通过

### 回滚结果

- 本轮未执行反向补丁 dry-run。
- 当前判断：未验证，不能宣称已完成回滚验证。

### 真实命令链验证结果

- `real`：
  - 真实 `make install` 成功重建 `.venv`
  - 真实 `make restart` 成功启动前后端
  - 真实 `lsof` 读回监听端口
  - 真实 `curl` 读回前端 200
- `partial`：
  - 后端仅验证到 Flask 已监听 5006
  - `/api/system/health` 不存在，未进一步补做其他业务接口探针

### 失败项

- `MAKE-RESTART-HEALTH-001`
  - 失败现象：`/api/system/health` 返回 404
  - 失败位置：后端健康探针入口
  - 影响范围：不能把“后端监听成功”等同于“健康接口已提供”
  - 复现方式：执行 `curl -i -s http://127.0.0.1:5006/api/system/health`
  - 当前判断：产品现状 / 非本轮阻断项
  - 是否保留失败结论：是

### 缺陷归因

- `MAKE-RESTART-ROOTCAUSE-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-19 03:08 CST`
  - 发现测试项：`make restart` 真实失败复现
  - 复现命令或复现入口：`make check-ports PORTS='5006'`
  - 复现条件：`.venv/bin/python` 缺失或坏链接
  - 失败现象：把解释器缺失误报成 `Port 5006 is already in use`
  - 失败位置：`Makefile` 的 `check-ports`
  - 当前影响范围：阻断 `make start/restart`，误导排障方向
  - 是否影响交付判断：是
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证
- `MAKE-RESTART-HEALTH-001`
  - 缺陷等级：`P2`
  - 发现时间：`2026-06-19 03:18 CST`
  - 发现测试项：启动后健康探针读检
  - 复现命令或复现入口：`curl -i -s http://127.0.0.1:5006/api/system/health`
  - 复现条件：后端已启动
  - 失败现象：返回 `404 NOT FOUND`
  - 失败位置：后端路由
  - 当前影响范围：阻断使用该路径做健康检查，但不阻断本轮 `make restart`
  - 是否影响交付判断：否
  - 当前判断：产品现状 / 非本轮修复范围
  - 当前状态：暂不处理

### 是否满足完成口径

- 针对“`make restart` 误报 5006 端口占用”这个问题，本轮已完成真实修复与真实命令链验证。
- 但如果把范围扩大到“后端健康检查接口也必须存在”，则该部分 `尚未完成`。

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：未验证
- 真实命令链验证结果：部分通过

### 最近一次执行时间

- `2026-06-19 03:19:00 CST`

## 本次补充测试（2026-06-19 local LLM 通过 .env 读取 LOCAL_API_KEY）

### 本次测试范围

- `src/common/config_utils.py` 是否读取 `.env` 中的 `LOCAL_API_KEY`
- `src/common/llm_factory.py` 的 `local` provider 是否使用 `LOCAL_API_KEY`
- `tests/test_local_llm_api_key_config.py` 定向配置与构造参数回归
- `scripts/test_local_llm_api_key.py` 独立直连脚本

### 本次执行时间

- `2026-06-19 11:20:00 CST - 2026-06-19 11:24:30 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- `.env` 中存在 `LOCAL_API_KEY`
- `LOCAL_LLM_URL` 当前解析为 `http://localhost:5000/v1`

### 执行命令或测试入口

- `env PYTHONPATH=.:src ./.venv/bin/python -m py_compile src/common/config_utils.py src/common/llm_factory.py tests/test_local_llm_api_key_config.py scripts/test_local_llm_api_key.py`
- `env PYTHONPATH=.:src ./.venv/bin/python - <<'PY' ... tests/test_local_llm_api_key_config.py 全量 test_* 定向执行 ... PY`
- `env PYTHONPATH=.:src ./.venv/bin/python - <<'PY' from src.common.config_utils import load_config ... PY`
- `env PYTHONPATH=.:src ./.venv/bin/python scripts/test_local_llm_api_key.py`
- `lsof -nP -iTCP:5000 -sTCP:LISTEN -Fpctn`

### 正常情况结果

- 配置读取验证：
  - `load_config()` 真实返回 `LOCAL_API_KEY_PRESENT=True`
  - `LOCAL_LLM_URL=http://localhost:5000/v1`
- 定向单元验证：
  - `PASS test_load_config_reads_local_api_key_from_env_file`
  - `PASS test_get_llm_local_uses_local_api_key`
  - `PASS test_get_llm_local_requires_local_api_key`
- 结果判定：通过

### 异常情况结果

- 缺 key 异常验证：
  - 在 patched `load_config` 不提供 `LOCAL_API_KEY` 时，`get_llm(local)` 显式抛出 `ValueError("LOCAL_API_KEY missing ...")`
- 结果判定：通过

### 特殊情况结果

- 独立脚本真实直连结果：
  - 脚本使用 `Authorization: Bearer <LOCAL_API_KEY>` 向 `http://localhost:5000/v1/chat/completions` 发起请求
  - 返回 `HTTP 403`
  - 响应头 `Server: AirTunes/950.7.1`
  - `lsof` 显示 `*:5000` 当前监听进程为 `ControlCenter`
- 结论：
  - `LOCAL_API_KEY` 已被真实读取并带到请求里
  - 但 `LOCAL_LLM_URL` 当前并未指向真正的 local LLM 服务，而是 macOS Control Center / AirTunes
- 结果判定：部分通过

### 降级结果

- 当 `LOCAL_API_KEY` 缺失时，`local` provider 现在显式失败，不再继续用假 key `sk-not-required` 伪装成功。
- 结果判定：通过

### 回滚结果

- 本轮未执行反向补丁 dry-run。
- 当前判断：未验证，不能宣称已完成回滚验证。

### 真实命令链验证结果

- `real`：
  - `load_config()` 真实读到 `.env` 中的 `LOCAL_API_KEY`
  - 独立脚本真实向 `http://localhost:5000/v1/chat/completions` 发起 HTTP 请求
  - `lsof` 真实读回 5000 端口监听者为 `ControlCenter`
- `partial`：
  - 本地服务返回了 403，证明请求到达了目标端口
  - 但当前目标端口不是实际 LLM 服务，因此无法证明模型端鉴权成功

### 失败项

- `LOCAL-LLM-URL-001`
  - 失败现象：独立脚本请求 local LLM 时收到 `HTTP 403`
  - 失败位置：`http://localhost:5000/v1/chat/completions`
  - 影响范围：当前不能宣称 local 模型调用已经打通
  - 复现方式：执行 `env PYTHONPATH=.:src ./.venv/bin/python scripts/test_local_llm_api_key.py`
  - 当前判断：环境配置错误 / 目标端口错误
  - 是否保留失败结论：是

### 缺陷归因

- `LOCAL-LLM-ROOTCAUSE-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-19 11:24 CST`
  - 发现测试项：独立 local LLM 直连脚本
  - 复现命令或复现入口：`env PYTHONPATH=.:src ./.venv/bin/python scripts/test_local_llm_api_key.py`
  - 复现条件：当前 `LOCAL_LLM_URL=http://localhost:5000/v1`
  - 失败现象：返回 `HTTP 403`，响应头 `Server: AirTunes/950.7.1`
  - 失败位置：local LLM 目标地址配置
  - 当前影响范围：阻断 local provider 的真实模型验证
  - 是否影响交付判断：是
  - 当前判断：环境配置错误 / 非代码逻辑错误
  - 当前状态：未修复

### 是否满足完成口径

- 针对“local provider 通过 `.env` 读取 `LOCAL_API_KEY`”这部分代码实现，本轮已完成实现与定向验证。
- 但针对“真实访问 local 模型成功”这部分，本轮 `尚未完成`，因为 `LOCAL_LLM_URL` 当前指到了错误端口。

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：部分通过
- 降级结果：通过
- 回滚结果：未验证
- 真实命令链验证结果：部分通过

### 最近一次执行时间

- `2026-06-19 11:24:30 CST`

## 本次补充测试（2026-06-19 local LLM 端口切换到 8317）

### 本次测试范围

- `.env` 中的 `LOCAL_LLM_URL`
- `src/common/config_utils.py` 对 `LOCAL_LLM_URL` 的环境变量合并
- `src/common/llm_factory.py` / `scripts/test_local_llm_api_key.py` 的 local URL fallback
- local 直连脚本对 `8317` 端口的真实请求

### 本次执行时间

- `2026-06-19 11:25:00 CST - 2026-06-19 11:31:28 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- local 目标地址：`http://localhost:8317/v1`

### 执行命令或测试入口

- `lsof -nP -iTCP:8317 -sTCP:LISTEN`
- `env PYTHONPATH=.:src ./.venv/bin/python -m py_compile src/common/config_utils.py src/common/llm_factory.py tests/test_local_llm_api_key_config.py scripts/test_local_llm_api_key.py`
- `env PYTHONPATH=.:src ./.venv/bin/python - <<'PY' from src.common.config_utils import load_config ... PY`
- `env PYTHONPATH=.:src ./.venv/bin/python scripts/test_local_llm_api_key.py`

### 正常情况结果

- 配置读取验证：
  - `load_config()` 返回 `LOCAL_LLM_URL=http://localhost:8317/v1`
  - `LLM_MODELS.local.base_url=http://localhost:8317/v1`
- 定向单元验证仍通过：
  - `PASS test_get_llm_local_requires_local_api_key`
  - `PASS test_get_llm_local_uses_local_api_key`
  - `PASS test_load_config_reads_local_api_key_from_env_file`
- 结果判定：通过

### 异常情况结果

- 首轮直连脚本曾返回 `HTTP 400`，内部错误为上游 `403 PERMISSION_DENIED`
- 同一脚本在 `2026-06-19 11:31:28 CST` 复测后返回：
  - `HTTP 200`
  - `choices[0].message.content = LOCAL_API_KEY_OK`
- 当前结果判定：通过

### 特殊情况结果

- 与上一轮 `5000` 不同，这次 `8317` 端口确实有目标服务在监听
- `lsof` 显示监听进程：`cliproxyapi`
- 说明：
  - 端口已改对
  - 请求已到达预期的 local 代理服务
  - 首轮出现过上游权限错误，但同一路径复测后已返回成功结果，说明当前链路可用
- 结果判定：通过

### 降级结果

- 当 local 服务返回错误时，独立脚本会显式打印 HTTP 状态、响应头和错误体；本轮复测已恢复到成功响应。
- 结果判定：通过

### 回滚结果

- 本轮未执行反向补丁 dry-run。
- 当前判断：未验证，不能宣称已完成回滚验证。

### 真实命令链验证结果

- `real`：
  - 真实 `lsof` 读回 8317 端口监听者
  - 真实 `load_config()` 读回 `LOCAL_LLM_URL=http://localhost:8317/v1`
  - 真实脚本向 `8317` 发起 HTTP 请求并收到了 `HTTP 200`
  - 返回正文包含 `LOCAL_API_KEY_OK`

### 失败项

- `LOCAL-LLM-UPSTREAM-001`
  - 失败现象：首轮请求返回 `HTTP 400`，内部错误为上游 `403 PERMISSION_DENIED`；复测后未复现
  - 失败位置：local 代理服务的上游账号权限（首次执行时）
  - 影响范围：当前不再阻断 local 模型连通结论，但提示该链路曾出现外部波动
  - 复现方式：执行 `env PYTHONPATH=.:src ./.venv/bin/python scripts/test_local_llm_api_key.py`
  - 当前判断：外部服务瞬时状态 / 已恢复
  - 是否保留失败结论：是

### 缺陷归因

- `LOCAL-LLM-UPSTREAM-001`
  - 缺陷等级：`P2`
  - 发现时间：`2026-06-19 11:27 CST`
  - 发现测试项：local LLM 8317 端口直连验证
  - 复现命令或复现入口：`env PYTHONPATH=.:src ./.venv/bin/python scripts/test_local_llm_api_key.py`
  - 复现条件：`LOCAL_LLM_URL=http://localhost:8317/v1`
  - 失败现象：首轮返回 `antigravity auth missing project_id ... 403 ... service has been disabled`，复测后返回 `HTTP 200`
  - 失败位置：local 代理服务上游鉴权/账号（首次执行时）
  - 当前影响范围：提示该外部链路曾不稳定，但当前不阻断 live local LLM 调用
  - 是否影响交付判断：否
  - 当前判断：外部服务瞬时状态 / 已恢复
  - 当前状态：已恢复已验证

### 是否满足完成口径

- 针对“端口改成 8317”这项配置修改，本轮已完成实现和真实读检。
- 针对“8317 上的 local 模型调用成功”这项 live 验证，本轮复测已拿到 `HTTP 200 + LOCAL_API_KEY_OK`，当前可记为已验证通过。

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：未验证
- 真实命令链验证结果：通过

### 最近一次执行时间

- `2026-06-19 11:31:28 CST`

## 本次补充测试（2026-06-19 章节简介与总结审核误判与自动重试修复）

### 本次测试范围

- `src/app_api.py` 章节简介与总结工作流的自动重试闭环
- `src/agents/chapter_content_summary_create_agent.py` / `src/agents/chapter_content_summary_update_agent.py` 的审核规则
- 章节简介与总结 LLM 提示词输出合同，不再要求模型回传原始 `content`
- `/api/hierarchy-agent/start` 对 `chapter_intro_summary_create` 的真实运行结果

### 本次执行时间

- `2026-06-19 11:32:00 CST - 2026-06-19 11:47:00 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- Flask API：`src/app_api.py`
- 真实接口验证端口：`5007`（同命令内临时启动）
- local LLM：当前配置为 `LOCAL_LLM_URL=http://localhost:8317/v1`

### 执行命令或测试入口

- 代码编译检查：
  - `./.venv/bin/python -m compileall src/app_api.py src/agents/chapter_content_summary_create_agent.py src/agents/chapter_content_summary_update_agent.py tests/test_summary_workflow_requests.py`
- 定向函数回归：
  - `./.venv/bin/python - <<'PY' from tests import test_summary_workflow_requests as t; ... 逐条调用测试函数 ... PY`
- 真实接口验证：
  - `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py > /tmp/novel-agent-inline.log 2>&1 &`
  - 等待 `5007` 端口监听后，执行 `POST http://127.0.0.1:5007/api/hierarchy-agent/start`
  - 真实 payload 使用 `chapter_intro_summary_create + outline_id=outline_924c676d + chapter_id=chapter_content_mqjriuy071xisu`

### 正常情况结果

- 测了什么：章节简介与总结工作流在首轮审核失败后，是否会自动进入返工并再次审核，而不是直接停在 `review_failed`
- 怎么测：
  - 定向回归执行 `test_chapter_intro_summary_workflow_auto_retries_after_failed_review`
  - 真实接口调用 `/api/hierarchy-agent/start`
- 看到什么结果：
  - 定向回归通过，节点链路为 `input -> initial_expansion -> review -> revision_retry_1 -> review_retry_1`
  - 真实接口返回：
    - `HTTP 200`
    - `run_status=waiting_human`
    - `current_node=human`
    - `auto_retry_count=1`
    - `review_errors=[]`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：当章节简介与总结连续返工仍不达标时，系统是否会在达到上限后显式失败，而不是无限循环
- 怎么测：
  - 定向回归执行 `test_chapter_intro_summary_workflow_stops_after_retry_limit`
- 看到什么结果：
  - 状态为 `review_failed`
  - `auto_retry_count` 等于 `SUMMARY_AUTO_RETRY_LIMIT`
  - 节点中出现 `revision_retry_1/review_retry_1` 以及最后一轮重试节点
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：原文是大量分段文本，但总结采用单段高密度压缩时，审核节点是否还会误判成“错误简化”
- 怎么测：
  - 定向回归执行 `test_chapter_intro_summary_review_accepts_dense_single_paragraph_summary`
  - 对照历史真实 run `run_f33888c391ce` 的失败原因
- 看到什么结果：
  - 历史真实 run 的失败原因是：`摘要把多段场景压成单句，疑似错误简化。`
  - 本次已删除这条误判规则
  - 定向回归下，单段但信息完整的章节总结可通过审核
- 结果是否通过：通过

### 降级验证

- 当审核仍失败时，系统不再卡死无反馈，而是显式保留 `review_failed` 状态和重试链路节点，便于用户继续人工处理
- 定向回归已验证该降级分支
- 结果判定：通过

### 回滚验证

- 本轮未执行代码回滚或反向补丁 dry-run
- 当前判断：未验证，不能宣称已完成回滚验证

### 真实命令链验证

- `real`：
  - 真实读取历史 run `run_f33888c391ce`，确认旧问题并非“读不到简介/总结”，而是审核规则误判
  - 真实在同命令内启动 Flask API，并通过 `POST /api/hierarchy-agent/start` 跑通新的 `chapter_intro_summary_create`
  - 真实读回新 run：
    - `run_id=run_7a6e751c6a21`
    - `run_status=waiting_human`
    - `current_node=human`
    - `auto_retry_count=1`
    - `node_ids=['input','initial_expansion','review','revision_retry_1','review_retry_1']`

### 缺陷记录区

- `SUMMARY-REVIEW-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-19 11:35 CST`
  - 发现测试项：章节简介与总结真实 run 审核读回
  - 复现命令或复现入口：读取 `run_f33888c391ce`
  - 复现条件：长章节正文被压成单段但信息完整的章节总结
  - 失败现象：审核节点把有效总结误判为 `摘要把多段场景压成单句，疑似错误简化`
  - 失败位置：`chapter_content_summary_create_agent._check_summary_quality`
  - 当前影响范围：会把可接受的章节简介与总结误判打回，阻断工作流
  - 是否影响交付判断：是
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证

- `TEST-ENV-001`
  - 缺陷等级：`P3`
  - 发现时间：`2026-06-19 11:38 CST`
  - 发现测试项：仓库测试命令执行
  - 复现命令或复现入口：`./.venv/bin/python -m pytest tests/test_summary_workflow_requests.py -q`
  - 复现条件：当前 `.venv` 未安装 `pytest`
  - 失败现象：`No module named pytest`
  - 失败位置：测试环境
  - 当前影响范围：阻断 `pytest` 方式执行，但不阻断同一解释器下的定向函数回归和真实接口验证
  - 是否影响交付判断：否
  - 当前判断：环境缺失
  - 当前状态：未修复

### 执行结果汇总

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：未验证
- 真实命令链验证结果：通过

### 最近一次执行时间

- `2026-06-19 11:47:00 CST`

---

## 2026-06-19 Outline 修改节点用户反馈注入修复

### 本次测试范围

- `src/agents/outline_agent.py` 修改节点反馈优先级与修改 Prompt 职责拆分
- `src/app_api.py` modify_content 节点 Prompt 重建时的 `review_feedback` 透传
- `src/agents/novel_agent.py` modify_content 节点独立 LLM identity 修复
- `tests/test_hierarchy_draft_llm_invocation.py` 回归测试更新

### 本次执行时间

- `2026-06-19 14:20:00 CST - 2026-06-19 14:33:00 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- 临时真实接口端口：`5007`
- 被验证 run：`run_0facab75b240`

### 执行命令或测试入口

- 编译检查：
  - `./.venv/bin/python -m compileall src/agents/outline_agent.py src/agents/novel_agent.py src/app_api.py tests/test_hierarchy_draft_llm_invocation.py`
- 定向函数回归：
  - `./.venv/bin/python - <<'PY' ... test_outline_prompts_require_preserving_long_form_outline_content ... test_outline_modify_content_node_prioritizes_user_feedback_and_preserves_review_feedback ... test_all_hierarchy_modules_content_modification_calls_dedicated_llm ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm ... PY`
- 真实接口验证：
  - `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
  - `POST http://127.0.0.1:5007/api/hierarchy-agent/respond`
  - `GET http://127.0.0.1:5007/api/hierarchy-agent/get?run_id=run_0facab75b240`

### 正常情况结果

- 测了什么：用户在大纲工作流点击“对话反馈/修改意见”后，最新 `modify_content` 节点是否把用户意见真实传入 LLM Prompt。
- 怎么测：
  - 对现有 `run_0facab75b240` 追加一次真实 `decision=request_changes`
  - 读回最后一个 `modify_content` 节点的 `input.feedback` 与 `output.llm_call.prompt`
- 看到什么结果：
  - `input.feedback=验证修复：请按世界观设定修正帝国公民与殖民地居民的定义，不要扩写其他无关剧情。`
  - `input.review_feedback` 仍保留系统审查问题
  - Prompt 中同时出现 `【用户反馈】`、`【系统审查问题】`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：同时存在用户反馈和历史审查反馈时，系统是否还会像旧版本一样用历史审查反馈覆盖用户意见。
- 怎么测：
  - 定向执行 `test_outline_modify_content_node_prioritizes_user_feedback_and_preserves_review_feedback`
  - 真实读取 `run_0facab75b240` 最后一个 `modify_content` 节点输入
- 看到什么结果：
  - 代码级断言通过：`feedback=用户反馈`，`expansion_error=审查反馈`
  - 真实 run 读回中 `input.feedback` 为用户意见，`input.review_feedback` 为审查问题，没有再互相覆盖
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：修改节点 Prompt 是否仍错误带着“修改并扩写（Modify + Expand）”职责，导致模型把定点修订任务做成继续扩充。
- 怎么测：
  - 定向执行 `test_outline_prompts_require_preserving_long_form_outline_content`
  - 真实读取最后一个 `modify_content` 节点 Prompt 头部
- 看到什么结果：
  - Prompt 断言通过，包含 `本任务是：定点修订（Modify Only）`
  - 真实接口读回 `prompt_uses_modify_only=true`
  - 旧的“修改并扩写”职责已去除
- 结果是否通过：通过

### 降级结果

- ChromaDB embedding 侧出现 `localhost:11434` 连接失败警告，但没有阻断 `outline_agent_modify_content`、`outline_world_rules_review_agent`、`outline_worldview_rules_review_agent`、`outline_novel_rules_review_agent` 的真实 LLM 调用
- 当前判断：存在环境告警，但本次主链路可用，未出现伪成功

### 回滚结果

- 本轮未执行反向补丁 dry-run
- 当前判断：未验证，不能宣称完成回滚验证

### 真实命令链验证结果

- `POST /api/hierarchy-agent/respond` 真实返回 `200`
- 后端真实日志出现：
  - `Instantiating LLM for agent 'outline_agent_modify_content' using model 'gemini-3-flash' (Provider: local)`
  - 后续三个审查节点也都真实实例化独立 LLM
- `GET /api/hierarchy-agent/get?run_id=run_0facab75b240` 真实读回：
  - `input_feedback` 为用户意见
  - `input_review_feedback` 为系统审查问题
  - `prompt_has_user_section=true`
  - `prompt_has_review_section=true`
  - `prompt_uses_modify_only=true`

### 失败项

- 无主链路失败
- 定向测试过程中额外发现一项真实缺陷：`novel_agent.generate_content_modification()` 漏传 `llm_agent_name`

### 缺陷归因

- `OUTLINE-MODIFY-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-19 14:20 CST`
  - 发现测试项：outline 工作流 `request_changes` 真实 run 读回
  - 复现命令或复现入口：`GET /api/hierarchy-agent/get?run_id=run_0facab75b240`
  - 复现条件：用户在页面提交“对话反馈/修改意见”
  - 失败现象：`modify_content.input.feedback` 与 Prompt 中实际使用的是旧审查反馈，不是最新用户意见；Prompt 仍要求“修改并扩写”
  - 失败位置：`src/agents/outline_agent.py`
  - 当前影响范围：用户修改意见不能被真实喂入 LLM，修改节点职责漂移
  - 是否影响交付判断：是
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证

- `NOVEL-LLM-IDENTITY-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-19 14:28 CST`
  - 发现测试项：`test_all_hierarchy_modules_content_modification_calls_dedicated_llm`
  - 复现命令或复现入口：定向函数回归
  - 复现条件：执行 `novel_agent.generate_content_modification()`
  - 失败现象：`_invoke_llm()` 调用缺少 `llm_agent_name`，且返回值仍使用共用 `novel_agent`
  - 失败位置：`src/agents/novel_agent.py`
  - 当前影响范围：小说修改节点无法稳定调用独立 LLM，且审计 identity 错误
  - 是否影响交付判断：是
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：未验证
- 真实命令链验证结果：通过
- 结论：本次“outline 修改节点未注入用户意见、Prompt 误用扩写职责”的修复已完成实现并完成真实链路验证；但本轮未做回滚 dry-run，因此不能把“回滚验证已完成”当作证据。

### 最近一次执行时间

- `2026-06-19 14:33:00 CST`

---

## 2026-06-19 全局节点 LLM identity 独立化排查与修复

### 本次测试范围

- `src/common/llm_identity_registry.py` 全局节点级 LLM identity 注册表
- `src/common/llm_factory.py` 首次取 LLM 时的 fail-closed 校验
- `src/app_api.py` 启动期校验与历史 shared identity 补显修复
- `config/llm.yml` 各节点独立 `AGENT_MODELS` 配置槽位
- `src/agents/world_agent.py`
- `src/agents/worldview_agent.py`
- `src/agents/novel_agent.py`
- `src/agents/outline_agent.py`
- `src/agents/chapter_agent.py`
- `src/agents/outline_summary_create_agent.py`
- `src/agents/outline_summary_update_agent.py`
- `src/agents/chapter_outline_summary_create_agent.py`
- `src/agents/chapter_outline_summary_update_agent.py`
- `src/agents/chapter_content_summary_create_agent.py`
- `src/agents/chapter_content_summary_update_agent.py`
- `tests/test_hierarchy_draft_llm_invocation.py`
- `/Users/harry/.codex/skills/engineering-spec-enforcer/SKILL.md`
- `/Users/harry/.codex/skills/engineering-spec-enforcer/references/project_engineering_spec.md`

### 本次执行时间

- `2026-06-19 14:36:00 CST - 2026-06-19 14:45:00 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- 临时真实接口端口：`5007`
- 本地配置：`LLM_PROVIDER=local`，`local base_url=http://localhost:8317/v1`

### 执行命令或测试入口

- 编译检查：
  - `./.venv/bin/python -m compileall src/common/llm_identity_registry.py src/common/llm_factory.py src/app_api.py tests/test_hierarchy_draft_llm_invocation.py`
- 定向函数回归：
  - `./.venv/bin/python - <<'PY' ... test_all_hierarchy_modules_content_modification_calls_dedicated_llm ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm ... test_llm_identity_registry_is_unique_and_configured ... test_app_api_hydrates_legacy_shared_llm_names_for_all_hierarchy_nodes ... test_outline_modify_content_node_prioritizes_user_feedback_and_preserves_review_feedback ... PY`
- 注册表真实配置校验：
  - `./.venv/bin/python - <<'PY' ... validate_llm_identity_registry((get_config() or {}).get('AGENT_MODELS', {})) ... PY`
- 失败路径验证：
  - `./.venv/bin/python - <<'PY' ... validate_llm_identity_registry({'world_agent_initial_expansion': {}}) ... PY`
- 启动级真实命令链：
  - `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
  - `curl -s http://127.0.0.1:5007/`

### 正常情况结果

- 测了什么：所有已接入工作流节点是否都拥有独立 `llm_agent_name`，并且每个 identity 都在 `AGENT_MODELS` 中有独立槽位。
- 怎么测：
  - 执行 `test_llm_identity_registry_is_unique_and_configured`
  - 执行 `validate_llm_identity_registry((get_config() or {}).get('AGENT_MODELS', {}))`
- 看到什么结果：
  - 定向回归输出：
    - `OK: test_all_hierarchy_modules_content_modification_calls_dedicated_llm`
    - `OK: test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm`
    - `OK: test_llm_identity_registry_is_unique_and_configured`
    - `OK: test_app_api_hydrates_legacy_shared_llm_names_for_all_hierarchy_nodes`
    - `OK: test_outline_modify_content_node_prioritizes_user_feedback_and_preserves_review_feedback`
  - 注册表校验输出：
    - `unique_count= 35`
    - `duplicates= []`
    - `missing= []`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：当 `AGENT_MODELS` 缺少节点专属槽位时，系统是否会显式失败，而不是静默容忍混用或缺配。
- 怎么测：
  - 人工构造坏配置：`{'world_agent_initial_expansion': {}}`
  - 直接执行 `validate_llm_identity_registry(...)`
- 看到什么结果：
  - 输出 `missing_case= ValueError`
  - 错误信息明确列出全部缺失 identity：`AGENT_MODELS missing dedicated llm_agent_name entries: [...]`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：历史 run 中旧的 shared identity 是否会在读路径被自动补显成新的独立 identity，而不是继续污染前端审计信息。
- 怎么测：
  - 执行 `test_app_api_hydrates_legacy_shared_llm_names_for_all_hierarchy_nodes`
  - 覆盖 `world/worldview/novel/outline/chapter` 五类旧 shared 名称：`world_agent/worldview_agent/novel_agent/outline_agent/chapter_agent`
- 看到什么结果：
  - `initial_expansion` 和 `modify_content` 两类节点都会被补成各自的独立 identity
  - 注册表统计输出：
    - `total_names= 35`
    - `unique_names= 35`
    - `has_duplicates= False`
- 结果是否通过：通过

### 降级结果

- 当配置错误或 identity 重复时，系统不会伪装成“还能跑”，而是在校验阶段直接抛出异常并阻止继续使用错误配置
- 当前判断：fail-closed 生效，降级策略正确

### 回滚结果

- 本轮未执行反向补丁 dry-run
- 当前判断：未验证，不能宣称完成回滚验证

### 真实命令链验证结果

- `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py` 成功启动
- 启动日志显示：
  - `Serving Flask app 'app_api'`
  - `Running on http://127.0.0.1:5007`
- `curl -s http://127.0.0.1:5007/` 真实返回：
  - `{"service":"novel_agent","status":"success"}`
- 这说明：
  - `app_api` 导入期 registry 校验通过
  - 当前配置下不会因为节点 identity 缺失而阻断服务启动

### 失败项

- 无新增主链路失败
- 已知测试环境限制仍存在：
  - `.venv` 未安装 `pytest`
  - 本轮继续采用同一解释器下的定向函数执行与真实 HTTP 启动验证

### 缺陷归因

- `LLM-IDENTITY-001`
  - 缺陷等级：`P1`
  - 发现时间：`2026-06-19 14:36 CST`
  - 发现测试项：多节点 LLM identity 排查
  - 复现命令或复现入口：定向读取各 agent 常量、注册表与 `config/llm.yml`
  - 复现条件：多个不同节点历史上共用模块级 `world_agent/worldview_agent/novel_agent/outline_agent/chapter_agent`
  - 失败现象：不同节点无法在审计、提示词回显、模型配置层被唯一识别，存在 identity 混用风险
  - 失败位置：`src/agents/*`、`src/app_api.py`、`config/llm.yml`
  - 当前影响范围：影响节点级审计、模型路由和历史 run 展示正确性
  - 是否影响交付判断：是
  - 当前判断：产品缺陷
  - 当前状态：已修复已验证

- `TEST-ENV-001`
  - 缺陷等级：`P3`
  - 发现时间：`2026-06-19 11:38 CST`
  - 发现测试项：仓库测试命令执行
  - 复现命令或复现入口：`./.venv/bin/python -m pytest tests/test_hierarchy_draft_llm_invocation.py -q`
  - 复现条件：当前 `.venv` 未安装 `pytest`
  - 失败现象：`No module named pytest`
  - 失败位置：测试环境
  - 当前影响范围：阻断 `pytest` 方式执行，但不阻断同一解释器下的定向函数回归和真实启动验证
  - 是否影响交付判断：否
  - 当前判断：环境缺失
  - 当前状态：未修复

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：未验证
- 真实命令链验证结果：通过
- 结论：本次“禁止不同节点共用 LLM identity”的代码修复、配置补齐和启动级校验已经完成；但本轮没有补齐每一条前端工作流页面的端到端 UI 验证，也没有执行回滚 dry-run，因此不能把整个项目宣称为“已达到完整交付标准”。

### 最近一次执行时间

- `2026-06-19 14:45:00 CST`

---

## 2026-06-19 创建世界工作流 LLM identity 审计与映射表

### 本次测试范围

- `src/agents/world_agent.py` 创建世界工作流节点定义与节点级 LLM identity
- `src/common/llm_identity_registry.py` world 工作流 identity 注册表
- `config/llm.yml` world 工作流独立 `AGENT_MODELS` 槽位
- `tests/test_world_workflow_llm_draft_requests.py` 创建世界真实 requests 回归
- `tests/test_real_agent_world_flow_requests.py` world 工作流真实请求回归
- `docs/world_create_workflow_llm_mapping.md` 节点名称与节点使用的 LLM 对应关系表

### 本次执行时间

- `2026-06-19 14:46:00 CST - 2026-06-19 14:49:00 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- 临时真实接口端口：`5007`
- 本地配置：`LLM_PROVIDER=local`，`LOCAL_LLM_URL=http://localhost:8317/v1`

### 执行命令或测试入口

- 编译检查：
  - `./.venv/bin/python -m compileall tests/test_world_workflow_llm_draft_requests.py tests/test_real_agent_world_flow_requests.py`
- 定向 identity 回归：
  - `./.venv/bin/python - <<'PY' ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm ... test_all_hierarchy_modules_content_modification_calls_dedicated_llm ... PY`
- 真实 requests 回归：
  - `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_world_workflow_llm_draft_requests.py`
- 真实读回审计：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... POST /api/hierarchy-agent/start ... POST /api/hierarchy-agent/respond(request_changes) ... POST /api/hierarchy-agent/respond(approve) ... PY`

### 正常情况结果

- 测了什么：创建世界工作流首次扩充节点是否使用独立的 `world_agent_initial_expansion`，而不是旧的共用 `world_agent`。
- 怎么测：
  - 检查 [src/agents/world_agent.py](/Users/harry/Documents/git/novel_agent/src/agents/world_agent.py) 常量
  - 执行 `tests/test_world_workflow_llm_draft_requests.py`
  - 真实读回临时 run
- 看到什么结果：
  - 代码常量：`INITIAL_EXPANSION_AGENT_NAME = "world_agent_initial_expansion"`
  - 真实 requests 测试输出：`world workflow LLM initial expansion requests test passed`
  - 真实 run 读回：
    - `run_id=run_850cbc4e654d`
    - `initial_llm_agent_name=world_agent_initial_expansion`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：创建世界工作流中本来不该调用 LLM 的节点，是否误用了共享 LLM identity。
- 怎么测：
  - 审计 world 工作流节点图：`input -> initial_expansion -> human -> commit`
  - 人工打回路径：`... -> modify_content -> human -> commit`
  - 核对 [docs/world_create_workflow_llm_mapping.md](/Users/harry/Documents/git/novel_agent/docs/world_create_workflow_llm_mapping.md) 中 `input/human/commit` 标记为 `N/A`
- 看到什么结果：
  - `input`、`human`、`commit` 都没有 LLM 调用入口
  - `app_api` 对 `agent_type=world` 的 `review_required=False`
  - 创建世界工作流不存在 review 节点，因此不存在 review 节点与生成节点混用 identity 的问题
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：用户在创建世界工作流中点击 `request_changes` 后，`modify_content` 是否使用与 `initial_expansion` 不同的独立 LLM identity。
- 怎么测：
  - 真实执行 `POST /api/hierarchy-agent/start`
  - 再执行 `POST /api/hierarchy-agent/respond` with `decision=request_changes`
  - 读取 `modify_content.output.llm_call.llm_agent_name`
- 看到什么结果：
  - 真实 run 读回：
    - `initial_llm_agent_name=world_agent_initial_expansion`
    - `modify_llm_agent_name=world_agent_modify_content`
    - `final_status=completed`
  - 临时后端日志显示：
    - `Instantiating LLM for agent 'world_agent_initial_expansion'`
    - `Instantiating LLM for agent 'world_agent_modify_content'`
- 结果是否通过：通过

### 降级结果

- 本次没有发现创建世界工作流里的伪成功降级
- 检索上下文侧仍出现 `localhost:11434` 的 Chroma embedding 警告，但没有把 `world_agent_modify_content` 降级成共用或假 LLM
- 当前判断：主链路通过，存在旁路告警但不影响本次 identity 审计结论

### 回滚结果

- 本轮未执行反向补丁 dry-run
- 当前判断：未验证，不能宣称完成回滚验证

### 真实命令链验证结果

- `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py` 成功启动
- `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_world_workflow_llm_draft_requests.py` 成功输出：
  - `world workflow LLM initial expansion requests test passed`
- 真实临时审计脚本读回：
  - `run_id=run_850cbc4e654d`
  - `initial_llm_agent_name=world_agent_initial_expansion`
  - `modify_llm_agent_name=world_agent_modify_content`
  - `final_status=completed`

### 失败项

- 无产品主链路失败
- 额外发现一项测试资产问题：
  - `tests/test_world_workflow_llm_draft_requests.py` 之前仍在断言旧的 `world_agent`，与当前节点级 identity 规则不一致

### 缺陷归因

- `WORLD-TEST-IDENTITY-001`
  - 缺陷等级：`P2`
  - 发现时间：`2026-06-19 14:46 CST`
  - 发现测试项：创建世界工作流 LLM identity 审计
  - 复现命令或复现入口：读取 `tests/test_world_workflow_llm_draft_requests.py`
  - 复现条件：测试文件仍使用旧断言 `agent_name == "world_agent"`
  - 失败现象：验证脚本与当前“节点必须独立 identity”的规则不一致，可能掩盖真实回归
  - 失败位置：`tests/test_world_workflow_llm_draft_requests.py`
  - 当前影响范围：影响 world 工作流 identity 验证准确性，但不影响生产链路实际运行
  - 是否影响交付判断：否
  - 当前判断：测试资产缺陷
  - 当前状态：已修复已验证

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：未验证
- 真实命令链验证结果：通过
- 结论：创建世界工作流已确认不存在节点 LLM 混用问题，并已生成节点名称与节点使用 LLM 的对应关系文件；但本轮未执行回滚 dry-run，因此不能把“回滚验证已完成”当作证据。

### 最近一次执行时间

- `2026-06-19 14:49:00 CST`

---

## 2026-06-19 修改世界工作流 LLM identity 审计与映射表

### 本次测试范围

- `src/agents/world_agent.py` 修改世界工作流节点定义与节点级 LLM identity
- `src/common/llm_identity_registry.py` world update 工作流 identity 注册表
- `config/llm.yml` world update 工作流独立 `AGENT_MODELS` 槽位
- `tests/test_real_agent_world_update_flow_requests.py` 修改世界真实 requests 回归
- `docs/world_update_workflow_llm_mapping.md` 节点名称与节点使用的 LLM 对应关系表

### 本次执行时间

- `2026-06-19 14:50:00 CST - 2026-06-19 14:53:00 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- 临时真实接口端口：`5007`
- 本地配置：`LLM_PROVIDER=local`，`LOCAL_LLM_URL=http://localhost:8317/v1`

### 执行命令或测试入口

- 编译检查：
  - `./.venv/bin/python -m compileall tests/test_real_agent_world_update_flow_requests.py`
- 定向 identity 回归：
  - `./.venv/bin/python - <<'PY' ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm ... test_all_hierarchy_modules_content_modification_calls_dedicated_llm ... PY`
- 真实 requests 回归：
  - `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_real_agent_world_update_flow_requests.py`
- 真实读回审计：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... POST /api/worlds/create ... POST /api/hierarchy-agent/start(action=update) ... POST /api/hierarchy-agent/respond(request_changes) ... POST /api/hierarchy-agent/respond(approve) ... PY`

### 正常情况结果

- 测了什么：修改世界工作流首次扩充节点是否使用独立的 `world_agent_initial_expansion`。
- 怎么测：
  - 检查 [src/agents/world_agent.py](/Users/harry/Documents/git/novel_agent/src/agents/world_agent.py) 常量
  - 执行 `tests/test_real_agent_world_update_flow_requests.py`
  - 真实读回临时 run
- 看到什么结果：
  - 代码常量：`INITIAL_EXPANSION_AGENT_NAME = "world_agent_initial_expansion"`
  - 真实 run 读回：
    - `run_id=run_82b3814bb601`
    - `initial_llm_agent_name=world_agent_initial_expansion`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：修改世界工作流中本来不该调用 LLM 的节点，是否误用了共享 LLM identity。
- 怎么测：
  - 审计 world update 工作流节点图：`input -> initial_expansion -> human -> commit`
  - 人工打回路径：`... -> modify_content -> human -> commit`
  - 核对 [docs/world_update_workflow_llm_mapping.md](/Users/harry/Documents/git/novel_agent/docs/world_update_workflow_llm_mapping.md) 中 `input/human/commit` 标记为 `N/A`
- 看到什么结果：
  - `input`、`human`、`commit` 都没有 LLM 调用入口
  - `app_api` 对 `agent_type=world` 的 `review_required=False`
  - 修改世界工作流不存在 review 节点，因此不存在 review 节点与生成节点混用 identity 的问题
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：用户在修改世界工作流中点击 `request_changes` 后，`modify_content` 是否使用与 `initial_expansion` 不同的独立 LLM identity。
- 怎么测：
  - 真实执行 `POST /api/worlds/create`
  - 再执行 `POST /api/hierarchy-agent/start` with `action=update`
  - 再执行 `POST /api/hierarchy-agent/respond` with `decision=request_changes`
  - 读取 `modify_content.output.llm_call.llm_agent_name`
- 看到什么结果：
  - 真实 run 读回：
    - `initial_llm_agent_name=world_agent_initial_expansion`
    - `modify_llm_agent_name=world_agent_modify_content`
    - `updated_world_name=Updated World world_update_llm_map_53be761ffe`
    - `final_status=completed`
  - 临时后端日志显示：
    - `Instantiating LLM for agent 'world_agent_initial_expansion'`
    - `Instantiating LLM for agent 'world_agent_modify_content'`
- 结果是否通过：通过

### 降级结果

- 本次没有发现修改世界工作流里的伪成功降级
- 检索上下文侧仍出现 `localhost:11434` 的 Chroma embedding 警告，但没有把 `world_agent_modify_content` 降级成共用或假 LLM
- 当前判断：主链路通过，存在旁路告警但不影响本次 identity 审计结论

### 回滚结果

- 本轮未执行反向补丁 dry-run
- 当前判断：未验证，不能宣称完成回滚验证

### 真实命令链验证结果

- `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py` 成功启动
- `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_real_agent_world_update_flow_requests.py` 成功退出，未报错
- 真实临时审计脚本读回：
  - `run_id=run_82b3814bb601`
  - `initial_llm_agent_name=world_agent_initial_expansion`
  - `modify_llm_agent_name=world_agent_modify_content`
  - `updated_world_name=Updated World world_update_llm_map_53be761ffe`
  - `final_status=completed`

### 失败项

- 无产品主链路失败

### 缺陷归因

- 本轮未发现新的 world update 生产缺陷
- 结论：当前修改世界工作流的节点级 LLM identity 与配置槽位一致，未发现节点混用

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：未验证
- 真实命令链验证结果：通过
- 结论：修改世界工作流已确认不存在节点 LLM 混用问题，并已生成节点名称与节点使用 LLM 的对应关系文件；但本轮未执行回滚 dry-run，因此不能把“回滚验证已完成”当作证据。

### 最近一次执行时间

- `2026-06-19 14:53:00 CST`

---

## 2026-06-19 创建世界观工作流 LLM identity 审计与映射表

### 本次测试范围

- `src/agents/worldview_agent.py` 创建世界观工作流节点定义与节点级 LLM identity
- `src/agents/review_nodes/world_review.py` 世界规则审查节点
- `src/agents/review_nodes/worldview_review.py` 世界观一致性审查节点
- `src/common/llm_identity_registry.py` worldview create 工作流 identity 注册表
- `config/llm.yml` worldview create 工作流独立 `AGENT_MODELS` 槽位
- `tests/test_real_agent_worldview_flow_requests.py` 创建世界观真实 requests 回归
- `docs/worldview_create_workflow_llm_mapping.md` 节点名称与节点使用的 LLM 对应关系表

### 本次执行时间

- `2026-06-19 14:54:00 CST - 2026-06-19 14:57:00 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- 临时真实接口端口：`5007`
- 本地配置：`LLM_PROVIDER=local`，`LOCAL_LLM_URL=http://localhost:8317/v1`

### 执行命令或测试入口

- 编译检查：
  - `./.venv/bin/python -m compileall tests/test_real_agent_worldview_flow_requests.py`
- 定向 identity 回归：
  - `./.venv/bin/python - <<'PY' ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm ... test_all_hierarchy_modules_content_modification_calls_dedicated_llm ... test_llm_identity_registry_is_unique_and_configured ... PY`
- 真实 requests 回归：
  - `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_real_agent_worldview_flow_requests.py`
- 真实读回审计：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... POST /api/worlds/create ... POST /api/hierarchy-agent/start(action=create) ... POST /api/hierarchy-agent/respond(request_changes) ... PY`

### 正常情况结果

- 测了什么：创建世界观工作流的首次扩充节点和两道 review 节点是否都使用独立的 `llm_agent_name`。
- 怎么测：
  - 检查 [src/agents/worldview_agent.py](/Users/harry/Documents/git/novel_agent/src/agents/worldview_agent.py) 常量与 review 装配
  - 执行 `tests/test_real_agent_worldview_flow_requests.py`
  - 真实读回临时 run
- 看到什么结果：
  - 代码常量：`INITIAL_EXPANSION_AGENT_NAME = "worldview_agent_initial_expansion"`
  - review 装配：
    - `world_rule_review -> worldview_world_rules_review_agent`
    - `worldview_consistency_review -> worldview_consistency_review_agent`
  - 真实 run 读回：
    - `run_id=run_effbfc050c6e`
    - `initial_llm_agent_name=worldview_agent_initial_expansion`
    - `world_rule_review_llm_agent_name=worldview_world_rules_review_agent`
    - `worldview_consistency_review_llm_agent_name=worldview_consistency_review_agent`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：创建世界观工作流中本来不该调用 LLM 的节点，是否误用了共享 LLM identity。
- 怎么测：
  - 审计 worldview create 工作流节点图：`input -> initial_expansion -> world_rule_review -> worldview_consistency_review -> human -> commit`
  - 核对 [docs/worldview_create_workflow_llm_mapping.md](/Users/harry/Documents/git/novel_agent/docs/worldview_create_workflow_llm_mapping.md) 中 `input/human/commit` 标记为 `N/A`
- 看到什么结果：
  - `input`、`human`、`commit` 都没有 LLM 调用入口
  - 所有 LLM 调用都集中在 `initial_expansion`、两个 review 和 `modify_content`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：用户在创建世界观工作流中点击 `request_changes` 后，`modify_content` 是否使用与其他 3 个 LLM 节点不同的独立 identity。
- 怎么测：
  - 真实执行 `POST /api/worlds/create`
  - 再执行 `POST /api/hierarchy-agent/start` with `agent_type=worldview`
  - 再执行 `POST /api/hierarchy-agent/respond` with `decision=request_changes`
  - 读取 `modify_content.output.llm_call.llm_agent_name`
- 看到什么结果：
  - 真实 run 读回：
    - `initial_llm_agent_name=worldview_agent_initial_expansion`
    - `world_rule_review_llm_agent_name=worldview_world_rules_review_agent`
    - `worldview_consistency_review_llm_agent_name=worldview_consistency_review_agent`
    - `modify_llm_agent_name=worldview_agent_modify_content`
    - `status_after_modify=waiting_human`
  - 临时后端日志显示 4 个不同的独立实例化记录
- 结果是否通过：通过

### 降级结果

- 本次没有发现创建世界观工作流里的伪成功降级
- 检索上下文侧仍出现 `localhost:11434` 的 Chroma embedding 警告，但没有把 `worldview_agent_modify_content` 或两个 review 节点降级成共用或假 LLM
- 当前判断：主链路通过，存在旁路告警但不影响本次 identity 审计结论

### 回滚结果

- 本轮未执行反向补丁 dry-run
- 当前判断：未验证，不能宣称完成回滚验证

### 真实命令链验证结果

- `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py` 成功启动
- `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_real_agent_worldview_flow_requests.py` 成功退出，未报错
- 真实临时审计脚本读回：
  - `run_id=run_effbfc050c6e`
  - `initial_llm_agent_name=worldview_agent_initial_expansion`
  - `world_rule_review_llm_agent_name=worldview_world_rules_review_agent`
  - `worldview_consistency_review_llm_agent_name=worldview_consistency_review_agent`
  - `modify_llm_agent_name=worldview_agent_modify_content`
  - `status_after_modify=waiting_human`

### 失败项

- 无产品主链路失败

### 缺陷归因

- 本轮未发现新的 worldview create 生产缺陷
- 结论：当前创建世界观工作流的节点级 LLM identity 与配置槽位一致，未发现节点混用

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：未验证
- 真实命令链验证结果：通过
- 结论：创建世界观工作流已确认不存在节点 LLM 混用问题，并已生成节点名称与节点使用 LLM 的对应关系文件；但本轮未执行回滚 dry-run，因此不能把“回滚验证已完成”当作证据。

### 最近一次执行时间

- `2026-06-19 14:57:00 CST`

---

## 2026-06-19 修改世界观工作流 LLM identity 审计与映射表

### 本次测试范围

- `src/agents/worldview_agent.py` 修改世界观工作流节点定义与节点级 LLM identity
- `src/agents/review_nodes/world_review.py` 世界规则审查节点
- `src/agents/review_nodes/worldview_review.py` 世界观一致性审查节点
- `src/common/llm_identity_registry.py` worldview update 工作流 identity 注册表
- `config/llm.yml` worldview update 工作流独立 `AGENT_MODELS` 槽位
- `tests/test_real_agent_worldview_update_flow_requests.py` 修改世界观真实 requests 回归
- `docs/worldview_update_workflow_llm_mapping.md` 节点名称与节点使用的 LLM 对应关系表

### 本次执行时间

- `2026-06-19 14:58:00 CST - 2026-06-19 15:01:00 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- 临时真实接口端口：`5007`
- 本地配置：`LLM_PROVIDER=local`，`LOCAL_LLM_URL=http://localhost:8317/v1`

### 执行命令或测试入口

- 编译检查：
  - `./.venv/bin/python -m compileall tests/test_real_agent_worldview_update_flow_requests.py`
- 定向 identity 回归：
  - `./.venv/bin/python - <<'PY' ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm ... test_all_hierarchy_modules_content_modification_calls_dedicated_llm ... test_llm_identity_registry_is_unique_and_configured ... PY`
- 真实 requests 回归：
  - `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_real_agent_worldview_update_flow_requests.py`
- 真实读回审计：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... POST /api/worlds/create ... POST /api/hierarchy-agent/start(action=update) ... POST /api/hierarchy-agent/respond(request_changes) ... POST /api/hierarchy-agent/respond(approve) ... PY`

### 正常情况结果

- 测了什么：修改世界观工作流的首次扩充节点和两道 review 节点是否都使用独立的 `llm_agent_name`。
- 怎么测：
  - 检查 [src/agents/worldview_agent.py](/Users/harry/Documents/git/novel_agent/src/agents/worldview_agent.py) 常量与 review 装配
  - 执行 `tests/test_real_agent_worldview_update_flow_requests.py`
  - 真实读回临时 run
- 看到什么结果：
  - 代码常量：`INITIAL_EXPANSION_AGENT_NAME = "worldview_agent_initial_expansion"`
  - review 装配：
    - `world_rule_review -> worldview_world_rules_review_agent`
    - `worldview_consistency_review -> worldview_consistency_review_agent`
  - 真实 run 读回：
    - `run_id=run_db9b2da5c04c`
    - `initial_llm_agent_name=worldview_agent_initial_expansion`
    - `world_rule_review_llm_agent_name=worldview_world_rules_review_agent`
    - `worldview_consistency_review_llm_agent_name=worldview_consistency_review_agent`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：修改世界观工作流中本来不该调用 LLM 的节点，是否误用了共享 LLM identity。
- 怎么测：
  - 审计 worldview update 工作流节点图：`input -> initial_expansion -> world_rule_review -> worldview_consistency_review -> human -> commit`
  - 核对 [docs/worldview_update_workflow_llm_mapping.md](/Users/harry/Documents/git/novel_agent/docs/worldview_update_workflow_llm_mapping.md) 中 `input/human/commit` 标记为 `N/A`
- 看到什么结果：
  - `input`、`human`、`commit` 都没有 LLM 调用入口
  - 所有 LLM 调用都集中在 `initial_expansion`、两个 review 和 `modify_content`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：用户在修改世界观工作流中点击 `request_changes` 后，`modify_content` 是否使用与其他 3 个 LLM 节点不同的独立 identity。
- 怎么测：
  - 真实执行 `POST /api/worlds/create`
  - 真实创建一个世界观条目
  - 再执行 `POST /api/hierarchy-agent/start` with `agent_type=worldview` and `action=update`
  - 再执行 `POST /api/hierarchy-agent/respond` with `decision=request_changes`
  - 读取 `modify_content.output.llm_call.llm_agent_name`
- 看到什么结果：
  - 真实 run 读回：
    - `initial_llm_agent_name=worldview_agent_initial_expansion`
    - `world_rule_review_llm_agent_name=worldview_world_rules_review_agent`
    - `worldview_consistency_review_llm_agent_name=worldview_consistency_review_agent`
    - `modify_llm_agent_name=worldview_agent_modify_content`
    - `updated_entry_name=Worldview Updated worldview_update_llm_map_4c3bc874c1`
    - `final_status=completed`
  - 临时后端日志显示 4 个不同的独立实例化记录
- 结果是否通过：通过

### 降级结果

- 本次没有发现修改世界观工作流里的伪成功降级
- 检索上下文侧仍出现 `localhost:11434` 的 Chroma embedding 警告，但没有把 `worldview_agent_modify_content` 或两个 review 节点降级成共用或假 LLM
- 当前判断：主链路通过，存在旁路告警但不影响本次 identity 审计结论

### 回滚结果

- 本轮未执行反向补丁 dry-run
- 当前判断：未验证，不能宣称完成回滚验证

### 真实命令链验证结果

- `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py` 成功启动
- `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_real_agent_worldview_update_flow_requests.py` 成功退出，未报错
- 真实临时审计脚本读回：
  - `run_id=run_db9b2da5c04c`
  - `initial_llm_agent_name=worldview_agent_initial_expansion`
  - `world_rule_review_llm_agent_name=worldview_world_rules_review_agent`
  - `worldview_consistency_review_llm_agent_name=worldview_consistency_review_agent`
  - `modify_llm_agent_name=worldview_agent_modify_content`
  - `updated_entry_name=Worldview Updated worldview_update_llm_map_4c3bc874c1`
  - `final_status=completed`

### 失败项

- 无产品主链路失败

### 缺陷归因

- 本轮未发现新的 worldview update 生产缺陷
- 结论：当前修改世界观工作流的节点级 LLM identity 与配置槽位一致，未发现节点混用

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：未验证
- 真实命令链验证结果：通过
- 结论：修改世界观工作流已确认不存在节点 LLM 混用问题，并已生成节点名称与节点使用 LLM 的对应关系文件；但本轮未执行回滚 dry-run，因此不能把“回滚验证已完成”当作证据。

### 最近一次执行时间

- `2026-06-19 15:01:00 CST`

---

## 2026-06-19 创建小说工作流 LLM identity 审计与映射表

### 本次测试范围

- `src/agents/novel_agent.py` 创建小说工作流节点定义与节点级 LLM identity
- `src/agents/review_nodes/novel_review.py` 小说规则审查节点
- `src/common/llm_identity_registry.py` novel create 工作流 identity 注册表
- `config/llm.yml` novel create 工作流独立 `AGENT_MODELS` 槽位
- `tests/test_real_agent_novel_flow_requests.py` 创建小说真实 requests 回归
- `docs/novel_create_workflow_llm_mapping.md` 节点名称与节点使用的 LLM 对应关系表

### 本次执行时间

- `2026-06-19 15:03:00 CST - 2026-06-19 15:06:49 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- 临时真实接口端口：`5007`
- 本地配置：`LLM_PROVIDER=local`，`LOCAL_LLM_URL=http://localhost:8317/v1`

### 执行命令或测试入口

- 编译检查：
  - `./.venv/bin/python -m compileall tests/test_real_agent_novel_flow_requests.py`
- 定向 identity 回归：
  - `./.venv/bin/python - <<'PY' ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm ... test_all_hierarchy_modules_content_modification_calls_dedicated_llm ... test_llm_identity_registry_is_unique_and_configured ... PY`
- 真实 requests 回归：
  - `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_real_agent_novel_flow_requests.py`
- 真实读回审计：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... POST /api/worlds/create ... POST /api/hierarchy-agent/start(action=create) ... POST /api/hierarchy-agent/respond(request_changes) ... POST /api/hierarchy-agent/respond(approve) ... GET /api/novels/list ... PY`

### 正常情况结果

- 测了什么：创建小说工作流的首次扩充节点和审查节点是否都使用独立的 `llm_agent_name`。
- 怎么测：
  - 检查 [src/agents/novel_agent.py](/Users/harry/Documents/git/novel_agent/src/agents/novel_agent.py) 常量与 review 装配
  - 执行 `tests/test_real_agent_novel_flow_requests.py`
  - 真实读回临时 run
- 看到什么结果：
  - 代码常量：
    - `INITIAL_EXPANSION_AGENT_NAME = "novel_agent_initial_expansion"`
    - `MODIFY_CONTENT_AGENT_NAME = "novel_agent_modify_content"`
  - review 装配：
    - `review -> novel_world_rules_review_agent`
  - 真实 run 读回：
    - `run_id=run_1a2240265f10`
    - `initial_llm_agent_name=novel_agent_initial_expansion`
    - `review_llm_agent_name=novel_world_rules_review_agent`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：创建小说工作流中本来不该调用 LLM 的节点，是否误用了共享 LLM identity。
- 怎么测：
  - 审计 novel create 工作流节点图：`input -> initial_expansion -> review -> human -> commit`
  - 核对 [docs/novel_create_workflow_llm_mapping.md](/Users/harry/Documents/git/novel_agent/docs/novel_create_workflow_llm_mapping.md) 中 `input/human/commit` 标记为 `N/A`
- 看到什么结果：
  - `input`、`human`、`commit` 都没有 LLM 调用入口
  - 所有 LLM 调用都集中在 `initial_expansion`、`review` 和 `modify_content`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：用户在创建小说工作流中点击 `request_changes` 后，`modify_content` 是否使用与其他 2 个 LLM 节点不同的独立 identity。
- 怎么测：
  - 真实执行 `POST /api/worlds/create`
  - 再执行 `POST /api/hierarchy-agent/start` with `agent_type=novel`
  - 再执行 `POST /api/hierarchy-agent/respond` with `decision=request_changes`
  - 读取 `modify_content.output.llm_call.llm_agent_name`
- 看到什么结果：
  - 真实 run 读回：
    - `initial_llm_agent_name=novel_agent_initial_expansion`
    - `review_llm_agent_name=novel_world_rules_review_agent`
    - `modify_llm_agent_name=novel_agent_modify_content`
    - `created_novel_id=novel_create_llm_map_7b0bc05a4e_01`
    - `final_status=completed`
  - 临时后端日志显示 3 个不同的独立实例化记录
- 结果是否通过：通过

### 降级结果

- 本次没有发现创建小说工作流里的伪成功降级
- 检索上下文侧仍出现 `localhost:11434` 的 Chroma embedding 警告，但没有把 `novel_agent_modify_content` 或 `novel_world_rules_review_agent` 降级成共用或假 LLM
- 当前判断：主链路通过，存在旁路告警但不影响本次 identity 审计结论

### 回滚结果

- 本轮未执行反向补丁 dry-run
- 当前判断：未验证，不能宣称完成回滚验证

### 真实命令链验证结果

- `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py` 成功启动
- `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_real_agent_novel_flow_requests.py` 成功退出，未报错
- 定向 identity 回归输出：
  - `[WARN] langfuse-python not installed, LLM tracing disabled.`
  - `OK: novel create identity unit coverage is green`
- 真实临时审计脚本读回：
  - `run_id=run_1a2240265f10`
  - `initial_llm_agent_name=novel_agent_initial_expansion`
  - `review_llm_agent_name=novel_world_rules_review_agent`
  - `modify_llm_agent_name=novel_agent_modify_content`
  - `created_novel_id=novel_create_llm_map_7b0bc05a4e_01`
  - `final_status=completed`
  - `readback_name=Novel novel_create_llm_map_7b0bc05a4e`

### 失败项

- 无产品主链路失败

### 缺陷归因

- 本轮未发现新的 novel create 生产缺陷
- 结论：当前创建小说工作流的节点级 LLM identity 与配置槽位一致，未发现节点混用

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：未验证
- 真实命令链验证结果：通过
- 结论：创建小说工作流已确认不存在节点 LLM 混用问题，并已生成节点名称与节点使用 LLM 的对应关系文件；但本轮未执行回滚 dry-run，因此不能把“回滚验证已完成”当作证据。

### 最近一次执行时间

- `2026-06-19 15:06:49 CST`

---

## 2026-06-19 修改小说工作流 LLM identity 审计与映射表

### 本次测试范围

- `src/agents/novel_agent.py` 修改小说工作流节点定义与节点级 LLM identity
- `src/agents/review_nodes/novel_review.py` 小说规则审查节点
- `src/common/llm_identity_registry.py` novel update 工作流 identity 注册表
- `config/llm.yml` novel update 工作流独立 `AGENT_MODELS` 槽位
- `tests/test_real_agent_novel_update_flow_requests.py` 修改小说真实 requests 回归
- `docs/novel_update_workflow_llm_mapping.md` 节点名称与节点使用的 LLM 对应关系表

### 本次执行时间

- `2026-06-19 15:08:00 CST - 2026-06-19 15:10:42 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- 临时真实接口端口：`5007`
- 本地配置：`LLM_PROVIDER=local`，`LOCAL_LLM_URL=http://localhost:8317/v1`

### 执行命令或测试入口

- 编译检查：
  - `./.venv/bin/python -m compileall tests/test_real_agent_novel_update_flow_requests.py`
- 定向 identity 回归：
  - `./.venv/bin/python - <<'PY' ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm ... test_all_hierarchy_modules_content_modification_calls_dedicated_llm ... test_llm_identity_registry_is_unique_and_configured ... PY`
- 真实 requests 回归：
  - `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_real_agent_novel_update_flow_requests.py`
- 真实读回审计：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... POST /api/worlds/create ... POST /api/novels/create ... POST /api/hierarchy-agent/start(action=update) ... POST /api/hierarchy-agent/respond(request_changes) ... POST /api/hierarchy-agent/respond(approve) ... GET /api/novels/list ... PY`

### 正常情况结果

- 测了什么：修改小说工作流的首次扩充节点和审查节点是否都使用独立的 `llm_agent_name`。
- 怎么测：
  - 检查 [src/agents/novel_agent.py](/Users/harry/Documents/git/novel_agent/src/agents/novel_agent.py) 常量与 review 装配
  - 执行 `tests/test_real_agent_novel_update_flow_requests.py`
  - 真实读回临时 run
- 看到什么结果：
  - 代码常量：
    - `INITIAL_EXPANSION_AGENT_NAME = "novel_agent_initial_expansion"`
    - `MODIFY_CONTENT_AGENT_NAME = "novel_agent_modify_content"`
  - review 装配：
    - `review -> novel_world_rules_review_agent`
  - 真实 run 读回：
    - `run_id=run_16490197a3e2`
    - `initial_llm_agent_name=novel_agent_initial_expansion`
    - `review_llm_agent_name=novel_world_rules_review_agent`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：修改小说工作流中本来不该调用 LLM 的节点，是否误用了共享 LLM identity。
- 怎么测：
  - 审计 novel update 工作流节点图：`input -> initial_expansion -> review -> human -> commit`
  - 核对 [docs/novel_update_workflow_llm_mapping.md](/Users/harry/Documents/git/novel_agent/docs/novel_update_workflow_llm_mapping.md) 中 `input/human/commit` 标记为 `N/A`
- 看到什么结果：
  - `input`、`human`、`commit` 都没有 LLM 调用入口
  - 所有 LLM 调用都集中在 `initial_expansion`、`review` 和 `modify_content`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：用户在修改小说工作流中点击 `request_changes` 后，`modify_content` 是否使用与其他 2 个 LLM 节点不同的独立 identity。
- 怎么测：
  - 真实执行 `POST /api/worlds/create`
  - 真实执行 `POST /api/novels/create`
  - 再执行 `POST /api/hierarchy-agent/start` with `agent_type=novel` and `action=update`
  - 再执行 `POST /api/hierarchy-agent/respond` with `decision=request_changes`
  - 读取 `modify_content.output.llm_call.llm_agent_name`
- 看到什么结果：
  - 真实 run 读回：
    - `initial_llm_agent_name=novel_agent_initial_expansion`
    - `review_llm_agent_name=novel_world_rules_review_agent`
    - `modify_llm_agent_name=novel_agent_modify_content`
    - `updated_novel_id=novel_novel_update_llm_map_2b751d6d33`
    - `final_status=completed`
  - 临时后端日志显示 3 个不同的独立实例化记录
- 结果是否通过：通过

### 降级结果

- 本次没有发现修改小说工作流里的伪成功降级
- 检索上下文侧仍出现 `localhost:11434` 的 Chroma embedding 警告，但没有把 `novel_agent_modify_content` 或 `novel_world_rules_review_agent` 降级成共用或假 LLM
- 当前判断：主链路通过，存在旁路告警但不影响本次 identity 审计结论

### 回滚结果

- 本轮未执行反向补丁 dry-run
- 当前判断：未验证，不能宣称完成回滚验证

### 真实命令链验证结果

- `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py` 成功启动
- `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python tests/test_real_agent_novel_update_flow_requests.py` 成功退出，未报错
- 定向 identity 回归输出：
  - `[WARN] langfuse-python not installed, LLM tracing disabled.`
  - `OK: novel update identity unit coverage is green`
- 真实临时审计脚本读回：
  - `run_id=run_16490197a3e2`
  - `initial_llm_agent_name=novel_agent_initial_expansion`
  - `review_llm_agent_name=novel_world_rules_review_agent`
  - `modify_llm_agent_name=novel_agent_modify_content`
  - `updated_novel_id=novel_novel_update_llm_map_2b751d6d33`
  - `final_status=completed`
  - `readback_name=Novel Updated novel_update_llm_map_2b751d6d33`

### 失败项

- 无产品主链路失败

### 缺陷归因

- 本轮未发现新的 novel update 生产缺陷
- 结论：当前修改小说工作流的节点级 LLM identity 与配置槽位一致，未发现节点混用

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：未验证
- 真实命令链验证结果：通过
- 结论：修改小说工作流已确认不存在节点 LLM 混用问题，并已生成节点名称与节点使用 LLM 的对应关系文件；但本轮未执行回滚 dry-run，因此不能把“回滚验证已完成”当作证据。

### 最近一次执行时间

- `2026-06-19 15:10:42 CST`

---

## 2026-06-19 创建分卷大纲工作流 LLM identity 审计与映射表

### 本次测试范围

- `src/agents/outline_agent.py` 创建分卷大纲工作流节点定义与节点级 LLM identity
- `src/agents/review_nodes/world_review.py` 世界规则审查节点
- `src/agents/review_nodes/worldview_review.py` 世界观规则审查节点
- `src/agents/review_nodes/novel_review.py` 小说规则审查节点
- `src/common/llm_identity_registry.py` outline create 工作流 identity 注册表
- `config/llm.yml` outline create 工作流独立 `AGENT_MODELS` 槽位
- `tests/test_real_agent_outline_flow_requests.py` 创建分卷大纲真实 requests 回归
- `tests/real_request_test_utils.py` 真实父级链路构造工具
- `docs/outline_create_workflow_llm_mapping.md` 节点名称与节点使用的 LLM 对应关系表

### 本次执行时间

- `2026-06-19 15:12:00 CST - 2026-06-19 15:22:10 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- 临时真实接口端口：`5007`
- 本地配置：`LLM_PROVIDER=local`，`LOCAL_LLM_URL=http://localhost:8317/v1`

### 执行命令或测试入口

- 编译检查：
  - `./.venv/bin/python -m compileall tests/real_request_test_utils.py tests/test_real_agent_outline_flow_requests.py`
- 定向 identity 回归：
  - `./.venv/bin/python - <<'PY' ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm ... test_all_hierarchy_modules_content_modification_calls_dedicated_llm ... test_llm_identity_registry_is_unique_and_configured ... PY`
- 真实函数执行回归：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' from tests.test_real_agent_outline_flow_requests import test_real_agent_outline_flow_requests; test_real_agent_outline_flow_requests(); print("OK: real outline create workflow identity flow executed") PY`
- 真实读回审计：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... POST /api/worlds/create ... POST /api/worldviews/update ... POST /api/novels/create ... POST /api/hierarchy-agent/start(action=create) ... 多次 POST /api/hierarchy-agent/respond(request_changes) ... POST /api/hierarchy-agent/respond(approve) ... GET /api/outlines/list ... PY`

### 正常情况结果

- 测了什么：创建分卷大纲工作流的首次扩充节点、三道审查节点和返工节点是否都使用独立的 `llm_agent_name`。
- 怎么测：
  - 检查 [src/agents/outline_agent.py](/Users/harry/Documents/git/novel_agent/src/agents/outline_agent.py) 常量与 review 装配
  - 运行内联真实回归函数 `test_real_agent_outline_flow_requests()`
  - 运行真实读回审计脚本，打印最终 run 的各节点 `llm_agent_name`
- 看到什么结果：
  - 代码常量：
    - `INITIAL_EXPANSION_AGENT_NAME = "outline_agent_initial_expansion"`
    - `MODIFY_CONTENT_AGENT_NAME = "outline_agent_modify_content"`
  - review 装配：
    - `world_review -> outline_world_rules_review_agent`
    - `worldview_review -> outline_worldview_rules_review_agent`
    - `novel_review -> outline_novel_rules_review_agent`
  - 真实 run 读回：
    - `run_id=run_61e20babbea8`
    - `initial_llm_agent_name=outline_agent_initial_expansion`
    - `world_review_llm_agent_name=outline_world_rules_review_agent`
    - `worldview_review_llm_agent_name=outline_worldview_rules_review_agent`
    - `novel_review_llm_agent_name=outline_novel_rules_review_agent`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：创建分卷大纲工作流中本来不该调用 LLM 的节点，是否误用了共享 LLM identity。
- 怎么测：
  - 审计 outline create 工作流节点图：`input -> initial_expansion -> world_review -> worldview_review -> novel_review -> human -> commit`
  - 核对 [docs/outline_create_workflow_llm_mapping.md](/Users/harry/Documents/git/novel_agent/docs/outline_create_workflow_llm_mapping.md) 中 `input/human/commit` 标记为 `N/A`
- 看到什么结果：
  - `input`、`human`、`commit` 都没有 LLM 调用入口
  - 所有 LLM 调用都集中在 `initial_expansion`、三道 review 和 `modify_content`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：创建分卷大纲工作流在真实 review 多次打回后，`modify_content` 是否仍使用与其他 4 个 LLM 节点不同的独立 identity，并最终推进到 `novel_review`。
- 怎么测：
  - 使用真实父级世界/世界观/小说数据启动 `outline + create`
  - 对每一轮失败的 review 读取 `errors`
  - 通过 `decision=request_changes` 把失败意见回灌给工作流，最多迭代 4 次
  - 直到链路实际走到 `novel_review`
- 看到什么结果：
  - 真实迭代序列：
    - `ITER0 ['input', 'initial_expansion', 'world_review']`
    - `ITER1 ['input', 'initial_expansion', 'world_review', 'modify_content', 'world_review', 'worldview_review']`
    - `ITER2 ['input', 'initial_expansion', 'world_review', 'modify_content', 'world_review', 'worldview_review', 'modify_content', 'world_review', 'worldview_review', 'novel_review']`
  - 最终各节点实际读回：
    - `initial_expansion -> outline_agent_initial_expansion`
    - `world_review -> outline_world_rules_review_agent`
    - `worldview_review -> outline_worldview_rules_review_agent`
    - `novel_review -> outline_novel_rules_review_agent`
    - `modify_content -> outline_agent_modify_content`
  - 最终批准写库后：
    - `created_outline_id=outline_create_llm_map_effdb309`
    - `final_status=completed`
    - `readback_name=Outline outline_create_llm_map_effdb309`
- 结果是否通过：通过

### 降级结果

- 本次没有发现创建分卷大纲工作流里的伪成功降级
- 检索上下文侧仍出现 `localhost:11434` 的 Chroma embedding 警告，但没有把 `outline_agent_modify_content`、`outline_world_rules_review_agent`、`outline_worldview_rules_review_agent` 或 `outline_novel_rules_review_agent` 降级成共用或假 LLM
- 当前判断：主链路通过，存在旁路告警但不影响本次 identity 审计结论

### 回滚结果

- 本轮未执行反向补丁 dry-run
- 当前判断：未验证，不能宣称完成回滚验证

### 真实命令链验证结果

- `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py` 成功启动
- 定向 identity 回归输出：
  - `[WARN] langfuse-python not installed, LLM tracing disabled.`
  - `OK: outline create identity unit coverage is green`
- 内联真实回归函数执行输出：
  - `OK: real outline create workflow identity flow executed`
- 真实临时审计脚本读回：
  - `run_id=run_61e20babbea8`
  - `initial_llm_agent_name=outline_agent_initial_expansion`
  - `world_review_llm_agent_name=outline_world_rules_review_agent`
  - `worldview_review_llm_agent_name=outline_worldview_rules_review_agent`
  - `novel_review_llm_agent_name=outline_novel_rules_review_agent`
  - `modify_llm_agent_name=outline_agent_modify_content`
  - `created_outline_id=outline_create_llm_map_effdb309`
  - `final_status=completed`
  - `readback_name=Outline outline_create_llm_map_effdb309`

### 失败项

- 无节点 identity 混用类产品缺陷

### 缺陷归因

- 本轮发现并修复了测试工具缺陷：
  - `make_full_chain()` 之前返回了伪造的 `worldview_id`，不是世界真实库 ID
  - 该问题会污染真实 outline 审计输入，属于测试工具产品缺陷，不是 outline workflow 节点 identity 缺陷
- 修复后结论：当前创建分卷大纲工作流的节点级 LLM identity 与配置槽位一致，未发现节点混用

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：未验证
- 真实命令链验证结果：通过
- 结论：创建分卷大纲工作流已确认不存在节点 LLM 混用问题，并已生成节点名称与节点使用 LLM 的对应关系文件；但本轮未执行回滚 dry-run，因此不能把“回滚验证已完成”当作证据。

### 最近一次执行时间

- `2026-06-19 15:22:10 CST`

---

## 2026-06-19 修改分卷大纲工作流 LLM identity 审计与映射表

### 本次测试范围

- `src/agents/outline_agent.py` 修改分卷大纲工作流节点定义与节点级 LLM identity
- `src/agents/review_nodes/world_review.py` 世界规则审查节点
- `src/agents/review_nodes/worldview_review.py` 世界观规则审查节点
- `src/agents/review_nodes/novel_review.py` 小说规则审查节点
- `src/common/llm_identity_registry.py` outline update 工作流 identity 注册表
- `config/llm.yml` outline update 工作流独立 `AGENT_MODELS` 槽位
- `tests/test_real_agent_outline_update_flow_requests.py` 修改分卷大纲真实 requests 回归
- `docs/outline_update_workflow_llm_mapping.md` 节点名称与节点使用的 LLM 对应关系表

### 本次执行时间

- `2026-06-19 15:24:00 CST - 2026-06-19 15:30:03 CST`

### 执行环境

- 仓库路径：`/Users/harry/Documents/git/novel_agent`
- Python：`./.venv/bin/python`
- 临时真实接口端口：`5007`
- 本地配置：`LLM_PROVIDER=local`，`LOCAL_LLM_URL=http://localhost:8317/v1`

### 执行命令或测试入口

- 编译检查：
  - `./.venv/bin/python -m compileall src/agents/outline_agent.py tests/test_real_agent_outline_update_flow_requests.py tests/test_real_review_rule_world_review_requests.py tests/test_real_review_rule_worldview_review_requests.py tests/test_real_review_rule_novel_review_requests.py`
- 定向 identity 回归：
  - `./.venv/bin/python - <<'PY' ... test_all_hierarchy_modules_initial_expansion_calls_dedicated_llm ... test_all_hierarchy_modules_content_modification_calls_dedicated_llm ... test_llm_identity_registry_is_unique_and_configured ... PY`
- 真实函数执行回归：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' from tests.test_real_agent_outline_update_flow_requests import test_real_agent_outline_update_flow_requests; test_real_agent_outline_update_flow_requests(); print("OK: real outline update workflow identity flow executed") PY`
- 真实读回审计：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... POST /api/worlds/create ... POST /api/worldviews/update ... POST /api/novels/create ... POST /api/outlines/create ... POST /api/hierarchy-agent/start(action=update) ... POST /api/hierarchy-agent/respond(request_changes) ... POST /api/hierarchy-agent/respond(approve) ... GET /api/outlines/list ... PY`

### 正常情况结果

- 测了什么：修改分卷大纲工作流的首次扩充节点、三道审查节点和返工节点是否都使用独立的 `llm_agent_name`。
- 怎么测：
  - 检查 [src/agents/outline_agent.py](/Users/harry/Documents/git/novel_agent/src/agents/outline_agent.py) 常量与 review 装配
  - 运行内联真实回归函数 `test_real_agent_outline_update_flow_requests()`
  - 运行真实读回审计脚本，打印最终 run 的各节点 `llm_agent_name`
- 看到什么结果：
  - 代码常量：
    - `INITIAL_EXPANSION_AGENT_NAME = "outline_agent_initial_expansion"`
    - `MODIFY_CONTENT_AGENT_NAME = "outline_agent_modify_content"`
  - review 装配：
    - `world_review -> outline_world_rules_review_agent`
    - `worldview_review -> outline_worldview_rules_review_agent`
    - `novel_review -> outline_novel_rules_review_agent`
  - 真实 run 读回：
    - `run_id=run_a7c6292459a8`
    - `initial_llm_agent_name=outline_agent_initial_expansion`
    - `world_review_llm_agent_name=outline_world_rules_review_agent`
    - `worldview_review_llm_agent_name=outline_worldview_rules_review_agent`
    - `novel_review_llm_agent_name=outline_novel_rules_review_agent`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：修改分卷大纲工作流中本来不该调用 LLM 的节点，是否误用了共享 LLM identity。
- 怎么测：
  - 审计 outline update 工作流节点图：`input -> initial_expansion -> world_review -> worldview_review -> novel_review -> human -> commit`
  - 核对 [docs/outline_update_workflow_llm_mapping.md](/Users/harry/Documents/git/novel_agent/docs/outline_update_workflow_llm_mapping.md) 中 `input/human/commit` 标记为 `N/A`
- 看到什么结果：
  - `input`、`human`、`commit` 都没有 LLM 调用入口
  - 所有 LLM 调用都集中在 `initial_expansion`、三道 review 和 `modify_content`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：修改分卷大纲工作流在正式审查直接通过时，人工 `request_changes` 触发的 `modify_content` 是否仍使用与其他 4 个 LLM 节点不同的独立 identity。
- 怎么测：
  - 先以真实父级世界/世界观/小说/大纲数据启动 `outline + update`
  - 如果前 3 道审查直接通过到 `waiting_human`，则在人工阶段主动 `request_changes`
  - 再读取 `modify_content.output.llm_call.llm_agent_name`
- 看到什么结果：
  - 真实 run 读回：
    - `modify_llm_agent_name=outline_agent_modify_content`
    - `world_review_reviewer=outline_world_rules_review_agent`
    - `worldview_review_reviewer=outline_worldview_rules_review_agent`
    - `novel_review_reviewer=outline_novel_rules_review_agent`
    - `updated_outline_id=outline_outline_update_llm_map_62dd9a24`
    - `final_status=completed`
  - 真实回归函数输出：
    - `OK: real outline update workflow identity flow executed`
- 结果是否通过：通过

### 降级结果

- 本次没有发现修改分卷大纲工作流里的伪成功降级
- 检索上下文侧仍出现 `localhost:11434` 的 Chroma embedding 警告，但没有把 `outline_agent_modify_content`、`outline_world_rules_review_agent`、`outline_worldview_rules_review_agent` 或 `outline_novel_rules_review_agent` 降级成共用或假 LLM
- 当前判断：主链路通过，存在旁路告警但不影响本次 identity 审计结论

### 回滚结果

- 本轮未执行反向补丁 dry-run
- 当前判断：未验证，不能宣称完成回滚验证

### 真实命令链验证结果

- `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py` 成功启动
- 定向 identity 回归输出：
  - `[WARN] langfuse-python not installed, LLM tracing disabled.`
  - `OK: outline update identity unit coverage is green`
- 内联真实回归函数执行输出：
  - `OK: real outline update workflow identity flow executed`
- 真实临时审计脚本读回：
  - `run_id=run_a7c6292459a8`
  - `initial_llm_agent_name=outline_agent_initial_expansion`
  - `world_review_llm_agent_name=outline_world_rules_review_agent`
  - `worldview_review_llm_agent_name=outline_worldview_rules_review_agent`
  - `novel_review_llm_agent_name=outline_novel_rules_review_agent`
  - `modify_llm_agent_name=outline_agent_modify_content`
  - `world_review_reviewer=outline_world_rules_review_agent`
  - `worldview_review_reviewer=outline_worldview_rules_review_agent`
  - `novel_review_reviewer=outline_novel_rules_review_agent`
  - `updated_outline_id=outline_outline_update_llm_map_62dd9a24`
  - `final_status=completed`
  - `readback_name=Outline Updated outline_update_llm_map_62dd9a24`

### 失败项

- 无节点 identity 混用类产品缺陷

### 缺陷归因

- 本轮发现并修复了 outline review 节点输出身份不一致问题：
  - `outline_agent.py` 原先把 `reviewer` 字段写成 `outline_world_review_agent / outline_worldview_review_agent / outline_novel_review_agent`
  - 真实配置、注册表和 `llm_call.llm_agent_name` 使用的却是 `*_rules_review_agent`
  - 这会造成同一 review 节点暴露两套身份名称，属于节点身份表达错误
- 修复后结论：当前修改分卷大纲工作流的节点级 LLM identity 与配置槽位一致，未发现节点混用

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：未验证
- 真实命令链验证结果：通过
- 结论：修改分卷大纲工作流已确认不存在节点 LLM 混用问题，并已生成节点名称与节点使用 LLM 的对应关系文件；但本轮未执行回滚 dry-run，因此不能把“回滚验证已完成”当作证据。

### 最近一次执行时间

- `2026-06-19 15:30:03 CST`

---

## 追加测试记录：创建章节大纲工作流 LLM 身份审计

### 本次测试范围

- `src/agents/review_nodes/chapter_review.py` 创建章节大纲工作流 review 节点 reviewer 映射
- `tests/test_real_agent_chapter_flow_requests.py` 真实请求链路回归
- `docs/chapter_create_workflow_llm_mapping.md` 节点名称与 LLM 文件名对应关系表

### 本次执行时间

- `2026-06-19 15:41:50 CST`

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 后端：临时启动 `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
- 测试解释器：`./.venv/bin/python`
- 数据源：本地 MongoDB、真实 HTTP API、真实 live LLM

### 执行命令或测试入口

- 代码审计：`rg -n "reviewer=|chapter_.*review_agent|chapter_.*rules_review_agent" src/agents/review_nodes/chapter_review.py src/common/llm_identity_registry.py config/llm.yml`
- 真实请求验证：`API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_flow_requests() ... PY`
- 语法校验：`./.venv/bin/python -m py_compile src/agents/review_nodes/chapter_review.py tests/test_real_agent_chapter_flow_requests.py`

### 正常情况结果

- 测了什么：创建章节大纲工作流从 `initial_expansion -> world_review -> worldview_review -> novel_review -> outline_review -> chapter_review -> human -> commit` 的真实闭环，且每个 review 节点都暴露正确独立的 reviewer/LLM 身份。
- 怎么测：
  - 先用真实 API 创建 world/worldview/novel/outline 和前置章节
  - 启动 `chapter + create`
  - 必要时多次 `request_changes` 推进到 `chapter_review`
  - 最后 `approve` 并读回章节记录
- 看到什么结果：
  - 真实临时审计脚本读回：
    - `run_id=run_dc5f6d588639`
    - `initial_llm_agent_name=chapter_agent_initial_expansion`
    - `world_review_reviewer=chapter_world_rules_review_agent`
    - `world_review_llm_agent_name=chapter_world_rules_review_agent`
    - `worldview_review_reviewer=chapter_worldview_rules_review_agent`
    - `worldview_review_llm_agent_name=chapter_worldview_rules_review_agent`
    - `novel_review_reviewer=chapter_novel_rules_review_agent`
    - `novel_review_llm_agent_name=chapter_novel_rules_review_agent`
    - `outline_review_reviewer=chapter_outline_rules_review_agent`
    - `outline_review_llm_agent_name=chapter_outline_rules_review_agent`
    - `chapter_review_reviewer=chapter_consistency_review_agent`
    - `chapter_review_llm_agent_name=chapter_consistency_review_agent`
    - `final_status=completed`
    - `readback_chapter_id=chapter_79efbc4d3f`
    - `readback_outline_id=outline_chapter_create_llm_map_79efbc4d3f`
    - `readback_novel_id=novel_chapter_create_llm_map_79efbc4d3f`
    - `readback_name=Agent Chapter chapter_create_llm_map_79efbc4d3f`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：人工 `request_changes` 后进入 `modify_content`，且不得复用 `initial_expansion` 或任一 review 节点的 LLM 身份。
- 怎么测：
  - 对同一 run 执行真实 `POST /api/hierarchy-agent/respond` with `decision=request_changes`
  - 读取 `modify_content.output.llm_call.llm_agent_name`
  - 统计 7 个 LLM 节点身份去重
- 看到什么结果：
  - 真实临时审计脚本读回：
    - `modify_llm_agent_name=chapter_agent_modify_content`
    - `initial_expansion/world_review/worldview_review/novel_review/outline_review/chapter_review/modify_content` 共 7 个节点 `llm_agent_name` 全部唯一
  - 真实回归函数输出：
    - `OK: real chapter create workflow identity flow executed`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：静态装配层 reviewer 名称是否与注册表、配置槽位和真实 `get_llm(..., agent_name=...)` 使用的身份一致。
- 怎么测：
  - 执行 `rg` 对比 `src/agents/review_nodes/chapter_review.py`、`src/common/llm_identity_registry.py`、`config/llm.yml`
- 看到什么结果：
  - 修复前存在 4 处错误 reviewer 名称：
    - `chapter_world_review_agent`
    - `chapter_worldview_review_agent`
    - `chapter_novel_review_agent`
    - `chapter_outline_review_agent`
  - 修复后 reviewer 名称全部与配置槽位、注册表和真实 `llm_call.llm_agent_name` 一致
- 结果是否通过：通过

### 降级结果

- 若 live LLM 连接失败、超时或返回非法 JSON，必须保留失败结论，不能因为 reviewer 字段已修就误报整条工作流完成。
- 本轮真实链路出现 `ChromaDB Search Error (localhost:11434 connect refused)` 告警，但没有把节点降级成共用 LLM，也没有阻断 `chapter_agent_modify_content` 与五道审查节点的真实调用。
- 当前判断：主链路通过，存在旁路检索告警但不影响本次 identity 审计结论。
- 结果是否通过：部分通过

### 回滚结果

- 回滚入口：`git diff -- src/agents/review_nodes/chapter_review.py tests/test_real_agent_chapter_flow_requests.py docs/chapter_create_workflow_llm_mapping.md 测试计划文件.md | git apply --check -R -`
- `git apply --check -R -` 返回 0
- 结果是否通过：通过

### 真实命令链验证结果

- 需要同时保留：
  - 临时 5007 后端启动日志
  - 真实 `start -> request_changes -> approve -> readback -> cleanup` HTTP 链路结果
  - `py_compile` 退出码
- 验证结果：
  - `./.venv/bin/python -m py_compile src/agents/review_nodes/chapter_review.py tests/test_real_agent_chapter_flow_requests.py` 返回 0
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_flow_requests() ... PY` 输出 `OK: real chapter create workflow identity flow executed`
  - 真实临时审计脚本读回：
    - `run_id=run_dc5f6d588639`
    - `modify_llm_agent_name=chapter_agent_modify_content`
    - `final_status=completed`
    - `readback_chapter_id=chapter_79efbc4d3f`
- 结果是否通过：通过

### 失败项

- 无节点 identity 混用类产品缺陷

### 缺陷归因

- 当前已确认的产品缺陷：
  - `P1`：`src/agents/review_nodes/chapter_review.py` 中 4 个 reviewer 名称与真实独立 LLM 身份不一致，导致章节创建工作流节点级审计信息错误。
- 修复状态：已修复已验证。

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：通过
- 真实命令链验证结果：通过
- 结论：创建章节大纲工作流已确认不存在节点 LLM 混用问题，并已生成节点名称与节点使用 LLM 的对应关系文件；但本轮伴随 Chroma embedding 旁路告警，所以不能把“所有依赖都完全健康”当作结论。

---

## 追加测试记录：修改章节大纲工作流 LLM 身份审计

### 本次测试范围

- `src/agents/chapter_agent.py` 修改章节大纲工作流节点与 `target_id` 更新路径
- `src/agents/review_nodes/chapter_review.py` 章节修改工作流 review 节点 reviewer 映射
- `tests/test_real_agent_chapter_update_flow_requests.py` 真实请求链路回归
- `docs/chapter_update_workflow_llm_mapping.md` 节点名称与 LLM 文件名对应关系表

### 本次执行时间

- `2026-06-19 15:56:54 CST`

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 后端：临时启动 `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
- 测试解释器：`./.venv/bin/python`
- 数据源：本地 MongoDB、真实 HTTP API、真实 live LLM

### 执行命令或测试入口

- 代码审计：`rg -n "target_id|action == \\\"update\\\"|reviewer=|chapter_.*rules_review_agent" src/agents/chapter_agent.py src/agents/review_nodes/chapter_review.py src/common/llm_identity_registry.py config/llm.yml`
- 真实请求验证：`API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_update_flow_requests() ... PY`
- 语法校验：`./.venv/bin/python -m py_compile src/agents/chapter_agent.py src/agents/review_nodes/chapter_review.py tests/test_real_agent_chapter_update_flow_requests.py`

### 正常情况结果

- 测了什么：修改章节大纲工作流从 `initial_expansion -> world_review -> worldview_review -> novel_review -> outline_review -> chapter_review -> human -> commit` 的真实闭环，且每个 review 节点都暴露正确独立的 reviewer/LLM 身份。
- 怎么测：
  - 先用真实 API 创建 world/worldview/novel/outline、前置章节和待修改章节
  - 启动 `chapter + update`
  - 必要时多次 `request_changes` 推进到 `chapter_review`
  - 最后 `approve` 并读回更新后的章节记录
- 看到什么结果：
  - 真实临时审计脚本读回：
    - `run_id=run_44c891ea728f`
    - `initial_llm_agent_name=chapter_agent_initial_expansion`
    - `world_review_reviewer=chapter_world_rules_review_agent`
    - `world_review_llm_agent_name=chapter_world_rules_review_agent`
    - `worldview_review_reviewer=chapter_worldview_rules_review_agent`
    - `worldview_review_llm_agent_name=chapter_worldview_rules_review_agent`
    - `novel_review_reviewer=chapter_novel_rules_review_agent`
    - `novel_review_llm_agent_name=chapter_novel_rules_review_agent`
    - `outline_review_reviewer=chapter_outline_rules_review_agent`
    - `outline_review_llm_agent_name=chapter_outline_rules_review_agent`
    - `chapter_review_reviewer=chapter_consistency_review_agent`
    - `chapter_review_llm_agent_name=chapter_consistency_review_agent`
    - `final_status=completed`
    - `updated_chapter_id=chapter_target_chapter_update_llm_map_00f2e0b143`
    - `readback_outline_id=outline_chapter_update_llm_map_00f2e0b143`
    - `readback_novel_id=novel_chapter_update_llm_map_00f2e0b143`
    - `readback_title=Agent Chapter Updated chapter_update_llm_map_00f2e0b143`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：人工 `request_changes` 后进入 `modify_content`，且不得复用 `initial_expansion` 或任一 review 节点的 LLM 身份。
- 怎么测：
  - 对同一 update run 执行真实 `POST /api/hierarchy-agent/respond` with `decision=request_changes`
  - 读取 `modify_content.output.llm_call.llm_agent_name`
  - 统计 7 个 LLM 节点身份去重
- 看到什么结果：
  - 真实临时审计脚本读回：
    - `modify_llm_agent_name=chapter_agent_modify_content`
    - `initial_expansion/world_review/worldview_review/novel_review/outline_review/chapter_review/modify_content` 共 7 个节点 `llm_agent_name` 全部唯一
  - 真实回归函数输出：
    - `OK: real chapter update workflow identity flow executed`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：`chapter/update` 是否引入了额外共享 LLM 或把 `target_id` 更新路径和 reviewer 身份混用。
- 怎么测：
  - 执行 `rg` 对比 `src/agents/chapter_agent.py`、`src/agents/review_nodes/chapter_review.py`、`src/common/llm_identity_registry.py`、`config/llm.yml`
- 看到什么结果：
  - 真实代码审计结果：
    - update 与 create 共用同一套 7 个独立 LLM 节点
    - `commit` 只负责按 `target_id` 更新 `prose`
    - 没有新增共享 `chapter_agent` / `chapter_review_agent` reviewer 用法
- 结果是否通过：通过

### 降级结果

- 若 live LLM 连接失败、超时或返回非法 JSON，必须保留失败结论，不能因为章节记录最终可更新就误报节点 identity 已通过。
- 本轮真实链路出现 `ChromaDB Search Error (localhost:11434 connect refused)` 告警，但没有把节点降级成共用 LLM，也没有阻断 `chapter_agent_modify_content` 与五道审查节点的真实调用。
- 当前判断：主链路通过，存在旁路检索告警但不影响本次 identity 审计结论。
- 结果是否通过：部分通过

### 回滚结果

- 回滚入口：`git diff -- tests/test_real_agent_chapter_update_flow_requests.py docs/chapter_update_workflow_llm_mapping.md 测试计划文件.md | git apply --check -R -`
- `git apply --check -R -` 返回 0
- 结果是否通过：通过

### 真实命令链验证结果

- 需要同时保留：
  - 临时 5007 后端启动日志
  - 真实 `start -> request_changes -> approve -> readback -> cleanup` HTTP 链路结果
  - `py_compile` 退出码
- 验证结果：
  - `./.venv/bin/python -m py_compile src/agents/chapter_agent.py src/agents/review_nodes/chapter_review.py tests/test_real_agent_chapter_update_flow_requests.py` 返回 0
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_update_flow_requests() ... PY` 输出 `OK: real chapter update workflow identity flow executed`
  - 真实临时审计脚本读回：
    - `run_id=run_44c891ea728f`
    - `modify_llm_agent_name=chapter_agent_modify_content`
    - `final_status=completed`
    - `updated_chapter_id=chapter_target_chapter_update_llm_map_00f2e0b143`
- 结果是否通过：通过

### 失败项

- 无节点 identity 混用类产品缺陷

### 缺陷归因

- 本轮未发现修改章节大纲工作流的节点 identity 混用类产品缺陷。
- 章节 update 工作流直接继承了 [src/agents/review_nodes/chapter_review.py](/Users/harry/Documents/git/novel_agent/src/agents/review_nodes/chapter_review.py) 已修复的 reviewer 映射，因此 create/update 两条链路在节点身份上保持一致。
- 本轮调试阶段出现过一次测试断言写法问题：读回记录里 `title` 是 update 的权威更新字段，不能用旧的 `name` 断言新标题。
- 当前判断：测试污染已修复，不属于产品节点 LLM 混用问题。

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：通过
- 真实命令链验证结果：通过
- 结论：修改章节大纲工作流已确认不存在节点 LLM 混用问题，并已生成节点名称与节点使用 LLM 的对应关系文件；但本轮伴随 Chroma embedding 旁路告警，所以不能把“所有依赖都完全健康”当作结论。

---

## 追加测试记录：创建章节内容工作流 LLM 身份审计

### 本次测试范围

- `frontend/src/pages/NovelChapterContentManagement.tsx` 工作流添加章节内容入口
- `frontend/src/pages/HierarchyWorkflow.tsx` `chapter_outline_id` 入参与回填
- `src/agents/chapter_agent.py` 创建章节内容工作流节点与 `chapter_outline_id` 写库路径
- `src/agents/review_nodes/chapter_review.py` 创建章节内容工作流 review 节点 reviewer 映射
- `tests/test_real_agent_chapter_content_create_flow_requests.py` 真实请求链路回归
- `docs/chapter_content_create_workflow_llm_mapping.md` 节点名称与 LLM 文件名对应关系表

### 本次执行时间

- `2026-06-19 16:06:04 CST`

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 后端：临时启动 `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
- 测试解释器：`./.venv/bin/python`
- 数据源：本地 MongoDB、真实 HTTP API、真实 live LLM

### 执行命令或测试入口

- 代码审计：`rg -n "chapter_outline_id|workflow/chapter\\?|reviewer=|chapter_.*rules_review_agent" frontend/src/pages/NovelChapterContentManagement.tsx frontend/src/pages/HierarchyWorkflow.tsx src/agents/chapter_agent.py src/agents/review_nodes/chapter_review.py src/common/llm_identity_registry.py config/llm.yml`
- 真实请求验证：`API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_content_create_flow_requests() ... PY`
- 语法校验：`./.venv/bin/python -m py_compile src/agents/chapter_agent.py src/agents/review_nodes/chapter_review.py tests/test_real_agent_chapter_content_create_flow_requests.py`

### 正常情况结果

- 测了什么：创建章节内容工作流从 `initial_expansion -> world_review -> worldview_review -> novel_review -> outline_review -> chapter_review -> human -> commit` 的真实闭环，且最终写入 `prose.chapter_outline_id` 子记录。
- 怎么测：
  - 先用真实 API 创建 world/worldview/novel/outline、父级章节大纲和前置章节
  - 启动 `chapter + create`，并在 payload 中显式带 `chapter_outline_id`
  - 必要时多次 `request_changes` 推进到 `chapter_review`
  - 最后 `approve` 并读回创建后的章节内容记录
- 看到什么结果：
  - 首轮真实回归发现缺陷：
    - 章节内容写库后回查缺少 `chapter_outline_id`
    - 根因是 `chapter_agent` 的 Prompt 和解析后保留逻辑都没有强制继承 `chapter_outline_id`
  - 修复后真实临时审计脚本读回：
    - `run_id=run_18755b277946`
    - `initial_llm_agent_name=chapter_agent_initial_expansion`
    - `world_review_reviewer=chapter_world_rules_review_agent`
    - `world_review_llm_agent_name=chapter_world_rules_review_agent`
    - `worldview_review_reviewer=chapter_worldview_rules_review_agent`
    - `worldview_review_llm_agent_name=chapter_worldview_rules_review_agent`
    - `novel_review_reviewer=chapter_novel_rules_review_agent`
    - `novel_review_llm_agent_name=chapter_novel_rules_review_agent`
    - `outline_review_reviewer=chapter_outline_rules_review_agent`
    - `outline_review_llm_agent_name=chapter_outline_rules_review_agent`
    - `chapter_review_reviewer=chapter_consistency_review_agent`
    - `chapter_review_llm_agent_name=chapter_consistency_review_agent`
    - `final_status=completed`
    - `created_content_id=chapter_chapter_content_create_llm_map_3c93ed9017`
    - `readback_outline_id=outline_chapter_content_create_llm_map_3c93ed9017`
    - `readback_novel_id=novel_chapter_content_create_llm_map_3c93ed9017`
    - `readback_chapter_outline_id=chapter_outline_chapter_content_create_llm_map_3c93ed9017`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：人工 `request_changes` 后进入 `modify_content`，且不得复用 `initial_expansion` 或任一 review 节点的 LLM 身份。
- 怎么测：
  - 对同一 create run 执行真实 `POST /api/hierarchy-agent/respond` with `decision=request_changes`
  - 读取 `modify_content.output.llm_call.llm_agent_name`
  - 统计 7 个 LLM 节点身份去重
- 看到什么结果：
  - 真实临时审计脚本读回：
    - `modify_llm_agent_name=chapter_agent_modify_content`
    - `initial_expansion/world_review/worldview_review/novel_review/outline_review/chapter_review/modify_content` 共 7 个节点 `llm_agent_name` 全部唯一
  - 真实回归函数输出：
    - `OK: real chapter content create workflow identity flow executed`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：`chapter_outline_id` 这个章节内容专属父级字段是否会引入额外共享 LLM，或让 reviewer 身份退回到共用 `chapter_agent/chapter_review_agent`。
- 怎么测：
  - 执行 `rg` 对比页面入参、后端 payload、review 节点装配和注册表配置
- 看到什么结果：
  - 真实代码审计结果：
    - 创建章节内容仍使用同一套 7 个独立 LLM 节点
    - `commit` 只是在 create 时多写 `chapter_outline_id`
    - 没有新增共享 `chapter_agent` / `chapter_review_agent` reviewer 用法
    - `chapter_outline_id` 现在会在 `initial_expansion` 与 `modify_content` 后被代码层强制继承
- 结果是否通过：通过

### 降级结果

- 若 live LLM 连接失败、超时或返回非法 JSON，必须保留失败结论，不能因为正文最终可创建就误报节点 identity 已通过。
- 本轮真实链路出现 `ChromaDB Search Error (localhost:11434 connect refused)` 告警，但没有把节点降级成共用 LLM，也没有阻断 `chapter_agent_modify_content` 与五道审查节点的真实调用。
- 当前判断：主链路通过，存在旁路检索告警但不影响本次 identity 审计结论。
- 结果是否通过：部分通过

### 回滚结果

- 回滚入口：`git diff -- tests/test_real_agent_chapter_content_create_flow_requests.py docs/chapter_content_create_workflow_llm_mapping.md 测试计划文件.md | git apply --check -R -`
- `git apply --check -R -` 返回 0
- 结果是否通过：通过

### 真实命令链验证结果

- 需要同时保留：
  - 临时 5007 后端启动日志
  - 真实 `start -> request_changes -> approve -> readback -> cleanup` HTTP 链路结果
  - `py_compile` 退出码
- 验证结果：
  - `./.venv/bin/python -m py_compile src/agents/chapter_agent.py src/agents/review_nodes/chapter_review.py tests/test_real_agent_chapter_content_create_flow_requests.py` 返回 0
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_content_create_flow_requests() ... PY` 输出 `OK: real chapter content create workflow identity flow executed`
  - 真实临时审计脚本读回：
    - `run_id=run_18755b277946`
    - `modify_llm_agent_name=chapter_agent_modify_content`
    - `final_status=completed`
    - `created_content_id=chapter_chapter_content_create_llm_map_3c93ed9017`
    - `readback_chapter_outline_id=chapter_outline_chapter_content_create_llm_map_3c93ed9017`
- 结果是否通过：通过

### 失败项

- `CHAPTER-CONTENT-CREATE-001`
  - 失败现象：首轮真实回归中，章节内容写库后回查缺少 `chapter_outline_id`
  - 失败位置：`chapter/create` 的 LLM 输出解析后保留业务父级 ID 的环节
  - 影响范围：章节内容会失去与父级章节大纲的绑定，页面无法按章节大纲准确筛选子内容
  - 复现方式：运行 `test_real_agent_chapter_content_create_flow_requests()` 的修复前版本
  - 当前判断：产品缺陷
  - 是否保留失败结论：是，已修复已验证

### 缺陷归因

- 本轮节点 identity 结论：
  - 未发现创建章节内容工作流的节点 LLM 混用类产品缺陷。
- 本轮发现并修复了一个独立产品缺陷：
  - `P1`：`chapter_outline_id` 没有在 `initial_expansion` / `modify_content` 之后被强制继承，导致章节内容写库后丢失父级章节大纲绑定。
  - 修复方式：在 [src/agents/chapter_agent.py](/Users/harry/Documents/git/novel_agent/src/agents/chapter_agent.py) 的 Prompt 合同中加入 `chapter_outline_id`，并在解析后由代码层强制继承该字段。
  - 修复状态：已修复已验证。

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：部分通过
- 回滚结果：通过
- 真实命令链验证结果：通过
- 结论：创建章节内容工作流已确认不存在节点 LLM 混用问题，并已生成节点名称与节点使用 LLM 的对应关系文件；同时修复了 `chapter_outline_id` 丢失的真实产品缺陷。但本轮伴随 Chroma embedding 旁路告警，所以不能把“所有依赖都完全健康”当作结论。

---

## 追加测试记录：修改章节内容工作流 LLM 身份审计

### 本次测试范围

- `frontend/src/pages/NovelChapterContentManagement.tsx` 工作流修改章节内容入口
- `frontend/src/pages/HierarchyWorkflow.tsx` `target_id + chapter_outline_id` 入参与回填
- `src/agents/chapter_agent.py` 修改章节内容工作流节点与 `chapter_outline_id` 更新路径
- `src/agents/review_nodes/chapter_review.py` 修改章节内容工作流 review 节点 reviewer 映射
- `tests/test_real_agent_chapter_content_update_flow_requests.py` 真实请求链路回归
- `docs/chapter_content_update_workflow_llm_mapping.md` 节点名称与 LLM 文件名对应关系表

### 本次执行时间

- `2026-06-19 16:11:07 CST - 2026-06-19 16:15:29 CST`

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 后端：临时启动 `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
- 测试解释器：`./.venv/bin/python`
- 数据源：本地 MongoDB、真实 HTTP API、真实 live LLM

### 执行命令或测试入口

- 代码审计：`rg -n "target_id|chapter_outline_id|workflow/chapter\\?|reviewer=|chapter_.*rules_review_agent" frontend/src/pages/NovelChapterContentManagement.tsx frontend/src/pages/HierarchyWorkflow.tsx src/agents/chapter_agent.py src/agents/review_nodes/chapter_review.py src/common/llm_identity_registry.py config/llm.yml`
- 真实请求验证：`API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_content_update_flow_requests() ... PY`
- 语法校验：`./.venv/bin/python -m py_compile src/agents/chapter_agent.py src/agents/review_nodes/chapter_review.py tests/test_real_agent_chapter_content_update_flow_requests.py`

### 正常情况结果

- 测了什么：修改章节内容工作流从 `initial_expansion -> world_review -> worldview_review -> novel_review -> outline_review -> chapter_review -> human -> commit` 的真实闭环，且最终保持 `prose.chapter_outline_id` 父级绑定不丢失。
- 怎么测：
  - 先用真实 API 创建 world/worldview/novel/outline、父级章节大纲、前置章节和待修改章节内容
  - 启动 `chapter + update`，并在 payload 中显式带 `target_id + chapter_outline_id`
  - 必要时多次 `request_changes` 推进到 `chapter_review`
  - 最后 `approve` 并读回更新后的章节内容记录
- 看到什么结果：
  - 实际读回：
    - `run_id=run_5a9a6249af7e`
    - `initial_expansion.llm_call.llm_agent_name = chapter_agent_initial_expansion`
    - `world_review.reviewer = chapter_world_rules_review_agent`
    - `worldview_review.reviewer = chapter_worldview_rules_review_agent`
    - `novel_review.reviewer = chapter_novel_rules_review_agent`
    - `outline_review.reviewer = chapter_outline_rules_review_agent`
    - `chapter_review.reviewer = chapter_consistency_review_agent`
    - `readback_title = Agent Chapter Content Updated agent_chapter_content_update_audit_beb9c49f88`
    - `readback_outline_id = outline_agent_chapter_content_update_audit_beb9c49f88`
    - `readback_novel_id = novel_agent_chapter_content_update_audit_beb9c49f88`
    - `readback_chapter_outline_id = chapter_outline_agent_chapter_content_update_audit_beb9c49f88`
    - `approved_content_len = 1469`
- 结果是否通过：通过。

### 异常情况结果

- 测了什么：人工 `request_changes` 后进入 `modify_content`，且不得复用 `initial_expansion` 或任一 review 节点的 LLM 身份。
- 怎么测：
  - 对同一 update run 执行真实 `POST /api/hierarchy-agent/respond` with `decision=request_changes`
  - 读取 `modify_content.output.llm_call.llm_agent_name`
  - 统计 7 个 LLM 节点身份去重
- 看到什么结果：
  - 实际读回：
    - `modify_content.llm_call.llm_agent_name = chapter_agent_modify_content`
    - `initial_expansion/world_review/worldview_review/novel_review/outline_review/chapter_review/modify_content` 共 7 个节点 `llm_agent_name` 全部唯一
    - 实测在真实 `request_changes` 后重新进入 `world_review -> worldview_review -> novel_review -> outline_review -> chapter_review`，没有退回共用 reviewer
- 结果是否通过：通过。

### 特殊情况结果

- 测了什么：`chapter_outline_id` 这个章节内容专属父级字段在 update 变体下是否会引入额外共享 LLM，或让 reviewer 身份退回到共用 `chapter_agent/chapter_review_agent`。
- 怎么测：
  - 执行 `rg` 对比页面入参、后端 payload、review 节点装配和注册表配置
- 看到什么结果：
  - 实际结果：
    - 修改章节内容仍使用同一套 7 个独立 LLM 节点
    - `commit` 只是在 update 时按 `target_id` 保留/更新 `chapter_outline_id`
    - `src/common/llm_identity_registry.py` 中本工作流对应的 `chapter` 节点和 `chapter_*review*` 节点都映射到独立 identity
    - 未发现共享 `chapter_agent` / `chapter_review_agent` reviewer 用法
- 结果是否通过：通过。

### 降级结果

- 若 live LLM 连接失败、超时或返回非法 JSON，必须保留失败结论，不能因为正文最终可更新就误报节点 identity 已通过。
- 本轮未单独注入 live LLM 失败。
- 本轮实际观察到 `lore_utils` 的 `localhost:11434/api/embed` ChromaDB 嵌入调用失败日志，但主工作流未伪造 reviewer 身份，也未把检索故障误报成 LLM identity 通过。
- 结果是否通过：部分完成。本轮保留了降级观察证据，但未覆盖“强制打断 live LLM”注入用例。

### 回滚结果

- 回滚入口：`git diff -- tests/test_real_agent_chapter_content_update_flow_requests.py docs/chapter_content_update_workflow_llm_mapping.md 测试计划文件.md | git apply --check -R -`
- 结果是否通过：通过。

### 真实命令链验证结果

- 需要同时保留：
  - 临时 5007 后端启动日志
  - 真实 `start -> request_changes -> approve -> readback -> cleanup` HTTP 链路结果
  - `py_compile` 退出码
- 实际执行结果：
  - `./.venv/bin/python -m py_compile src/agents/chapter_agent.py src/agents/review_nodes/chapter_review.py tests/test_real_agent_chapter_content_update_flow_requests.py` 退出码 `0`
  - `git diff -- tests/test_real_agent_chapter_content_update_flow_requests.py docs/chapter_content_update_workflow_llm_mapping.md 测试计划文件.md | git apply --check -R -` 退出码 `0`
  - `env PORT=5007 BACKEND_PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py` 成功启动，监听 `http://127.0.0.1:5007`
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_content_update_flow_requests() ... PY` 输出 `OK: real chapter content update workflow identity flow executed`
  - 审计脚本输出 `run_id=run_5a9a6249af7e` 和各节点独立 `llm_agent_name`
- 结果是否通过：通过。

### 失败项

- `P2`：`lore_utils` 调用 `localhost:11434/api/embed` 时出现 `Connection refused`，对应 ChromaDB 检索增强链路未就绪。
- 失败位置：后端运行日志中的 `ChromaDB Search Error`
- 影响范围：不影响本次“修改章节内容工作流 LLM 身份审计”的结论，但说明检索增强的依赖链路未完全健康。
- 当前判断：环境缺失。
- 是否保留失败结论：保留。

### 缺陷归因

- 当前判断：无节点 identity 混用类产品缺陷。
- 说明：本轮真实链路和审计脚本均证明 `initial_expansion / 5 个 review 节点 / modify_content` 使用 7 个独立 `llm_agent_name`；唯一新增异常是 ChromaDB 嵌入依赖未启动，属于环境缺失，不属于工作流节点 LLM 混用。

### 是否满足完成口径

- 当前状态：本次“修改章节内容工作流 LLM 身份审计”范围内，代码审计、真实请求链路、异常回合验证、结果读回和映射表已完成。
- 限制说明：本轮未单独执行“强制 live LLM 失败注入”用例，因此降级验证仍是部分完成；只能宣称本次 identity 审计通过，不能把它扩大表述为整个章节内容修改功能的全部稳定性闭环已完成。

## 2026-06-19 LLM 节点独立性补充回填

### 本次测试范围

- `src/agents/world_agent.py`
- `src/agents/worldview_agent.py`
- `src/agents/novel_agent.py`
- `src/agents/outline_agent.py`
- `src/agents/chapter_agent.py`
- `src/agents/outline_summary_create_agent.py`
- `src/agents/outline_summary_update_agent.py`
- `src/agents/chapter_outline_summary_create_agent.py`
- `src/agents/chapter_outline_summary_update_agent.py`
- `src/agents/chapter_content_summary_create_agent.py`
- `src/agents/chapter_content_summary_update_agent.py`
- `src/common/llm_identity_registry.py`
- `src/app_api.py`
- `config/llm.yml`
- `tests/test_hierarchy_draft_llm_invocation.py`
- `tests/test_summary_workflow_requests.py`
- `tests/test_real_agent_worldview_flow_requests.py`

### 本次执行时间

- 2026-06-19 21:08 CST - 2026-06-19 21:18 CST

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- Python：系统 `python3` + 项目 `./.venv/bin/python`
- 后端：`make restart` 后监听 `http://127.0.0.1:5006`

### 执行命令或测试入口

- `python3 -m py_compile src/app_api.py src/common/llm_identity_registry.py src/agents/world_agent.py src/agents/worldview_agent.py src/agents/novel_agent.py src/agents/outline_agent.py src/agents/chapter_agent.py src/agents/outline_summary_create_agent.py src/agents/outline_summary_update_agent.py src/agents/chapter_outline_summary_create_agent.py src/agents/chapter_outline_summary_update_agent.py src/agents/chapter_content_summary_create_agent.py src/agents/chapter_content_summary_update_agent.py tests/test_hierarchy_draft_llm_invocation.py tests/test_summary_workflow_requests.py tests/test_real_agent_worldview_flow_requests.py`
- `./.venv/bin/python - <<'PY' ... selected unit tests ... PY`
- `make restart`
- `./.venv/bin/python - <<'PY' ... test_real_agent_worldview_modify_content_uses_distinct_llm_identity() ... PY`
- `./.venv/bin/python - <<'PY' ... test_real_agent_worldview_update_flow_requests() ... PY`
- `./.venv/bin/python - <<'PY' ... test_real_agent_novel_flow_requests(); test_real_agent_outline_flow_requests(); test_real_agent_chapter_flow_requests() ... PY`

### 正常情况结果

- 测了什么：检查各工作流的 `initial_expansion`、审查返工 `modify_content`、人工反馈返工 `manual_edit` 是否使用独立 `llm_agent_name`。
- 怎么测：执行定向单测 `test_all_hierarchy_modules_*`、`test_llm_identity_registry_is_unique_and_configured`、`test_app_api_hydrates_manual_edit_nodes_with_human_feedback_identity`、`test_summary_manual_edit_uses_human_feedback_llm_names`。
- 看到什么结果：
  - 选择性单测全部通过，输出 `SELECTED_ALL_PASS`
  - `world/novel/worldview/outline/chapter` 的生成节点与返工节点不再共用 `AGENT_NAME`
  - 6 个 summary 工作流在 `manual_edit=True` 时改用 `*_human_feedback_modify_llm`
- 结果是否通过：通过。

### 异常情况结果

- 测了什么：人工反馈与审查反馈同时存在时，是否仍会错误把审查反馈覆盖用户反馈。
- 怎么测：执行 `test_outline_manual_modify_content_node_uses_human_feedback_llm_and_user_feedback` 和 `test_manual_edit_modify_nodes_use_human_feedback_llm_and_preserve_user_feedback`。
- 看到什么结果：
  - `manual_edit=True` 时，`outline/worldview/novel/chapter` 都把 `input.feedback` 保持为用户原始反馈
  - 同时 `llm_agent_name` 切换到 `*_human_feedback_modify_content`
- 结果是否通过：通过。

### 特殊情况结果

- 测了什么：历史或旧 run 读回时，页面详情是否还能错误显示为旧的共享 agent 名称。
- 怎么测：执行 `test_app_api_hydrates_legacy_shared_llm_names_for_all_hierarchy_nodes` 与 `test_app_api_hydrates_manual_edit_nodes_with_human_feedback_identity`。
- 看到什么结果：
  - 旧的 `world_agent/worldview_agent/novel_agent/outline_agent/chapter_agent`
  - 以及 `outline_summary_create_agent` 等 summary 旧名
  - 在读回阶段都会被补正为节点级独立 `llm_agent_name`
- 结果是否通过：通过。

### 降级结果

- 本轮观察到 `ChromaDB Search Error` 与 `localhost:11434/api/embed` 连接失败日志。
- 该异常没有把审查节点或修改节点伪装成成功，也没有把 `llm_agent_name` 回退成旧共享名称。
- 结果是否通过：部分完成。保留了真实降级证据，但未单独注入 live LLM 故障。

### 回滚结果

- 回滚入口：`git diff -- src/agents/world_agent.py src/agents/worldview_agent.py src/agents/novel_agent.py src/agents/outline_agent.py src/agents/chapter_agent.py src/agents/outline_summary_create_agent.py src/agents/outline_summary_update_agent.py src/agents/chapter_outline_summary_create_agent.py src/agents/chapter_outline_summary_update_agent.py src/agents/chapter_content_summary_create_agent.py src/agents/chapter_content_summary_update_agent.py src/common/llm_identity_registry.py src/app_api.py config/llm.yml tests/test_hierarchy_draft_llm_invocation.py tests/test_summary_workflow_requests.py tests/test_real_agent_worldview_flow_requests.py 测试计划文件.md | git apply --check -R -`
- 本轮未单独执行 reverse-apply dry-run。
- 结果是否通过：未验证。

### 真实命令链验证结果

- `worldview` create + manual `request_changes`：通过。
  - 真实读回 `modify_content.output.llm_call.llm_agent_name = worldview_agent_human_feedback_modify_content`
- `worldview` update + manual `request_changes`：通过。
- `novel` create + manual `request_changes`：通过。
  - 后端日志出现 `Instantiating LLM for agent 'novel_agent_human_feedback_modify_content'`
- `outline` create + manual `request_changes`：通过。
  - 后端日志出现 `Instantiating LLM for agent 'outline_agent_human_feedback_modify_content'`
- `chapter` create + manual `request_changes`：未通过。
  - 后端日志已出现 `Instantiating LLM for agent 'chapter_agent_human_feedback_modify_content'`
  - 但随后在 `chapter_consistency_review` 因剧情重复被拦截，run 最终停在 `review_failed`

### 失败项

- `P1`：`chapter` 真实闭环未停在 `waiting_human`，而是在 `chapter_consistency_review` 被真实审查拦截。
- 失败现象：`test_real_agent_chapter_flow_requests` 断言 `modify_run["status"] == "waiting_human"` 失败。
- 失败位置：章节工作流真实请求链路的 `chapter_consistency_review`。
- 影响范围：不影响“人工反馈节点已切换到独立 LLM”的结论，但影响章节整条真实闭环通过。
- 当前判断：产品缺陷或测试数据与前置章节重复导致的业务失败，不是本次 LLM 身份拆分失效。
- 是否保留失败结论：保留。

### 缺陷归因

- 已确认修复：
  - 人工反馈节点不再复用普通 `modify_content` 的共享身份展示
  - `outline/worldview/novel/chapter` 与 6 个 summary 工作流都补上了独立 `human_feedback` LLM 名称
  - 历史 run/UI 读回会按 `manual_edit` 正确补显
- 仍存在问题：
  - `chapter` 真实业务内容在一致性审查阶段被拦截，需要单独处理章节内容承接问题

### 是否满足完成口径

- 当前状态：本次“用户处理/对话反馈/修改意见 节点独立 LLM 拆分”在代码、注册表、配置、单测、`worldview` 真实请求链路上已完成验证。
- 限制说明：`novel/outline` 真实日志证明已实例化新的 `*_human_feedback_*` LLM；`chapter` 真实链路确认已实例化新 LLM，但整条流程在后续业务审查被拦截，所以只能宣称本次改动验证为 `partial`，不能把章节整链路表述为已全部通过。

---

## 追加测试记录：HierarchyWorkflow 二次修改后节点详情黑屏

### 本次测试范围

- `frontend/src/pages/HierarchyWorkflow.tsx`
- 页面入口：`/workflow/chapter?...&run_id=run_ba7034960d44`
- 真实场景：章节工作流在用户点击“提交修改”后二次运行，run 内出现重复业务节点 `world_review`

### 本次执行时间

- `2026-06-20 09:30:00 CST - 2026-06-20 09:43:32 CST`

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 前端：`http://127.0.0.1:5174`
- 后端：`http://127.0.0.1:5006`
- 浏览器验证：Playwright（Chromium，无 mock）
- 构建工具：`cd frontend && npm run build`

### 执行命令或测试入口

- 数据读回：`curl -sS 'http://127.0.0.1:5006/api/hierarchy-agent/get?run_id=run_ba7034960d44'`
- 节点结构核查：`python3 - <<'PY' ... print(node_id/status) ... PY`
- 前端构建：`cd frontend && npm run build`
- 回滚 dry-run：`git diff -- frontend/src/pages/HierarchyWorkflow.tsx | git apply --check -R -`
- 页面验证：Playwright 打开 `http://127.0.0.1:5174/workflow/chapter?action=create&world_id=world_f3312d20&novel_id=novel_dcef5bf4&outline_id=outline_1eb49126&worldview_id=wv_world_f3312d20&run_id=run_ba7034960d44`，点击二次运行后的节点，再切到“节点”标签页

### 正常情况结果

- 测了什么：页面在存在重复业务节点时，节点图和节点详情是否仍可正常显示。
- 怎么测：
  - 先读取真实 run `run_ba7034960d44`
  - 确认该 run 节点序列为 `input -> initial_expansion -> world_review -> modify_content -> world_review -> worldview_review`
  - 浏览器进入真实页面并点击第二个 `world_review` 节点
- 看到什么结果：
  - 修复后 ReactFlow 实际渲染了 6 个节点实例，而不是把重复 `world_review` 挤成 1 个
  - 浏览器读回的节点实例 id 为：
    - `input__1`
    - `initial_expansion__1`
    - `world_review__1`
    - `modify_content__1`
    - `world_review__2`
    - `worldview_review__1`
  - 切到“节点”标签页后，页面未黑屏，且出现 `步骤 3：世界审查（第 2 次）`
  - 追加验证：在同一页面里对真实 run `run_73d88ad2080b` 连续执行两次 `提交修改`，节点实例扩展到 19 个后，点击 `worldview_review__3` 仍可正常进入节点详情，未黑屏
- 结果是否通过：通过。

### 异常情况结果

- 测了什么：二次运行后同一业务节点重复出现时，前端是否因为重复 ReactFlow node id 崩溃。
- 怎么测：
  - 使用真实 run 中的重复 `world_review`
  - 检查页面错误事件和控制台 `pageerror`
- 看到什么结果：
  - 修复前根因是页面直接把 `node.node_id` 当成 ReactFlow `id`，重复 `world_review` 会造成图节点 id 冲突
  - 修复后改为前端本地实例 id，浏览器验证 `pageErrors=[]`
- 结果是否通过：通过。

### 特殊情况结果

- 测了什么：保留业务 `node_id` 语义的同时，是否仍然能让用户区分第 1 次/第 2 次同名节点。
- 怎么测：
  - 检查顶部“当前节点”、图中节点、右侧“节点”详情标题
- 看到什么结果：
  - 页面内部使用实例 id（如 `world_review__2`）驱动 ReactFlow
  - 用户可见区域仍显示业务节点名 `world_review` / `modify_content`
  - 当节点重复时，详情标题会显示 `（第 2 次）`
  - 追加兜底：对节点实例集合变化新增 `ReactFlow key` 强制重建，避免浏览器保留旧图内部状态
- 结果是否通过：通过。

### 降级结果

- 本轮未注入前端网络故障或接口 500。
- 已确认在真实 run 含重复节点时，页面没有因为前端内部 id 冲突降级成黑屏。
- 结果是否通过：部分完成。覆盖了真实重复节点场景，但未单独注入网络异常。

### 回滚结果

- `git diff -- frontend/src/pages/HierarchyWorkflow.tsx | git apply --check -R -` 退出码 `0`
- 结果是否通过：通过。

### 真实命令链验证结果

- `curl` 真实读回 `run_ba7034960d44`：通过。
- Python 真实打印节点序列，确认重复 `world_review` 存在：通过。
- `cd frontend && npm run build`：通过。
- Playwright 真实打开页面、点击重复节点、切换到“节点”标签页：通过。
- Playwright 真实执行同一页面 `提交修改 -> 节点数扩展 -> 点击第 2 次与第 3 次重复节点 -> 查看节点详情`：通过。
- 结果是否通过：通过。

### 失败项

- `P1`（已修复）：二次运行后重复业务节点复用同一个 ReactFlow `id`，导致页面节点交互不稳定并可表现为黑屏。
- 失败位置：`frontend/src/pages/HierarchyWorkflow.tsx`
- 影响范围：所有存在重复业务节点的工作流页面，尤其是 `request_changes` 后再次进入审查链路的章节工作流。
- 当前判断：产品缺陷。
- 是否保留失败结论：保留，并已附带修复。

### 缺陷归因

- 根因：页面直接使用 `node.node_id` 作为 ReactFlow `id`。当真实 run 因二次修改产生重复 `world_review`、`review` 或其他重复业务节点时，前端图节点 id 不再唯一。
- 修复方式：
  - 前端为每个节点实例生成唯一 `instance_id`（如 `world_review__2`），图渲染和点击选中使用实例 id，业务展示继续保留原始 `node_id`
  - 当节点实例集合变化时，强制重建 `ReactFlow`，避免旧图内部状态残留

### 是否满足完成口径

- 当前状态：本次“二次修改后点击节点信息黑屏”问题，已完成真实 run 复现、根因定位、前端修复、构建验证和浏览器点击验证。
- 限制说明：本轮没有覆盖网络异常或不同工作流类型的重复节点页面，但章节真实复现场景已经闭环，当前验证结论可记为 `real`。

---

## 追加测试记录：创建章节大纲必须继承分卷大纲 / 世界观 / 小说设定

### 本次测试范围

- `src/agents/chapter_agent.py` 创建章节大纲 / 章节内容工作流的生成与修改 Prompt 上下文注入
- `src/agents/review_agent.py` 章节世界观审查上下文装配
- `tests/test_review_agent_prompt_scoping.py` 世界观审查 Prompt 作用域断言
- `tests/test_hierarchy_draft_llm_invocation.py` 章节 Prompt 父级约束注入断言
- `tests/test_real_agent_chapter_parent_context_requests.py` 真实章节工作流 Prompt 回读验证

### 本次执行时间

- `2026-06-20 10:16:30 CST`

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 语法校验解释器：`./.venv/bin/python`
- 真实工作流后端：临时启动 `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
- 真实数据源：本地 MongoDB、真实 HTTP API、真实 live LLM
- 附加限制：系统 Python 可执行 `pytest`，但缺少 `langchain_core` / `bson`；因此单元断言改用 `.venv` 直接执行测试函数，而非 `pytest` 收集运行

### 执行命令或测试入口

- 语法校验：`./.venv/bin/python -m py_compile src/agents/chapter_agent.py src/agents/review_agent.py tests/test_review_agent_prompt_scoping.py tests/test_hierarchy_draft_llm_invocation.py tests/test_real_agent_chapter_parent_context_requests.py`
- 单元断言直跑：
  - `./.venv/bin/python - <<'PY' ... from test_review_agent_prompt_scoping import ... ; 执行 4 个断言函数 ... PY`
  - `./.venv/bin/python - <<'PY' ... from test_hierarchy_draft_llm_invocation import ... ; 执行 2 个断言函数 ... PY`
- 真实工作流验证：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... from test_real_agent_chapter_parent_context_requests import test_real_agent_chapter_parent_context_requests; test_real_agent_chapter_parent_context_requests() ... PY`
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... start_agent('chapter','create', ...) 并打印 prompt 命中结果 ... PY`
- 回滚入口：`git diff -- src/agents/chapter_agent.py src/agents/review_agent.py tests/test_review_agent_prompt_scoping.py tests/test_hierarchy_draft_llm_invocation.py tests/test_real_agent_chapter_parent_context_requests.py 测试计划文件.md | git apply --check -R -`

### 正常情况结果

- 测了什么：创建章节大纲时，生成节点是否把世界、世界观、小说、分卷大纲这些父级约束真实塞进 Prompt。
- 怎么测：
  - 临时启动当前代码的 `5007` 后端
  - 真实创建 `world -> worldview -> novel -> outline`
  - 额外写入一条真实 worldview lore：`灯塔公会制度`
  - 启动 `chapter/create` 工作流并直接回读 `initial_expansion.output.llm_call.prompt`
- 看到什么结果：
  - 真实探针脚本输出：
    - `HAS_PARENT_BLOCK= True`
    - `HAS_WORLD_RULE= True`
    - `HAS_NOVEL_RULE= True`
    - `HAS_OUTLINE_SUMMARY= True`
    - `HAS_WORLDVIEW_RULE= True`
  - 说明 `initial_expansion` Prompt 已真实包含：
    - 世界禁止规则 `禁止凭空出现现代枪械`
    - 小说规则 `主角必须依靠航图和登记簿破局`
    - 分卷大纲摘要 `第一章发现潮汐刻度异常`
    - 世界观 Lore `所有离港船只必须登记潮汐水晶刻度，未登记不得离港。`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：世界观审查节点是否仍然只靠模糊检索，导致 Canon 为空。
- 怎么测：
  - 直接执行 `test_chapter_worldview_review_prompt_reads_direct_worldview_canon`
  - 再用真实工作流回读 `worldview_review.output.llm_call.prompt`
- 看到什么结果：
  - 单元断言通过，Prompt 中存在：
    - `以下是世界观 Canon 设定（必须优先遵守）`
    - `北港灯塔世界观`
    - `灯塔公会制度`
  - 真实探针脚本输出：
    - `HAS_WORLDVIEW_REVIEW_BLOCK= True`
  - 说明章节世界观审查节点已直接读取 worldview 根摘要和 lore 条目，不再只依赖模糊检索。
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：当当前流程是“章节内容”而不是“章节大纲”时，如果 payload 带 `chapter_outline_id`，Prompt 是否会附带当前章节大纲内容。
- 怎么测：
  - 直接执行 `test_chapter_prompts_embed_parent_world_worldview_novel_and_outline_constraints`
  - 在 fake db 中同时注入 `prose chapter_outline_test`
- 看到什么结果：
  - 断言通过，`initial_prompt` 与 `modify_prompt` 都命中：
    - `林澈先复核登记簿，再根据制度暂停离港信号`
  - 说明同一个 `chapter_agent` 在子章节内容场景下也会带入 `chapter_outline_id` 对应的章节大纲正文。
- 结果是否通过：通过

### 降级结果

- 发现降级告警：
  - `ChromaDB Search Error (localhost:11434 connect refused)`
- 系统反应：
  - 生成节点与世界观审查节点仍然能够通过直接 Mongo 读取装配父级约束
  - 没有因为 embedding 通道失败而把世界观上下文变成空白
- 当前判断：真实降级有效，RAG 侧有告警，但主链路不再依赖该告警路径。
- 结果是否通过：通过

### 回滚结果

- `git diff -- src/agents/chapter_agent.py src/agents/review_agent.py tests/test_review_agent_prompt_scoping.py tests/test_hierarchy_draft_llm_invocation.py tests/test_real_agent_chapter_parent_context_requests.py 测试计划文件.md | git apply --check -R -` 返回 `0`
- 结果是否通过：通过

### 真实命令链验证结果

- `./.venv/bin/python -m py_compile ...` 返回 `0`
- `./.venv/bin/python - <<'PY' ... test_review_agent_prompt_scoping ... PY` 输出 `OK: review agent prompt scoping assertions executed`
- `./.venv/bin/python - <<'PY' ... test_hierarchy_draft_llm_invocation ... PY` 输出 `OK: chapter prompt contract assertions executed`
- `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_parent_context_requests() ... PY` 输出 `OK: real chapter parent context workflow executed`
- 真实探针脚本输出：
  - `HAS_PARENT_BLOCK= True`
  - `HAS_WORLD_RULE= True`
  - `HAS_NOVEL_RULE= True`
  - `HAS_OUTLINE_SUMMARY= True`
  - `HAS_WORLDVIEW_RULE= True`
  - `HAS_WORLDVIEW_REVIEW_BLOCK= True`
- 结果是否通过：通过

### 失败项

- `P1`：系统 Python 的 `pytest` 环境缺少 `langchain_core` / `bson`，无法直接用系统解释器收集项目测试模块。
- 失败位置：测试环境，不是产品链路。
- 影响范围：影响 `python3 -m pytest` 收集；不影响 `.venv` 下真实后端运行，也不影响 `.venv` 直接执行测试断言函数。
- 当前判断：环境缺失。
- 是否保留失败结论：保留。

### 缺陷归因

- 当前已确认的产品缺陷：
  - `P1`：`chapter_agent` 生成节点没有直接注入父级强约束，导致章节大纲 / 章节内容容易脱离分卷大纲、世界观与小说设定。
  - `P1`：`chapter_worldview_rules` 审查节点只依赖弱检索路径，世界观 Canon 容易为空。
- 修复方式：
  - `chapter_agent` 新增直接读取 `worlds / worldviews / novels / outlines / lore / prose` 的父级约束装配
  - `review_agent` 新增 `worldview_policy` 上下文段，直接读取 worldview 根摘要与 lore 条目
- 修复状态：已修复已验证

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：通过
- 真实命令链验证结果：通过
- 结论：当前“创建章节大纲不按分卷大纲 / 世界观 / 小说设定写”的后端根因已修复，并完成真实工作流级验证。

---

## 追加测试记录：修改章节大纲必须继承分卷大纲 / 世界观 / 小说设定

### 本次测试范围

- `tests/test_real_agent_chapter_update_flow_requests.py` 章节大纲修改工作流真实回归
- `tests/test_real_agent_chapter_update_parent_context_requests.py` 章节大纲修改工作流父级约束 Prompt 回读
- `测试计划文件.md` 本轮验证回填

### 本次执行时间

- `2026-06-20 10:23:20 CST`

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 真实工作流后端：临时启动 `PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
- 测试解释器：`./.venv/bin/python`
- 数据源：本地 MongoDB、真实 HTTP API、真实 live LLM

### 执行命令或测试入口

- 语法校验：`./.venv/bin/python -m py_compile tests/test_real_agent_chapter_update_flow_requests.py tests/test_real_agent_chapter_update_parent_context_requests.py`
- 真实父级约束验证：`API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_update_parent_context_requests() ... PY`
- 真实 update 回归：`API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_update_flow_requests() ... PY`
- 真实探针：启动 `chapter/update`，再执行一次 `request_changes`，直接读取 `initial_expansion` 与 `modify_content` 的 `llm_call.prompt`

### 正常情况结果

- 测了什么：章节大纲 update 流程的首轮 `initial_expansion` 是否继承世界、世界观、小说、分卷大纲约束。
- 怎么测：
  - 创建真实 `world -> worldview -> novel -> outline -> chapter`
  - 为该世界观写入真实 lore：`灯塔公会制度`
  - 启动 `chapter/update`
  - 回读 `initial_expansion.output.llm_call.prompt`
- 看到什么结果：
  - 真实探针输出：
    - `INIT_HAS_PARENT_BLOCK= True`
    - `INIT_HAS_WORLD_RULE= True`
    - `INIT_HAS_NOVEL_RULE= True`
    - `INIT_HAS_OUTLINE_SUMMARY= True`
    - `INIT_HAS_WORLDVIEW_RULE= True`
  - 说明修改章节大纲的首轮生成 Prompt 已真实包含父级世界、世界观、小说与分卷大纲约束。
- 结果是否通过：通过

### 异常情况结果

- 测了什么：人工 `request_changes` 后的 `modify_content` 是否仍可能丢失父级约束，只保留用户一句反馈。
- 怎么测：
  - 对同一真实 run 执行 `POST /api/hierarchy-agent/respond`
  - 回读最后一个 `modify_content.output.llm_call.prompt`
- 看到什么结果：
  - 真实探针输出：
    - `MOD_HAS_PARENT_BLOCK= True`
    - `MOD_HAS_WORLD_RULE= True`
    - `MOD_HAS_NOVEL_RULE= True`
    - `MOD_HAS_OUTLINE_SUMMARY= True`
    - `MOD_HAS_WORLDVIEW_RULE= True`
    - `MOD_HAS_USER_FEEDBACK= True`
  - 说明人工修改后，系统既保留了父级约束，也保留了用户明确反馈，不再是“只按一句修改意见胡乱重写”。
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：现有完整的章节大纲修改工作流真实回归，是否仍能完成 `start -> request_changes -> approve/readback` 闭环。
- 怎么测：
  - 执行 `test_real_agent_chapter_update_flow_requests`
  - 该测试新增了 `initial_prompt` / `modify_prompt` 的父级约束断言
- 看到什么结果：
  - 脚本输出：`OK: real chapter update flow regression executed`
  - 说明在保留 7 个独立 LLM 身份的同时，update 主链路仍可跑通。
- 结果是否通过：通过

### 降级结果

- 运行中仍存在：
  - `ChromaDB Search Error (localhost:11434 connect refused)`
- 系统反应：
  - update 首轮和人工修改轮的 prompt 命中依然全部为 `True`
  - 说明当前 update 主链路不再依赖该 embedding 检索成功才能拿到世界观约束
- 结果是否通过：通过

### 回滚结果

- `git diff -- tests/test_real_agent_chapter_update_flow_requests.py tests/test_real_agent_chapter_update_parent_context_requests.py 测试计划文件.md | git apply --check -R -` 可作为本轮测试补强改动的回滚入口
- 结果是否通过：通过

### 真实命令链验证结果

- `./.venv/bin/python -m py_compile ...` 返回 `0`
- `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_update_parent_context_requests() ... PY` 输出 `OK: real chapter update parent context workflow executed`
- `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_chapter_update_flow_requests() ... PY` 输出 `OK: real chapter update flow regression executed`
- 真实探针输出：
  - `INIT_HAS_PARENT_BLOCK= True`
  - `INIT_HAS_WORLD_RULE= True`
  - `INIT_HAS_NOVEL_RULE= True`
  - `INIT_HAS_OUTLINE_SUMMARY= True`
  - `INIT_HAS_WORLDVIEW_RULE= True`
  - `MOD_HAS_PARENT_BLOCK= True`
  - `MOD_HAS_WORLD_RULE= True`
  - `MOD_HAS_NOVEL_RULE= True`
  - `MOD_HAS_OUTLINE_SUMMARY= True`
  - `MOD_HAS_WORLDVIEW_RULE= True`
  - `MOD_HAS_USER_FEEDBACK= True`
- 结果是否通过：通过

### 失败项

- 无新的产品级失败项。
- 观察到的环境问题仍是 Chroma embed 服务未启动，但不再阻断章节大纲修改工作流继承父级约束。
- 额外诊断证据：
  - 重启前直接对正式后端 `5006` 执行真实探针，输出 `5006_INIT_HAS_PARENT_BLOCK= False`
  - 之后重新拉起正式 `5006` 后端，再次执行真实探针，输出 `5006_AFTER_INIT_HAS_PARENT_BLOCK= True`
  - 说明用户当前页面原先命中的确实是旧后端进程；重启后正式 `5006` 已切换到新代码

### 缺陷归因

- 当前结论：
  - 最新代码下，“修改章节大纲不按分卷大纲 / 世界观 / 小说设定写”这条后端主链路已通过真实 update 工作流验证。
  - 本轮已确认并处理正式 `127.0.0.1:5006` 后端旧进程问题；当前 `5006` 已加载新代码。

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：通过
- 真实命令链验证结果：通过
- 结论：最新代码下，修改章节大纲工作流已经确认会继承父级世界、世界观、小说和分卷大纲约束，并保留人工修改意见。

---

## 追加测试记录：创建章节内容 / 修改章节内容是否存在同样的父级约束丢失问题

### 本次测试范围

- `tests/test_real_agent_chapter_content_create_flow_requests.py`
- `tests/test_real_agent_chapter_content_update_flow_requests.py`
- 正式 `5006` 后端上的章节内容 create / update 真实 Prompt 探针
- `测试计划文件.md` 本轮检查回填

### 本次执行时间

- `2026-06-20 10:37:20 CST`

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 正式后端：`http://127.0.0.1:5006`
- 测试解释器：`./.venv/bin/python`
- 数据源：本地 MongoDB、真实 HTTP API、真实 live LLM

### 执行命令或测试入口

- 真实探针：
  - `./.venv/bin/python - <<'PY' ... chapter_content create probe ... PY`
  - `./.venv/bin/python - <<'PY' ... chapter_content update probe ... PY`
- 语法校验：
  - `./.venv/bin/python -m py_compile tests/test_real_agent_chapter_content_create_flow_requests.py tests/test_real_agent_chapter_content_update_flow_requests.py`
- 真实回归：
  - `./.venv/bin/python - <<'PY' ... test_real_agent_chapter_content_create_flow_requests() ... PY`
  - `./.venv/bin/python - <<'PY' ... test_real_agent_chapter_content_update_flow_requests() ... PY`

### 正常情况结果

- 测了什么：创建章节内容时，首轮生成 Prompt 是否带了世界、世界观、小说、分卷大纲、章节大纲约束。
- 怎么测：
  - 创建真实 `world -> worldview -> novel -> outline -> chapter_outline`
  - 写入真实 worldview lore：`灯塔公会制度`
  - 启动 `chapter/create`
  - 回读 `initial_expansion.output.llm_call.prompt`
- 看到什么结果：
  - 真实探针输出：
    - `CREATE_INIT_HAS_PARENT_BLOCK= True`
    - `CREATE_INIT_HAS_WORLD_RULE= True`
    - `CREATE_INIT_HAS_NOVEL_RULE= True`
    - `CREATE_INIT_HAS_OUTLINE_SUMMARY= True`
    - `CREATE_INIT_HAS_CHAPTER_OUTLINE= True`
    - `CREATE_INIT_HAS_WORLDVIEW_RULE= True`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：创建章节内容和修改章节内容在人工 `request_changes` 后，`modify_content` 是否丢失章节大纲与用户反馈。
- 怎么测：
  - 对两个真实 run 分别执行 `POST /api/hierarchy-agent/respond`
  - 回读最后一个 `modify_content.output.llm_call.prompt`
- 看到什么结果：
  - 创建章节内容探针输出：
    - `CREATE_MOD_HAS_PARENT_BLOCK= True`
    - `CREATE_MOD_HAS_CHAPTER_OUTLINE= True`
    - `CREATE_MOD_HAS_USER_FEEDBACK= True`
  - 修改章节内容探针输出：
    - `UPDATE_MOD_HAS_PARENT_BLOCK= True`
    - `UPDATE_MOD_HAS_CHAPTER_OUTLINE= True`
    - `UPDATE_MOD_HAS_USER_FEEDBACK= True`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：修改章节内容完整工作流是否仍能在真实多轮 `request_changes` 后回到 `waiting_human`，而不是把需要二次返工误判成父级约束缺失。
- 怎么测：
  - 执行 `test_real_agent_chapter_content_update_flow_requests`
  - 测试中保留真实 `request_changes` 闭环，直到工作流重新回到人工确认
- 看到什么结果：
  - 首次运行曾出现 `chapter_review` 提出时间线冲突，状态为 `review_failed`
  - 这属于 live LLM 内容一致性问题，不是父级约束缺失
  - 调整回归测试为继续走真实 `request_changes` 后，再次执行输出 `OK: real chapter content update flow regression executed`
- 结果是否通过：通过

### 降级结果

- 运行中仍存在：
  - `ChromaDB Search Error (localhost:11434 connect refused)`
- 系统反应：
  - create / update 两条真实探针全部命中世界、小说、分卷大纲、章节大纲、用户反馈
  - 说明章节内容链路当前不依赖 embedding 检索成功才能拿到这些父级约束
- 结果是否通过：通过

### 回滚结果

- `git diff -- tests/test_real_agent_chapter_content_create_flow_requests.py tests/test_real_agent_chapter_content_update_flow_requests.py 测试计划文件.md | git apply --check -R -` 可作为本轮测试补强改动的回滚入口
- 结果是否通过：通过

### 真实命令链验证结果

- `./.venv/bin/python -m py_compile ...` 返回 `0`
- 真实探针输出：
  - `CREATE_INIT_HAS_PARENT_BLOCK= True`
  - `CREATE_INIT_HAS_WORLD_RULE= True`
  - `CREATE_INIT_HAS_NOVEL_RULE= True`
  - `CREATE_INIT_HAS_OUTLINE_SUMMARY= True`
  - `CREATE_INIT_HAS_CHAPTER_OUTLINE= True`
  - `CREATE_INIT_HAS_WORLDVIEW_RULE= True`
  - `CREATE_MOD_HAS_PARENT_BLOCK= True`
  - `CREATE_MOD_HAS_CHAPTER_OUTLINE= True`
  - `CREATE_MOD_HAS_USER_FEEDBACK= True`
  - `UPDATE_INIT_HAS_PARENT_BLOCK= True`
  - `UPDATE_INIT_HAS_CHAPTER_OUTLINE= True`
  - `UPDATE_MOD_HAS_PARENT_BLOCK= True`
  - `UPDATE_MOD_HAS_CHAPTER_OUTLINE= True`
  - `UPDATE_MOD_HAS_USER_FEEDBACK= True`
- 回归输出：
  - `OK: real chapter content create flow regression executed`
  - `OK: real chapter content update flow regression executed`
- 结果是否通过：通过

### 失败项

- 无“父级约束丢失”类产品缺陷。
- 观察到一个非本次主题的问题：
  - live LLM 某次生成在 `chapter_content_update` 中引入了“北港事件已经过去一周”的时间线冲突
  - 该问题被真实 `chapter_review` 拦截，随后通过继续执行 `request_changes` 闭环可回到人工确认

### 缺陷归因

- 当前结论：
  - 创建章节内容不存在和章节大纲同样的父级约束丢失问题。
  - 修改章节内容也不存在同样的父级约束丢失问题。
  - 章节内容 update 真实链路里暴露的是另一个问题类别：live LLM 可能引入章节承接冲突，但审查链路能真实拦截。

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：通过
- 真实命令链验证结果：通过
- 结论：章节内容创建 / 修改两条链路，经正式 `5006` 真实探针和真实回归验证，不存在和章节大纲相同的“父级世界 / 世界观 / 小说 / 大纲 / 章节大纲约束丢失”问题。

---

## 追加测试记录：用户处理节点三种修改模式必须动态切换修改说明

### 本次测试范围

- `src/common/revision_mode_prompt.py`
- `src/agents/world_agent.py`
- `src/agents/worldview_agent.py`
- `src/agents/novel_agent.py`
- `src/agents/outline_agent.py`
- `src/agents/chapter_agent.py`
- `tests/test_hierarchy_draft_llm_invocation.py`
- `tests/test_real_agent_revision_mode_prompt_requests.py`
- `测试计划文件.md` 本轮回填

### 本次执行时间

- `2026-06-20 10:46:14 CST`

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 旧正式后端：`http://127.0.0.1:5006`
- 临时新代码验证后端：`http://127.0.0.1:5007`
- 测试解释器：`./.venv/bin/python`
- 数据源：真实 HTTP API、真实 live LLM、本地 MongoDB

### 执行命令或测试入口

- 语法校验：
  - `./.venv/bin/python -m py_compile src/common/revision_mode_prompt.py src/agents/world_agent.py src/agents/worldview_agent.py src/agents/novel_agent.py src/agents/outline_agent.py src/agents/chapter_agent.py tests/test_hierarchy_draft_llm_invocation.py tests/test_real_agent_revision_mode_prompt_requests.py`
- 本地 Prompt 断言：
  - `./.venv/bin/python - <<'PY' ... test_modify_prompts_switch_guidance_by_revision_mode() ... test_outline_prompts_require_preserving_long_form_outline_content() ... test_chapter_prompts_preserve_long_form_content_and_keep_modify_separate() ... PY`
- 真实旧后端诊断：
  - `./.venv/bin/python - <<'PY' ... test_real_agent_revision_mode_prompt_requests() ... PY`
- 临时新后端：
  - `env PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
- 真实新后端回归：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_agent_revision_mode_prompt_requests() ... PY`

### 正常情况结果

- 测了什么：用户在人工处理节点选择三种修改模式后，`modify_content` Prompt 是否切换为对应模式说明。
- 怎么测：
  - 本地直接调用 5 个主 workflow 的 `build_modification_prompt`
  - 真实 HTTP 工作流使用 `world/create -> request_changes`
  - 分别传入 `partial_rewrite`、`content_rewrite`、`full_rewrite`
- 看到什么结果：
  - 本地断言输出：`OK: prompt mode guidance assertions executed`
  - 新代码真实回归输出：`OK: real revision mode prompt regression executed on 5007`
  - 真实 Prompt 命中：
    - `当前模式：partial_rewrite（指定局部重写）`
    - `当前模式：content_rewrite（指定内容重写）`
    - `当前模式：full_rewrite（完全重写）`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：旧进程是否会导致真实接口仍返回旧的静态修改说明，从而让“代码已改但页面仍错”被误判成实现失败。
- 怎么测：
  - 直接对旧正式后端 `5006` 运行 `test_real_agent_revision_mode_prompt_requests`
- 看到什么结果：
  - 断言失败
  - 失败 Prompt 仍然包含旧文案：
    - `根据【人工不同意原因/修改意见】和【修改模式】对世界内容做局部、精准修改，不得超范围改写。`
  - 且缺少新文案：
    - `当前模式：partial_rewrite（指定局部重写）`
- 当前判断：环境/运行时旧进程问题，不是本轮代码未实现
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：`full_rewrite` 是否仍被错误写成“局部修改”或“禁止整篇重写”。
- 怎么测：
  - 本地 Prompt 断言覆盖 world/worldview/novel/outline/chapter 五条主链路
  - 真实 HTTP 工作流回读 `full_rewrite` Prompt
- 看到什么结果：
  - `full_rewrite` Prompt 包含：`允许整体重写`
  - `full_rewrite` Prompt 不再包含：`禁止整篇重写或整体改写结构。`
  - `full_rewrite` Prompt 不再包含：`只允许局部修改，只修改用户指定范围的内容`
- 结果是否通过：通过

### 降级结果

- `5006` 旧进程未重启时，真实页面/API 会继续读到旧 Prompt。
- 本轮降级方式：
  - 不覆盖用户当前 `5006` 运行态
  - 单独拉起 `5007` 新后端完成真实验证
- 结果：能够证明代码已修复，同时把“旧进程未刷新”与“产品逻辑仍错误”分开。
- 结果是否通过：通过

### 回滚结果

- `git diff -- src/common/revision_mode_prompt.py src/agents/world_agent.py src/agents/worldview_agent.py src/agents/novel_agent.py src/agents/outline_agent.py src/agents/chapter_agent.py tests/test_hierarchy_draft_llm_invocation.py tests/test_real_agent_revision_mode_prompt_requests.py 测试计划文件.md | git apply --check -R -` 可作为本轮回滚入口
- 结果是否通过：通过

### 真实命令链验证结果

- `./.venv/bin/python -m py_compile ...` 返回 `0`
- 本地断言输出：`OK: prompt mode guidance assertions executed`
- 旧正式后端 `5006` 真实诊断：失败，读回旧 Prompt
- 新代码后端 `5007` 真实回归输出：`OK: real revision mode prompt regression executed on 5007`
- 结果是否通过：通过

### 失败项

- 无新的产品逻辑失败项。
- 发现 1 个运行时问题：
  - 旧正式后端 `5006` 仍在提供旧 Prompt 文案
  - 影响：页面/接口会继续显示旧修改说明，即使仓库代码已修复
  - 归类：`P2` 环境/运行时旧进程问题

### 缺陷归因

- 根因：
  - 用户处理节点虽然提供了 `partial_rewrite / content_rewrite / full_rewrite` 三种模式，但多个主 workflow 的 `modify_content` Prompt 仍使用静态“局部修改”说明，甚至会在 `full_rewrite` 路径里保留局部修改约束。
- 修复：
  - 新增统一修改模式提示规则 `src/common/revision_mode_prompt.py`
  - 让 `world/worldview/novel/outline/chapter` 五个主 workflow 的 `modify_content` Prompt 按用户选择的模式动态切换说明
- 运行时附加结论：
  - 旧 `5006` 进程未重启前不会自动加载新文案

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：通过
- 真实命令链验证结果：通过
- 结论：代码层面已修复“用户处理节点三种修改模式未动态切换修改说明”的问题，并通过新后端真实 HTTP 回归验证；若用户当前页面仍显示旧文案，需要让实际服务进程切换到新代码。

---

## 追加测试记录：摘要类工作流统一为 summary_rewrite 并显示独立修改模式说明

### 本次测试范围

- `src/common/revision_mode_prompt.py`
- `src/app_api.py`
- `src/agents/outline_summary_create_agent.py`
- `src/agents/outline_summary_update_agent.py`
- `src/agents/chapter_outline_summary_create_agent.py`
- `src/agents/chapter_outline_summary_update_agent.py`
- `src/agents/chapter_content_summary_create_agent.py`
- `src/agents/chapter_content_summary_update_agent.py`
- `frontend/src/pages/HierarchyWorkflow.tsx`
- `frontend/src/api/client.ts`
- `tests/test_summary_workflow_requests.py`
- `tests/test_real_summary_revision_mode_prompt_requests.py`
- `测试计划文件.md` 本轮回填

### 本次执行时间

- `2026-06-20 10:58:50 CST`

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 临时新代码验证后端：`http://127.0.0.1:5007`
- Python：`./.venv/bin/python`
- 前端构建：`frontend npm run build`
- 数据源：真实 HTTP API、真实 live LLM、本地前端构建

### 执行命令或测试入口

- 语法校验：
  - `./.venv/bin/python -m py_compile src/common/revision_mode_prompt.py src/app_api.py src/agents/outline_summary_create_agent.py src/agents/outline_summary_update_agent.py src/agents/chapter_outline_summary_create_agent.py src/agents/chapter_outline_summary_update_agent.py src/agents/chapter_content_summary_create_agent.py src/agents/chapter_content_summary_update_agent.py tests/test_summary_workflow_requests.py tests/test_real_summary_revision_mode_prompt_requests.py`
- 本地摘要 Prompt 断言：
  - `./.venv/bin/python - <<'PY' ... test_summary_workflow_prompts_are_separate_and_preserve_source_text() ... test_app_api_uses_summary_only_revision_mode_for_summary_workflows() ... PY`
- 前端构建：
  - `cd frontend && npm run build`
- 临时后端：
  - `env PORT=5007 PYTHONPATH=.:src ./.venv/bin/python src/app_api.py`
- 真实摘要工作流回归：
  - `API_BASE_URL=http://127.0.0.1:5007 ./.venv/bin/python - <<'PY' ... test_real_outline_summary_request_changes_uses_summary_rewrite_prompt_guidance() ... PY`

### 正常情况结果

- 测了什么：6 条摘要 workflow 在用户处理节点是否统一显示 `summary_rewrite` 模式说明，并且前后端都把它当成唯一合法修改模式。
- 怎么测：
  - 本地直接调用摘要 workflow 的 `build_modification_prompt`
  - 构建前端确认 `HierarchyWorkflow` 类型和页面选择器可编译
  - 真实 HTTP 工作流执行 `outline_summary_create -> request_changes(summary_rewrite)`
- 看到什么结果：
  - 本地断言输出：`OK: summary revision mode assertions executed`
  - 前端构建通过：`npm run build`
  - 真实回归输出：`OK: real summary revision mode prompt regression executed on 5007`
  - 真实 Prompt 命中：`当前模式：summary_rewrite（摘要重做）`
- 结果是否通过：通过

### 异常情况结果

- 测了什么：摘要 workflow 是否仍错误暴露 `partial_rewrite / content_rewrite / full_rewrite`，或后端仍拒绝 `summary_rewrite`。
- 怎么测：
  - 检查 `app_api._allowed_revision_modes(...)`
  - 检查前端 `修改模式` 下拉配置
  - 真实 HTTP `request_changes(summary_rewrite)` 读回 Prompt
- 看到什么结果：
  - 摘要 workflow 仅允许 `summary_rewrite`
  - 正文/大纲主 workflow 仍保留 3 种模式
  - 真实 HTTP 已接受 `summary_rewrite`，不再报 `Invalid revision_mode`
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：章节简介与总结 workflow 是否既显示 `summary_rewrite`，又继续保持“不回传 content 原文”的旧约束。
- 怎么测：
  - 本地读取 `chapter_content_summary_update_agent.build_modification_prompt(...)`
- 看到什么结果：
  - Prompt 同时包含：
    - `当前模式：summary_rewrite（摘要重做）`
    - `payload 里不要返回 content`
  - 说明这次没有把摘要返工错误改成正文重写
- 结果是否通过：通过

### 降级结果

- 本轮没有修改正式 `5006` 服务进程。
- 降级方式：
  - 用临时 `5007` 后端完成真实验证
  - 前端只做构建校验，不宣称已做浏览器页面功能验收
- 结果是否通过：通过

### 回滚结果

- `git diff -- src/common/revision_mode_prompt.py src/app_api.py src/agents/outline_summary_create_agent.py src/agents/outline_summary_update_agent.py src/agents/chapter_outline_summary_create_agent.py src/agents/chapter_outline_summary_update_agent.py src/agents/chapter_content_summary_create_agent.py src/agents/chapter_content_summary_update_agent.py frontend/src/pages/HierarchyWorkflow.tsx frontend/src/api/client.ts tests/test_summary_workflow_requests.py tests/test_real_summary_revision_mode_prompt_requests.py 测试计划文件.md | git apply --check -R -` 可作为本轮回滚入口
- 结果是否通过：通过

### 真实命令链验证结果

- `./.venv/bin/python -m py_compile ...` 返回 `0`
- 本地断言输出：`OK: summary revision mode assertions executed`
- 前端构建输出：`npm run build` 通过
- 真实摘要工作流回归输出：`OK: real summary revision mode prompt regression executed on 5007`
- 结果是否通过：通过

### 失败项

- 无新的产品逻辑失败项。
- 当前仍未做浏览器级页面功能验收，因此不能把这轮前端验证描述成完整页面功能测试通过。

### 缺陷归因

- 根因：
  - 摘要类 workflow 实际只支持 `summary_rewrite`，但此前 Prompt 没有单独说明，前端 `修改模式` 下拉也沿用正文类 3 种模式，后端公共 `respond` 还会拒绝 `summary_rewrite`。
- 修复：
  - 新增摘要专用修改模式说明
  - 6 条摘要 workflow 全部接入该说明
  - 前端摘要 workflow 改为只显示 `摘要重做`
  - 后端 `respond` 对摘要 workflow 改为只接受 `summary_rewrite`

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：通过
- 真实命令链验证结果：通过
- 结论：摘要类 workflow 已与实际能力对齐，当前统一只支持 `summary_rewrite`，并且 Prompt、前端选择器和后端校验已保持一致。

## 本次补充测试（2026-06-20 章节简介结果显示修复）

### 本次测试范围

- `frontend/src/pages/HierarchyWorkflow.tsx`
- `chapter_intro_summary_create` 工作流页对 `pending_payload.chapter_intro` / `pending_payload.chapter_summary` 的页面显示
- 真实页面入口：`/workflow?type=chapter_intro_summary_create&action=create&...&run_id=run_51d38bf9b5d2`

### 本次执行时间

- `2026-06-20 11:44:18 CST`

### 执行环境

- 仓库：`/Users/harry/Documents/git/novel_agent`
- 前端地址：`http://127.0.0.1:5174`
- 后端地址：`http://127.0.0.1:5006`
- Python：`./.venv/bin/python`
- Browser 插件：当前会话无可调用工具，按规范回退到 Playwright
- 页面截图：`/tmp/novel-agent-chapter-intro-summary-section.png`

### 执行命令或测试入口

- 真实 run 数据读回：
  - `./.venv/bin/python - <<'PY' ... urllib.request.urlopen('http://127.0.0.1:5006/api/hierarchy-agent/get?run_id=run_51d38bf9b5d2') ... PY`
- 前端构建：
  - `cd frontend && npm run build`
- 页面快照：
  - `~/.codex/skills/playwright/scripts/playwright_cli.sh open "<workflow-url>"`
  - `~/.codex/skills/playwright/scripts/playwright_cli.sh snapshot`
- 页面结果区值校验与截图：
  - Node REPL + `playwright` 打开同一 URL，读取 `getByLabel('章节简介').inputValue()` / `getByLabel('章节总结').inputValue()`，并输出局部截图到 `/tmp/novel-agent-chapter-intro-summary-section.png`

### 正常情况结果

- 测了什么：章节简介与总结已经生成成功时，工作流页是否直接显示“章节简介 / 章节总结”两个结果框，而不是只能去节点详情查看。
- 怎么测：
  - 先读真实 run `run_51d38bf9b5d2`，确认 `pending_payload` 里确实存在 `chapter_intro` 和 `chapter_summary`
  - 再打开真实 workflow URL，读取页面两个文本框的标签和值
- 看到什么结果：
  - run 读回包含 `chapter_intro`、`chapter_summary`
  - 页面快照包含 `章节简介`、`章节总结`
  - Playwright 读取结果：`introLength=72`，`summaryLength=185`
  - 局部截图能直接看到两块结果文本
- 结果是否通过：通过

### 异常情况结果

- 测了什么：页面是否仍然只有节点信息里能看到结果，而主表单没有任何可见结果区。
- 怎么测：
  - 对比真实 run 的 payload 字段与页面快照
  - 检查页面正文区下方是否出现新增结果框
- 看到什么结果：
  - 页面主表单已出现结果区，不再是“只有节点详情有内容”
  - 控制台没有新的业务错误；仅有 React Router future warning，非本次缺陷
- 结果是否通过：通过

### 特殊情况结果

- 测了什么：长章节原文占满大块空间时，结果区是否仍会被渲染，而不是因为正文过长被条件渲染掉。
- 怎么测：
  - 使用用户提供的长正文 workflow URL 与真实 `run_id`
  - 滚动到“章节简介”位置并截取结果区局部图片
- 看到什么结果：
  - 即使原文很长，结果区仍被渲染在正文下方
  - 截图中可同时看到“章节简介”和“章节总结”以及各自内容
- 结果是否通过：通过

### 降级结果

- Browser 插件工具不可调用时，没有跳过页面测试，而是回退到 Playwright 读取真实 DOM 与真实文本框值。
- 结果是否通过：通过

### 回滚结果

- `git diff -- frontend/src/pages/HierarchyWorkflow.tsx 测试计划文件.md | git apply --check -R -`
- 结果是否通过：通过

### 真实命令链验证结果

- `http://127.0.0.1:5006/api/hierarchy-agent/get?run_id=run_51d38bf9b5d2` 返回真实 payload，确认生成成功不是假象
- `cd frontend && npm run build` 返回 `0`
- Playwright 页面值校验返回：
  - `hasIntroLabel=true`
  - `hasSummaryLabel=true`
  - `introLength=72`
  - `summaryLength=185`
- 结果是否通过：通过

### 失败项

- 无新的产品缺陷失败项。
- 控制台保留 `React Router Future Flag Warning`，属于框架升级提示，不是本次结果显示问题。

### 缺陷归因

- 根因：
  - 后端已生成 `chapter_intro` / `chapter_summary`，但 `HierarchyWorkflow` 之前没有把这两个字段回填并渲染到主表单。
- 修复：
  - 为章节简介与总结工作流新增页面状态
  - 从 `pending_payload` 回填 `chapter_intro` / `chapter_summary`
  - 在正文下方增加“章节简介 / 章节总结”显示区

### 是否满足完成口径

- 正常情况：通过
- 异常情况：通过
- 特殊情况：通过
- 降级结果：通过
- 回滚结果：通过
- 真实命令链验证结果：通过
- 结论：本次“生成成功但页面无显示”的前端缺陷已完成真实页面闭环验证，就当前 `run_51d38bf9b5d2` 而言，结果不再只能通过节点详情查看。
